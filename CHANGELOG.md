# Changelog

## [Unreleased]

### Removed
- **ContentList의 분류 필터 슬롯(`.content-list__filter`) 삭제** — `sm` 전용 가로 스크롤 칩 행이었다. 거르는 일은 목록 위의 **검색바(FilterBar)** 가 맡는다.
  - **입구가 둘이면 안 된다.** 같은 화면에서 분류를 거르는 곳이 `sm`의 칩 행과 그 위의 검색바로 갈리고, **폭에 따라 어느 쪽이 진짜인지가 바뀐다.** 조회 조건은 FilterBar의 몫이고 목록은 결과를 보여주는 일만 한다.
  - **"좁히면 행의 분류를 숨긴다"는 규칙도 함께 없앴다** — 칩 행에 딸린 규칙이었지 분류에 딸린 규칙이 아니었다. 잠깐 `.content-list-container--narrowed`로 옮겨 살렸다가(같은 사이클 안에서) 되돌렸다.
    - **전제가 둘 다 칩 행의 것이었다.** ① 무엇으로 좁혔는지가 목록 **바로 위에 선택된 칩으로** 떠 있었고 ② 단일 선택이라 모든 행이 정말 같은 분류였다. 검색바의 필터는 **다중 선택**이고(아무것도 안 고른 상태가 전체 — `filter-bar.md`) 선택값은 드롭다운 트리거 안에 있다.
    - 그래서 지금 숨기면 **둘 이상을 고른 경우 진짜 다른 값을 지우고**, 하나만 골랐더라도 목록이 스스로 무엇인지 말하지 못한다. 중복이 눈에 걸리는 것보다 그쪽이 나쁘다.
    - 규칙 둘(분류 숨김 + 뒤따르는 가운뎃점 제거)이 사라지면서, `display:none`인 칸 때문에 필요했던 **인접 선택자 우회도 함께 없어졌다.**
  - 실측 — 390px: 분류가 항상 보이고 메타는 `4대보험 · 2024.03.20 · 조회 1,011`. 1000px: 분류가 열로 서고 열 이름 grid 유지. content-list.md v0.48.1 → v0.50.0 (MINOR)
  - ⚠️ **`.content-list__filter { display: none }` 한 줄은 남겼다.** 규칙을 통째로 지우니 **이미 배포된 프로토타입에서 칩이 모든 폭에 드러났다** — 전에는 base가 `display:none`이고 `sm`에서만 켜졌으므로, 규칙이 사라지자 그 div가 평범한 블록으로 되살아난 것이다. 이 시스템의 CSS는 Pages로 프로토타입에 바로 실리므로 **제거한 슬롯은 닫히는 쪽으로 실패해야 한다** — 여기서 지운 것이 남의 화면에서는 지워지는 게 아니라 드러난다. 남은 마크업이 없어지면 이 한 줄도 지운다. (실측: 1200·390 모두 `display:none`, 높이 0, 헤더 밑선 1px 유지) content-list.md v0.50.0 → v0.50.1 (PATCH)
  - ⚠️ **넘겨받는 쪽의 한계를 재 뒀다.** 390px에서 FilterBar는 **분류 하나 + 검색** 구성이면 358px로 들어가지만(넘침 0), 문서의 4필터 예시(드롭다운 3 + 기간 + 검색)는 **183px 넘쳐** 페이지가 가로로 스크롤된다(문서폭 556). FilterBar에는 `sm` 규칙이 없다 — 게시판이 쓰는 구성은 지금도 되지만, 필터가 여럿인 화면은 FilterBar 쪽에서 풀어야 한다.
- **프로토타입 셸의 보기 모드 세그먼트(시나리오↔인터랙티브) 삭제** — 하이파이를 실제로 만들어 보니 가르는 값어치가 없었다.
  - **두 모드는 실은 하나였다.** `data-step-next` 리스너도 오버레이도 컴포넌트 init도 **모드를 확인한 적이 없다** — 시나리오 보기에서도 버튼은 눌렸고 화면은 넘어갔다. 구 템플릿으로 재서 확인했다(시나리오 모드에서 「다음」 → 패널 입력→약관).
  - **게다가 지도가 거짓말을 했다.** 그렇게 넘어가도 nav의 활성 표시는 따라오지 않아 **화면은 2단계인데 목록은 1단계**가 켜져 있었다(같은 실측).
  - 이제 하나다. 어느 시나리오에 있든 그 화면의 인터랙션이 그대로 돌고, 스텝으로 넘어가면 `syncNav`가 목록의 현재 위치를 옮긴다 — nav가 점프 컨트롤이자 "지금 여기" 표시가 됐다.
  - **잃은 것이 없다.** 인터랙티브 보기의 실제 동작은 "nav를 숨기고 1단계부터"였는데, 크롬이 그걸 두 가지로 이미 한다 — **사이드바 접기**(36px 레일만 남음)와 **폭 미리보기**(틀 안은 크롬이 아예 없음). 덤으로 로우파이·하이파이가 같은 셸을 쓰게 됐다(차이는 JS가 붙었는가뿐). planner.md · figma.md
- **ContentHeader · AttachmentList 미착수 종결** — 만들지 않기로 결정했다. 상세 화면(지식센터 통합자료실)을 실제로 조립해 보니 **새 CSS 한 줄 없이** 기존 컴포넌트만으로 나온다. 두 요청은 사실 하나였다: 작성자·작성일·조회수도, "첨부파일: 파일 목록"도 같은 **이름-값 쌍**이고, 그걸 세로로 쌓는 컴포넌트가 이미 있다 — 정보 테이블(`table--info table--dense`). 행 헤더(`table__head-cell table__row-header`)가 이름, 셀이 값이며 첨부는 `colspan`으로 값 칸을 늘린 같은 구조의 행이다. 첨부 한 줄은 `.icon icon--sm` + `.link` + `.text-helper`, 하단 목록은 `.btn`. 390px에서 가로 스크롤 없이 렌더되는 것을 확인했다. FileUpload의 `file-card`는 썸네일 격자 + 삭제 버튼이라 읽기 전용 다운로드에 맞지 않아 재사용하지 않는다. 조립 패턴·근거와 **다시 필요해지는 조건**(첨부가 이미지 위주가 될 때)을 `_requests.md`에 남겼다. REQ-001 우선순위 1에서 남은 신규 컴포넌트는 **Card 하나**다. _requests.md v0.6.0 → v0.7.0 · components/_index.md v1.6.0 → v1.7.0
- **ContentBody 삭제** (같은 사이클에 추가했다가 되돌림). 컴포넌트의 경계를 잘못 그었다 — 상세 화면에서 시스템이 책임질 부분은 본문 **주위**(제목부·첨부·하단 네비)이고, 본문 안쪽은 에디터가 만들고 에디터가 스타일링한다. 만들었던 문서가 다룬 것(태그 선택자 스코프·세로 리듬·표 스크롤 래퍼)은 전부 본문 **안쪽** 이야기였다. 본문 밖은 기존 컴포넌트로 충분하다: 페이지 제목·breadcrumb은 `.text-page-title`+Breadcrumb, 글 제목·메타는 타이포 유틸 조립, 하단 목록 버튼은 Button. 새 Organism이 아니라 **표기 규칙**(메타 순서·구분자)이 필요했고 그건 ContentHeader의 몫이다. `_requests.md`에 결정 근거와 **다시 필요해지는 조건**(조회 화면에 에디터 CSS가 따라오지 않는 경우 — 본문 글자가 시스템 폰트로 나오는지로 판별)을 남겼다. components/_index.md v1.5.0 → v1.6.0 · _requests.md v0.5.0 → v0.6.0

### Added
- **넘어간 화면도 같은 폭에서 열린다** — `sm`에서 목록으로 갔으면 목록도 `sm`으로 뜬다. 전에는 어느 폭에서 넘어가든 다음 화면이 `lg`로 열려서, 모바일을 보다가 한 번 넘어가면 매번 다시 `sm`을 눌러야 했다.
  - 보고 있던 모드를 주소에 `?proto-view=sm`으로 남기고(`replaceState`), 최상위를 옮길 때 함께 넘긴다. 첫 모드는 그 값이 정한다(없거나 모르는 값이면 `lg`).
  - **폭을 세 곳에 싣는다 — 하나만 믿으면 끊긴다.** ① 틀의 주소(`?proto-frame=1&proto-view=sm`) ② 바깥이 iframe에 적어 둔 `dataset.view` ③ 직전 문서(`document.referrer`)의 값. 특히 ③이 실제 경로다 — 틀 안 A에서 B로 넘어오면 A가 referrer이고, A의 주소에 폭이 들어 있다. 그래서 **바깥 페이지가 옛 번들이어도** 폭이 이어진다.
  - 덤으로 새로고침·링크 공유에서도 폭이 유지된다 — 주소가 상태를 들고 있기 때문이다.
  - 실측 — ① 새 번들: A에서 sm 선택 → 틀 주소 `proto-view=sm&proto-frame=1&…` → 「목록으로」 → 최상위 `_pageB.html?proto-view=sm`, 활성 `sm`. ② **규칙이 전혀 없는 바깥**이 A를 틀에 띄운 경우: A → B 이동 후에도 최상위 `_pageB.html?proto-view=sm`, 틀 390×844. 콘솔 오류 없음. build.py · planner.md
- **ContentList에 목록 끝 슬롯(`.content-list__end`) 추가 — Pagination이거나 「더 보기」 하나.** 좁은 화면에서 번호보다 이어 붙이는 편이 나은 목록이 있다는 요청(REQ-005)을 **새 컴포넌트 없이** 닫았다.
  - **없던 것은 컨트롤이 아니라 자리였다.** 버튼도(`btn--secondary btn--solid`), 로딩 상태도(`btn--loading`), 끝났다는 한 줄도 이미 있는 것으로 된다. 없던 것은 그것들이 설 자리였고, 프로토타입마다 `.kc-pager` 같은 자리를 새로 만들며 간격이 제각각이었다.
  - **"화면이 고르는 방식"을 한 단계 더 좁혔다 — 고르는 것은 화면이지 폭이 아니다.** 번호와 「더 보기」는 모양이 아니라 **데이터 모델**이 다르다(갈아 끼우기 vs 쌓기). 한 화면이 폭에 따라 둘 다이면 창을 줄이는 순간 지금 상태의 뜻이 바뀌고 복귀 규칙도 두 벌이 된다. 좁아서 번호가 불편한 것뿐이면 `pagination--simple`이 먼저다.
  - **함께 정할 것 넷에 답을 못박았다.** ⓐ 불러오는 중은 **같은 버튼**의 `btn--loading` — 목록 아래에 스피너 블록을 얹으면 버튼이 밀려 **손가락 아래 있던 것이 바뀐다.** ⓑ 붙은 뒤에는 **새로 붙은 첫 항목의 링크로 포커스**를 옮긴다(도착 지점이 이걸로 정해진다). ⓒ 마지막에 닿으면 버튼을 **`__end-note` 한 줄로 바꾼다** — 버튼만 지우면 눌리던 자리가 빈 여백이 되어 사람들이 그 자리를 다시 누른다. ⓓ 복귀는 `replaceState`로 불러온 양을 URL에 남기고(`pushState`면 뒤로가 목록 안에서 되감겨 **상세에서 목록으로 나갈 수 없다**), 돌아왔을 때 **그 양을 먼저 그린 뒤** 스크롤·앵커를 복원한다. 이 비용을 감당 못 하면 Pagination을 고른다.
  - **버튼은 fill이 아니라 solid다.** 목록을 잇는 보조 액션이지 화면의 결정이 아니다 — 검은 면으로 두면 목록 끝에서 가장 무거운 것이 「더 보기」가 된다(렌더로 확인하고 바꿨다).
  - **자동 로드(무한 스크롤)는 넣지 않았다** — 목록 끝에 닿을 수 없고, 사람이 멈출 수 없고, 복귀가 더 어렵다.
  - **문서 미리보기에 「더 보기」 세 상태를 넣었다** — 첫 미리보기에는 Pagination만 넣어서, 문서만 봐서는 「더 보기」가 있는 줄 알 수 없었다(기본·불러오는 중·마지막을 나란히 둔다). 실측 — 390·900px에서 Pagination·「더 보기」·불러오는 중·마지막 네 상태 모두 넘침 없이 렌더, 버튼 폭은 `max-width: 20em`에서 267px로 멈춘다(1000px짜리 띠가 되지 않게). content-list.md v0.50.1 → v0.51.1 · pagination.md v1.1.0 → v1.2.0 · _requests.md v0.10.0 → v0.11.0
- **FilterBar `sm` — 검색만 남기고 필터는 「필터」 버튼 뒤의 시트로** (REQ-006). 문서가 스스로 "sm 규칙이 없다"고 적어 둔 자리였고, 실측도 그대로였다: 390px에서 드롭다운 3 + 기간 + 검색이 바(358px) 안에서 **183px 넘쳐** 페이지가 가로로 스크롤됐다.
  - **처음엔 접기만 했다가 되돌렸다.** 바 프레임을 버리고 컨트롤이 각자 테두리를 갖고 접히게 하면 넘침은 사라지지만, **36px짜리 표적이 다섯 개 늘어선다.** 누를 것이 많아질수록 잘못 누른다 — 그래서 표적을 「필터」 하나로 모으고, 실제 선택은 넓은 시트 안에서 큰 행(높이 36 · 폭 342)으로 하게 바꿨다.
  - **검색은 시트에 넣지 않는다.** 목록을 좁히는 가장 잦은 행위라 탭을 한 번도 더 들이지 않는다. 「필터」와 한 줄에 나란히 선다.
  - **마크업은 한 벌이다.** 시트는 Modal 마크업 그대로이고, `md` 이상에서는 그 껍데기(overlay·modal·body)를 **`display: contents`** 로 없애 안의 컨트롤이 바의 칸으로 그대로 선다. 폭별로 마크업을 두 벌 두면 선택 상태가 두 곳에 생긴다 — ContentList의 칩 행에서 겪은 그대로다.
  - **`role="dialog"`는 `sm`에서만 켠다.** md에서는 시트가 바의 칸일 뿐인데 역할이 남아 있으면 스크린리더가 "대화상자"라고 읽는다. 보이는 것과 읽히는 것이 어긋나면 안 된다.
  - **시트 안에서는 드롭다운이 열린 채로 선다** — 모달 본문이 스크롤 컨테이너라 겹쳐 뜨는 패널은 잘리고, 무엇보다 **겹치는 층을 하나 더 만들지 않는 것이 이 화면의 목적**이다. 라벨은 `data-placeholder`가 댄다(마크업을 더하지 않는다). **기간(DRP)만 예외** — 달력·단축·확인/취소가 든 판이라 펼치면 시트가 달력 화면이 된다. 시트 위로 올라오는 한 겹 더의 판으로 띄웠다(제 확인/취소로 닫힌다).
  - **명시도로 두 번 졌다.** ① ghost의 "테두리 없음"이 `.dropdown--button.dropdown--ghost .dropdown__trigger`(0,3,0)라 `.filter-bar__bar .dropdown__trigger`(0,2,0)로는 지고 소스 순서로도 못 이긴다. ② `display: contents`를 `.filter-bar__sheet`(0,1,0)로 걸었더니 뒤에 실리는 `.modal-overlay`의 `display: flex`가 이겨 **md인데도 필터 전체가 화면 한가운데 오버레이로 떴다.** 둘 다 한 단계 올려 순서에 기대지 않게 고쳤다.
  - **닫힌 드롭다운 패널의 `pointer-events: none`도 되돌려야 했다** — 보이기만 하고 이걸 빠뜨리면 옵션이 눈에는 보이는데 눌리지 않는다(첫 렌더에서 그랬다).
  - 실측(390px) — 바 358×36 한 줄, 넘침 0, 문서폭 390. 시트 390×635에 옵션 10개가 한 화면. 옵션 둘을 고르면 배지는 **1**(필터 개수이지 옵션 수가 아니다). 「적용」·`Escape`·배경 탭 모두 닫히고 포커스가 「필터」로 돌아오며 배경 스크롤 잠금이 풀린다. 초기화로 배지가 사라진다. DRP는 화면 밖으로 나가지 않는다(0,80,390×720). 1200px: 한 줄 36px, 트리거 테두리 `rgba(0,0,0,0)` 그대로(데스크톱 모습 불변). 콘솔 오류 없음. filter-bar.md v0.11.0 → v0.13.0
- **프로토타입 크롬에 사이드바 접기와 폭 미리보기 추가** (`initProtoChrome` · `_PROTO_CHROME_CSS`). 브라우저 크기를 건드리지 않고 데스크톱부터 모바일까지 확인한다.
  - **접기** — 시나리오 사이드바가 폭에 상관없이 152px + 여백을 먹어 390px 확인이 사실상 불가능했다. 토글을 누르면 **36px 레일**로 접히고, 900px 미만이면 자동으로 접힌다. 사람이 한 번 접거나 편 선택은 `sessionStorage`에 남아 자동 판정보다 우선한다.
  - **접어도 폭 컨트롤은 레일에 남긴다.** 처음엔 토글만 남겼는데, 접기의 목적이 "좁은 폭에서 실제 화면 보기"인데 접으면서 폭 전환이 사라지면 **재려던 도구를 제 손으로 치우는 셈**이다(자리가 모자라면 틀을 켜며 자동으로 접히므로 실제로 그 상태에 빠졌다).
  - **`비교` — lg·md·sm을 한 화면에 나란히.** 배율은 **하나**로 묶었다. 셋을 각자 칸에 맞춰 키우면 모바일이 데스크톱보다 좁다는 사실 자체가 그림에서 사라진다.
  - **`lg`·`md`·`sm`은 1:1**, 그리고 **틀 모서리를 끌면 그 사이의 임의 폭**이 된다. 레이아웃이 무너지는 폭은 대개 프리셋 사이에 있어 세 점만으로는 어디서 무너지는지 알 수 없다. 이름 있는 폭을 벗어나면 **어느 버튼도 켜지지 않고** 실제 px이 표시된다 — 848px을 보면서 `lg`가 눌려 있으면 그 표시가 거짓말이 된다. 틀이 가운데 정렬이라 폭은 이동거리의 **두 배**로 바꿔 모서리가 손끝을 따라오게 했다.
  - **iframe이어야 한다.** 미디어쿼리는 컨테이너가 아니라 **뷰포트**를 본다 — `.proto-content`의 폭만 줄이면 `max-width: 767px` 규칙이 걸리지 않아 데스크톱 레이아웃을 좁은 상자에 욱여넣은 그림이 나온다. 확인하려던 것과 정반대이고, 그걸 보고 판단하면 틀린 결론에 이른다. `transform: scale`도 레이아웃 뷰포트를 건드리지 않아 비교 모드의 안쪽은 여전히 1280·768·390으로 계산된다.
  - **재는 도구는 자기 자신이 정확해야 한다** — 세 가지를 실측으로 잡았다. ① `box-sizing: content-box`(border-box면 테두리 2px을 빼앗겨 sm이 388이 되고, 390에서만 갈리는 규칙을 확인할 수 없다). ② 가로 스크롤 + 자동 접힘(`max-width:100%`가 틀을 창에 맞춰 줄여 lg 버튼이 1280을 말하면서 1095를 보여줬다). ③ 배율에 곱해지지 않는 것들(테두리 2px×3·gap·레이블 줄)을 먼저 빼기(빼지 않으니 셋째 틀이 20px 잘렸다).
  - **높이 계산이 무효였다** — `calc(100vh - var(--space-40))`인데 스케일에 `--space-40`이 없다(32 다음이 48). calc이 무효 → height auto → **iframe 기본 높이 150px**이라 틀 안에 제목 줄만 보였다. 폭만 재고 있어서 숫자 검증은 전부 통과했고, 스크린샷 한 장이 잡아냈다. `calc(100vh - var(--space-20) * 2 - var(--stroke-sm) * 2)`로 고쳤다.
  - **틀 안은 크롬을 벗는다**(`?proto-frame=1`) — 시나리오는 `?scenario=`로 함께 넘어가고, 바깥에서 시나리오를 바꾸면 세 틀이 모두 따라간다. 틀 안에서 시나리오를 적용하는 시점은 **파싱 뒤로 미뤘다** — 프로토타입 파일의 전환 리스너보다 먼저 클릭하면 아무도 듣고 있지 않아 조용히 무시된다.
  - 실측(1440×900) — 세 틀의 `innerWidth` 1280 / 768 / 390, `max-width:767px` 규칙은 sm에서만 참, 비교 행이 칸 안에 들어감(1348/1352, 1280×800에서도 1190/1192), 페이지 넘침 없음, 시나리오 전환이 세 틀에 반영, 끌기 1280 → 848이 표시·틀 안 `innerWidth` 양쪽에서 일치, 콘솔 오류 없음. build.py · planner.md
- **프로토타입 크롬에 `화면 목록` 링크(`.proto-back`) 정식 편입.** 템플릿이 주지 않아 프로토타입들이 Button 클래스로 각자 만들어 쓰고 있었다 — `btn--ghost btn--sm`이라 **32px 높이·13px·본문색**이라서, 화면을 빠져나가는 탈출구가 바로 아래 시나리오 목록과 무게가 같았다. 그룹 레이블과 같은 급(**22px·11px·subtle**, 아이콘 16→12px)으로 내렸다. 규칙을 `.proto-sidebar .proto-back`으로 쓴 이유 — 크롬 CSS가 컴포넌트 CSS보다 **먼저** 실려 특이도가 같으면 소스 순서로 `.btn`이 이긴다. 한 단계 올려두면 `btn` 클래스를 아직 달고 있는 기존 프로토타입 파일도 고치지 않고 새 무게를 따라간다. build.py · planner.md
- **REQ-004 접수 — Tab의 `sm` 드롭다운 전환(`tab--collapse`). 만들지 않기로 하고 조건과 함께 보류했다.**
  - **요청자가 스스로 단 단서가 결론이었다** — "이 화면의 탭은 페이지 이동을 대체하고 있고, TopNav·SidebarNav가 구현되면 이 자리가 탭이 아니게 된다." 그렇다면 지금 만드는 것은 **내비게이션 문제를 Tab의 variant로 푸는 것**이고, 그 자리가 제 컴포넌트를 찾는 순간 소비자가 0이 된다. 이 사이클에서 같은 실수를 이미 세 번 했다(ContentBody·ContentHeader·AttachmentList — 전부 필요해 보였고 전부 지웠다).
  - **현재 동작이 깨지지 않는 것을 재서 확인했다.** 그 화면의 탭 6개를 390px에서 `tab-scroller`로 렌더 — 트랙 326px에 내용 518px, **6개 중 3개가 온전히 보이고** 4번째가 걸치며 다음 화살표가 뜬다. 좁지만 "어디에 있고 더 있다"가 읽힌다.
  - **드롭다운은 형태가 다른 게 아니라 정보를 버린다.** 탭의 값은 형제를 보여주는 것이다 — 스크롤은 "여섯 개가 있고 지금 첫 번째"를 유지하지만 드롭다운은 "지금 첫 번째"만 남긴다. 가장 좁은 화면, 즉 방향 감각이 가장 필요한 자리에서 그걸 버린다.
  - **되살아나는 조건을 숫자로 적었다** — ① TopNav·SidebarNav 구현 뒤에도 진짜 패널 전환 탭이 스크롤로 감당되지 않는 화면이 나오거나 ② 390px에서 온전히 보이는 탭이 **1개 이하**가 될 때(지금은 3개).
  - **함께 물어본 네 가지에 지금 답을 정해 남겼다** — 되살아났을 때 처음부터 다시 생각하지 않도록. ⓐ `display:none`은 **접근성 트리에서 실제로 제거하므로** CSS로 갈라도 요구 조건은 만족한다. 진짜 비용은 `id`·`label`이 두 벌이 되고 `aria-controls`가 같은 패널을 두 곳에서 가리키는 것이라, **JS로 바꿔 그리는 쪽**이 낫다. ⓑ 상태는 두 컨트롤에 각각 두지 않고 **패널의 `hidden` 하나를 진실**로 삼아 파생시킨다. ⓒ 키보드가 가장 큰 부담이다 — `role="tablist"`는 방향키+로빙 탭인덱스, `select`는 네이티브 목록 탐색이라 **같은 자리에서 키보드 계약이 폭에 따라 달라진다.** ⓓ 선택 기준 표의 축은 탭 수 × 라벨 길이이고 판정은 "390px에서 온전히 보이는 탭이 2개 이상인가" — 재서 정할 값이다.
  - tab.md 제약에 "좁은 화면에서 드롭다운으로 바꾸지 않는다"와 REQ-004 참조를 적었다. tab.md v0.8.2 → v0.8.3 · _requests.md v0.9.0 → v0.10.0
- **CommentList(Organism) 추가** — 목록 + 작성 폼 + 수정·삭제 + 답글 한 단계. 게시글 상세 프로토타입이 댓글을 Avatar와 타이포 유틸로 조립하며 남긴 요청(REQ-003)을 닫았다.
  - **"필요해지면"이 아니라 이미 필요했다.** 요청은 "작성 폼·수정·대댓글이 필요해지면 정식 컴포넌트가 있어야 한다"였는데, **읽기만 되는 댓글란은 게시판이 아니다** — 작성 폼과 수정·삭제는 조건부가 아니라 기본이다. 그래서 셋을 한 번에 넣었다. 답글만 **자리를 열고 쓸지는 제품이 정하도록** 남겼다(구조를 지금 안 정하면 나중에 마크업을 다시 짜야 한다).
  - **작성 폼은 목록 아래다.** 게시판 댓글은 읽고 나서 쓴다 — 위에 두면 화면을 연 사람이 가장 먼저 만나는 것이 빈 입력창이고, 남의 말을 읽기 전에 쓰라는 화면이 된다. SNS 타임라인은 쓰는 것이 첫 행동이라 위가 맞지만 글에 딸린 댓글은 다르다. 답글 폼만 예외로 답할 댓글 바로 아래에 연다.
  - **`.comment`는 flex가 아니라 grid다.** 처음 flex로 짰더니 답글 목록(li의 셋째 자식)이 아바타·본문 **옆**에 세 번째 칸으로 붙었다(실측: 답글 본문이 부모 본문보다 434px 오른쪽). grid로 바꾸니 답글이 본문 아래 둘째 줄에 서고, 그 줄이 본문과 같은 열이라 **답글의 아바타가 부모 본문의 시작점에 정확히 선다** — 들여쓰기를 px로 잴 필요가 없어졌다(실측 부모 본문 150 = 답글 아바타 150).
  - **답글은 한 단계까지.** 두 단계를 넘기면 좁은 화면에서 들여쓰기가 본문 폭을 먹고 누구에게 하는 말인지 오히려 흐려진다. 한 단계뿐이라 `sm`에 따로 줄 것이 없다 — 390px에서도 본문이 354px 남는다.
  - **본문은 15px**(`--font-size-lg`)이다. 글 본문(board 사다리 17px)과 같게 두면 댓글이 글만큼 무거워져 무엇이 글이고 무엇이 반응인지 흐려진다.
  - **메타의 구분자는 CSS가 넣는다**(`time::before`). 마크업에 「·」를 적으면 「작성자」 뱃지가 붙고 빠질 때마다 구분자 개수를 손으로 맞춰야 한다.
  - **편집 폼은 마크업에 두고 상태로 보인다** — JS가 만들어 넣으면 라벨·id·버튼 문구가 JS 문자열에 갇혀 화면마다 손댈 수 없다. `initCommentList`는 **화면 상태만** 바꾸고 등록·저장·삭제의 전송은 호스트가 맡는다.
  - **권한 없는 버튼은 그리지 않는다** — 남의 댓글에 비활성 수정·삭제를 남기면 누를 수 있는 것처럼 보이고 자리만 차지한다. 답글이 달린 댓글을 지울 때만 `comment--deleted`로 자리를 남긴다(대화의 고리가 끊기므로).
  - **댓글이 달린 상태로 보니 세 군데가 어색했다** — 실제 게시글 카드 안에 4개를 넣고 다시 봤다.
    - **작성 폼이 네 번째 댓글처럼 읽혔다.** 마지막 댓글과 간격이 댓글 사이와 같은 16px인 데다, 아바타 + 오른쪽 블록이라는 **모양까지 항목과 똑같다.** 같은 모양이 나란히 서면 같은 것으로 읽힌다. 컨테이너의 **직계** 폼만 골라(`.comment-list-container > .comment-form`) 선 하나와 24px 여백으로 뗐다 — 수정 폼·답글 폼은 항목 안에 있어 이 규칙에 걸리지 않는다. 댓글이 없을 때는 선을 지운다(빈 상태가 상자에 갇힌 것처럼 보인다).
    - **첫 답글 위의 선을 지웠다.** 들여쓴 자리에서 시작하는 토막 선이 부모 댓글 안에 떠서, 이어지는 말인 답글이 별개 항목으로 갈려 보였다. 답글이 둘 이상일 때 그들 **사이**에만 선이 필요하다.
    - **액션 줄이 본문의 마지막 줄처럼 읽혔다.** `btn--ghost`의 기본색이 `--color-text-body`라 「답글 수정 삭제」가 **댓글 본문과 완전히 같은 색**이었다 — 2px 작을 뿐이니 문장의 연장으로 읽히는 게 당연하다. `--color-text-subtle`로 내리고 본문에서 4 → 8px 띄웠다.
      - 브랜드 색·가운뎃점 구분자·12px도 나란히 렌더해 봤다. **브랜드 색**은 댓글마다 파란 단어가 셋씩 붙어 목록 전체가 파래지고 이미 파란 「작성자」 뱃지와 겹친다. **가운뎃점**은 메타 줄이 이미 다른 뜻(값 구분)으로 쓰고 있어 같은 기호가 두 뜻을 진다. **12px**은 시스템의 최소 버튼 글자(13px)보다 작다.
      - 결과적으로 **본문만 진하고 그 위(메타)·아래(액션)는 물러나는** 한 규칙이 됐다. 대비 4.51:1로 텍스트 기준을 넘고, 누를 수 있다는 것은 ghost의 hover 배경이 말한다.
      - ⚠️ Organism이 Button의 색을 덮은 첫 사례다. 소비자가 하나뿐이라 지역 override로 뒀고, **같은 요구가 한 번 더 나오면 Button에 `ghost-subtle` 같은 variant로 올린다**고 주석에 적었다.
    - 제목과 목록 사이를 16 → 20px로 넓혔다. 댓글 사이와 같으면 제목이 목록의 첫 항목처럼 붙는다. comment-list.md v0.1.0 → v0.1.2
  - Chromium 실측 — 빈/공백 입력에서 등록 잠김, 글자 수 갱신, 수정 진입 시 본문↔편집폼 전환, 답글 정렬, 삭제됨 항목의 왼쪽 정렬(들여쓰기 0). 콘솔 오류 없음. components/_index.md v1.8.0 → v1.9.0 · _requests.md v0.8.0 → v0.9.0
- **FileUpload — 이미지가 아닌 파일의 미리보기를 확장자 글자로** (`file-upload-item__ext`). `pdf`·`xlsx`·`hwp`·`html`을 붙이면 data URI가 그대로 `<img>`에 들어가 **깨진 이미지 아이콘**이 떴다. 이제 이미지만 썸네일이고 나머지는 `PDF`·`XLSX`·`HWP`·`HTML`이 글자로 선다.
  - **판정은 확장자 문자열이 아니라 MIME**(`file.type`)으로 한다 — 확장자는 사용자가 바꿔 붙일 수 있다. 확장자는 **표시에만** 쓴다(점이 없으면 `FILE`).
  - **아이콘이 아니라 글자인 이유** — 아이콘은 같은 계열을 하나로 뭉갠다(`xls`/`xlsx`, `doc`/`hwp`). 글자는 파일이 말하는 그대로이고 새 형식이 생겨도 그릴 것이 없다. 시스템에 `icon-pdf`·`icon-excel`이 있지만 **나머지를 받을 일반 파일 아이콘이 없어서**, 절반은 아이콘 절반은 글자가 되면 규칙이 없어 보인다.
  - ⚠️ **PDF 1페이지 렌더링은 넣지 않았다.** 브라우저에서 PDF를 그리려면 외부 렌더러(PDF.js 등)가 필요한데 `components.js`는 **의존성이 없는 것이 규칙**이고, 렌더러 하나를 넣으면 모든 화면이 그 용량을 진다. 대신 **자리를 열어 뒀다** — 서버가 만든 썸네일을 `img.file-upload-item__thumb`으로 넣으면 그대로 표시된다. 문서 썸네일은 원래 서버 몫이다(한 번 변환해 캐시하면 모두가 재사용하는데, 브라우저에서 그리면 열어보는 사람마다 다시 그린다). **아직 올리지 않은 파일**에는 서버 썸네일이 없으므로 그때는 확장자 글자가 맞는 표시다.
  - **HEIC처럼 MIME은 `image/*`인데 브라우저가 못 그리는 형식**도 `img`의 `error`로 받아 확장자로 떨어진다. 이때 **라이트박스도 함께 닫는다** — 보이는 것만 바꾸고 리스너를 남기면 눌리는데 빈 라이트박스가 열린다.
  - **라이트박스는 이미지에만 열린다.** 볼 것이 없는 파일에 확대 인터랙션을 붙이면 눌리는데 아무 일도 안 일어난다. 다운로드·삭제는 모든 파일에서 그대로다.
  - 실제 파일 7종(png·pdf·xlsx·hwp·html·확장자 없음·heic)을 Chromium에서 붙여 확인했다 — 썸네일/확장자 분기, `cursor`·overlay 유무, 클릭 시 라이트박스가 png에서만 열리는 것까지. file-upload.md v0.6.2 → v0.7.0
- **아바타 식별색 팔레트 추가** (`--color-avatar-*` 16 · `--color-surface-avatar-1~8` · `--color-text-avatar-1~8`) **+ Avatar `avatar--c1`~`c8`** — 게스트·비회원 댓글에서 작성자를 서로 구분해야 한다는 요청(REQ-002)을 닫았다. Avatar를 만들 때 "팔레트가 먼저 있어야 한다"를 제약으로 적어 뒀고, 그 조건이 충족된 것이다. `c1` 파랑 · `c2` 초록 · `c3` 주황 · `c4` 보라 · `c5` 자홍 · `c6` 청록 · `c7` 연두 · `c8` 남보라.
  - ⚠️ **대비를 3.72 → 4.5:1로 올렸다. 요청받은 값과 다르다.** 문서 미리보기의 더미는 **사람 아이콘**, 즉 그래픽이라 비텍스트 기준(3:1)만 넘기면 됐다. 그런데 팔레트의 주된 쓰임은 **이니셜**이고 이니셜은 텍스트라 **4.5:1이 하한**이다. 같은 값을 두 용도로 쓸 수 없어 텍스트 기준에 맞췄다 — 사람 마크는 4.5:1이면 여유 있게 통과하므로 잉크는 하나면 된다.
  - **상태색과 겹치지 않게 따로 둔다.** 팔레트의 green·orange·red를 사람에게 쓰면 목록에서 빨간 아바타가 "오류"로 읽힌다. 새 원시 팔레트를 두되 **50~950 램프는 만들지 않았다** — 각 색조가 `-100`(면)·`-600`(잉크) 두 자리만 갖는다. 쓰는 곳이 아바타 하나뿐이라 중간 단계를 두면 다른 용도로 새어 나간다.
  - **잉크 명도는 색조마다 다르다**(L 30~64). 같은 명도를 쓰면 대비가 3.1~6.9로 널뛰어 초록·청록은 흐리고 남보라는 새까맣다. 색조별로 4.5:1에 닿는 **가장 밝은** 값을 잡아 실측 **4.50~4.80:1**로 모았다.
  - **배정은 결정적이다** — `avatarColorIndex(key)`(31진법 누적 해시 → 1~8). 난수를 쓰면 새로고침마다 같은 사람의 색이 바뀌어 "색이 사람을 가리킨다"는 전제가 무너진다. `components.js`에 번들되므로 프로토타입이 그대로 부르고, 서버 렌더링이 있으면 같은 식을 서버에서도 써야 첫 화면과 갱신에서 색이 튀지 않는다.
  - **이름을 모르면 로고가 아니라 사람 마크다.** 식별색이 붙었는데 사진도 이니셜도 없으면 그 사람의 잉크로 칠한 사람 마크를 그린다 — 로고는 "우리 쪽"을 뜻해서 특정 사람 자리에 두면 뜻이 어긋난다.
  - **값 교체를 선택자 싸움이 아니라 프로퍼티 교체로 만들었다.** 처음엔 `avatar--c*`가 로고 규칙(`.avatar:not(:has(…))`, 특이도 0-2-0)에 져서 로고와 사람 마크가 **겹쳐 그려졌다.** 면·잉크·마크를 `--avatar-surface`·`--avatar-ink`·`--avatar-mark`로 빼고 기본값을 `:where(.avatar)`(특이도 0)에 두니, 색 클래스가 순서나 특이도에 기대지 않고 그냥 이긴다. 선택자 8개짜리 override 두 벌도 사라졌다.
  - ⚠️ **색은 읽기 보조이지 신원이 아니다.** 여덟 색이 돌고 동명이인은 색이 겹친다. 색만으로 사람을 가르는 배치를 만들지 않는다 — 이름이 항상 함께 있어야 하고, 색을 못 보는 사람에게 잃는 정보가 없어야 한다. 색을 늘려 해결하지 않는다(구분 안 되는 이웃 색이 생긴다).
  - `:::palette` 디렉티브가 하이픈 이름을 받도록 정규식을 `(\w+)` → `([\w-]+)`로 넓혔다(옆 줄의 `:::scale`은 이미 그랬다). 여덟 색조를 `:::palette avatar-1`~`-8`로 그린다.
  - Chromium 실측 — 여덟 짝 모두 4.50~4.80:1, 사람 마크가 제 잉크를 받고, 색이 붙은 아바타에서 로고가 사라지며, 기본 아바타의 로고는 그대로다. 해시는 같은 열쇠에 같은 값을 낸다. 콘솔 오류 없음. tokens/color.md v1.10.0 → v1.11.0 · avatar.md v0.2.3 → v0.3.0 · _requests.md v0.7.0 → v0.8.0
- **김반장 로고 자산 반영 + Avatar 기본값이 실제 로고가 됐다.** Figma 로고 세트(layout × 배경 × svg·png)를 받아 정리하고 `embed_logo.py`로 심볼을 `--avatar-logo`에 박았다.
  - **이름을 시스템 어휘로 바꿨다** — `_layout=full, background=dark.svg` 같은 Figma 내보내기 이름은 `background=`가 **자산의 색이 아니라 놓일 배경**을 뜻해(dark가 흰 글씨다) 읽는 사람이 반대로 이해한다. `logo-kbz.svg`(심볼) · `-mono` · `-full` · `-full-inverse` · `-full-mono`로 정리했다. `inverse`는 시스템이 이미 쓰는 말이다(`--color-text-inverse` = 어두운 배경 위 텍스트).
  - **`only symbol`의 light와 dark는 시각적으로 같아 하나만 남겼다** — 심볼에는 글자가 없어 배경별 변형이 생기지 않는다(그라디언트 id만 달랐다). 같은 그림이 두 이름으로 있으면 갈라진다.
  - **Avatar는 심볼만 쓴다.** 이름까지 든 `full`은 가로로 길어 24~48px 원 안에서 글자가 뭉갠다.
  - **면 색을 연한 브랜드에서 중립으로 되돌렸다.** 마크가 이미 파랑·노랑을 갖고 있어 blue-50 면 위에서는 마크의 파랑과 겹쳐 탁해진다. 이니셜과 **같은 면**(`surface-subtle`)을 쓰게 해 규칙도 한 줄 줄었다 — 브랜드 색은 면이 아니라 마크가 낸다.
  - **`background-size`는 72%다.** 62·72·82%를 네 크기에 나란히 렌더해 골랐다 — 82%는 원 밖으로 넘치고 62%는 면 안에서 뜬다.
  - ⚠️ **`xs`(24)에서는 3D 마크가 색 덩어리로 읽힌다.** 아트워크가 그라디언트와 입체 면을 가진 마크라 24px에서는 형태가 안 잡힌다. 로고가 알아볼 수 있어야 하는 자리라면 `lg` 이상을 쓰도록 문서에 적었다. 로고는 WCAG 1.4.11의 대비 예외라 대비 수치는 걷어냈다 — 정보가 아니라 정체를 가리키고, 이름은 `aria-label`이 따로 말한다.
- **Avatar 문서 미리보기의 `image` 더미를 사람마다 다른 색으로 바꿨다** — 여덟 개가 전부 같은 파란 그림이라 "사진이 들어가는 자리"라는 것만 보이고 목록에서 사람이 갈린다는 느낌이 없었다. 색조마다 연한 면 + **같은 색조를 진하게 한** 사람 아이콘으로 바꿨다.
  - **색은 시스템 팔레트의 램프를 그대로 옮겼다** — 면은 `-100` 자리(`blue-100` = `hsl(215, 71%, 92%)`), 아이콘은 `-600` 자리(`blue-600` = `hsl(216, 84%, 42%)`). 목표 대비는 **3.72:1** — 시스템에서 **가장 연한** tint 짝(caution `#fff8ef` + `#e74900`)과 같은 값이다. 새 기준을 만드는 대신 있는 것을 옮겼다.
  - **처음엔 탁했다** — 면 `s57/l88`, 아이콘 `s52`로 잡아 놓고 4.5:1을 맞췄더니 면은 어둡고 아이콘은 채도가 낮아 전체가 무겁게 읽혔다. 진하게 보인 원인은 명도가 아니라 **채도**였다 — 시스템의 tint는 면이 더 밝고(l92) 아이콘이 더 선명하다(s84). 채도를 올리고 면을 밝히니 같은 대비에서 훨씬 가볍다. 네 안(현행·목표 5.13·4.5·3.8)을 나란히 렌더해 골랐다.
  - **대비를 tint 밴드의 위에서 아래 끝으로 내렸다.** 처음엔 brand 짝(5.13:1)에 맞췄는데 여전히 무거웠다. 5.13·4.5·3.72·3.0을 나란히 렌더해 **3.72**를 골랐다 — 3.0(비텍스트 하한)은 분홍·자홍이 형광으로 뜨고 24px에서 형태가 풀린다. 3.72는 24px에서도 형태가 살아 있고 하한 위에 여유가 있다.
  - **아이콘 명도는 색조별로 따로 푼다.** 여덟 색조에 같은 명도를 쓰면 대비가 **3.13~6.92:1**로 널뛴다 — 초록·청록은 흐리고 남보라는 새까맣다. 색조마다 목표에 닿는 **가장 밝은** 명도를 찾아 전부 **3.74~3.86:1**로 모았다(명도는 L 30~64로 제각각이다).
  - ⚠️ **이건 미리보기 더미이지 시스템 기능이 아니다.** 이름을 해시해 색을 배정하는 방식(Slack·Google)은 목록에서 사람을 구분해 주지만 **아바타 전용 팔레트**가 먼저 있어야 한다 — 지금 팔레트의 red·green·orange는 상태를 뜻해서 사람에게 쓰면 뜻이 겹친다. 팔레트를 정의하기 전에는 시스템이 색을 배정하지 않는다는 것을 제약에 적었다. 사진이 없으면 이니셜, 그것도 없으면 로고다. avatar.md v0.2.0 → v0.2.3
- **Avatar 문서 미리보기에서 `avatar--square`를 원형과 나란히 보이게 했다.** 원형만 8개에 사각이 1개라 shape가 두 값의 차원이라는 것이 미리보기에서 안 보였다. logo·image·initials 세 행 모두에서 **네 크기 × 두 shape**를 나란히 놓았다 — Tag·Badge가 shape와 size 조합을 한 행에 늘어놓는 것과 같은 방식이다. avatar.md v0.1.1 → v0.2.0
- **`logo/` 폴더와 `scripts/embed_logo.py` 추가** — 김반장 로고 원본이 놓일 자리. `icons/`에 두지 않는 이유: 그쪽은 `sync_icons.py`가 Figma에서 통째로 덮어쓰는 영역이고 단색 `currentColor` 24×24 규격인데, 로고는 고유 색을 가진 브랜드 자산이라 그 규칙 밖이다.
  - **자산을 넣고 스크립트 두 줄이면 끝난다** — `python3 scripts/embed_logo.py` → `python3 build.py`. 스크립트가 `logo-kbz.svg`(우선) 또는 `logo-kbz.png`를 data URI로 인코딩해 `avatar.md`의 `--avatar-logo` 한 줄을 바꿔 쓴다. 멱등이라 여러 번 돌려도 안전하다.
  - **CSS가 `logo/`를 상대 경로로 가리키지 않는 이유** — 컴포넌트 CSS는 `components.css`로도 번들되고 `design-system.html`에 인라인으로도 주입되며 프로토타입 HTML은 아무 폴더에나 놓인다. 그때마다 상대 경로의 기준이 달라져 깨진다. 시스템의 다른 자산도 같은 이유로 인라인이다(`build.py`가 파일 69개를 HTML 안에 임베드한다). 이 폴더는 배포 산출물이 아니라 **빌드 입력**이고, 원본을 잃지 않으려 커밋한다.
  - **PNG는 정사각 192px 이상을 요구한다** — Avatar 최대 48px × 3배 화면 = 144px. 스크립트가 PNG 헤더에서 크기를 읽어 정사각이 아니거나 작으면 경고한다. svg에 `viewBox`가 없어도 경고한다(크기가 아바타를 따라가지 않는다).
  - **파일 이름은 안 맞춰도 된다.** `logo/`에 svg나 png를 하나만 넣으면 스크립트가 `logo-kbz.{ext}`로 바꾼 뒤 진행한다 — 자산을 넣는 사람이 파일명 규칙까지 외울 이유가 없다. 같은 확장자가 둘 이상이면 어느 것이 로고인지 알 수 없으므로 멈춘다.
  - svg는 base64가 아니라 URL 인코딩으로 넣는다 — 짧고, 디프에서 사람이 읽을 수 있다. 큰따옴표는 `%22`로 인코딩해 `url("…")` 밖으로 새지 않게 한다. 2색 SVG를 넣어 24·32·40·48px과 square에서 렌더 확인했고 실패한 요청이 없다. avatar.md v0.1.0 → v0.1.1
- **Avatar(Atom) 추가** — 사람·계정·조직을 한 자리에서 대신하는 그림. 사진 → 이니셜 → **김반장 로고** 순으로 떨어진다. shape `round`(기본)·`avatar--square`, size `avatar--xs` 24 · 기본 32 · `avatar--lg` 40 · `avatar--xl` 48.
  - **기본값에 마크업이 필요 없다.** content를 클래스가 아니라 **자식으로** 판정한다 — `<span class="avatar"></span>` 하나면 로고가 나오고, `img.avatar__img`나 `span.avatar__initials`를 넣으면 CSS가 알아서 비킨다(`:not(:has(…))`). "기본은 로고"라는 요구가 클래스 이름이 아니라 **빈 상태**로 표현된다.
  - **로고 자리는 `--avatar-logo` 커스텀 프로퍼티 하나다.** 지금은 **임시 사람 마크**가 들어 있다 — 실제 김반장 로고 SVG가 확정되면 이 값만 갈아끼우면 시스템 전체의 기본 아바타가 로고로 바뀐다. sprite에 심볼로 넣지 않은 이유: sprite는 `sync_icons.py`가 Figma에서 덮어쓰는 영역이고 로고는 아이콘이 아니다. `background-image`(mask 아님)로 둬서 단색·다색 마크 어느 쪽이든 받는다.
  - ⚠️ **size 이름은 Button을 따른다.** 처음에 24를 `avatar--sm`으로 뒀다가 되돌렸다 — `btn--sm`은 32라서 같은 행에 나란히 두면 8px이 어긋난다. 같은 높이는 같은 이름이어야 한다: `avatar--xs`=`btn--xs`=24, 기본=`btn--sm`=32, `avatar--lg`=`btn--lg`=40. 네 크기 모두 기존 height 토큰(`tight`·`compact`·`spacious`·`loose`)이라 **새 토큰이 없다.**
  - **안쪽 실선은 `::after`의 `inset box-shadow`다.** 바깥 `border`로 주면 상자가 커져 같은 행의 버튼과 높이가 어긋난다. 흰 사진·밝은 로고가 흰 배경에 녹지 않게 형태의 경계만 잡는 장식이라 대비 기준을 적용하지 않는다.
  - **접근성은 "이름을 누가 말하는가" 하나로 정리했다** — 이름 텍스트가 옆에 있으면 root가 `aria-hidden`, 단독이면 `role="img" aria-label`, 링크·버튼 안이면 그쪽이 이름을 갖는다. `.avatar__initials`는 root가 `aria-label`을 가질 때 항상 `aria-hidden`이다(안 그러면 "홍" 한 글자가 그대로 읽힌다). 이니셜 대비 **7.95:1**, 로고 마크 대비 **5.75:1** 실측.
  - **겹쳐 쌓는 배치(avatar group)와 접속 상태 점은 정의하지 않았다.** 필요해지면 임의로 만들지 말고 `_requests.md`에 남기도록 문서에 못박았다. components/_index.md v1.7.0 → v1.8.0
- **게시판 본문용 타이포 사다리 추가** (`--font-size-board-*` · `.text-board-*`) — 업무 화면 크기를 서비스 화면에 그대로 넣으니 작아 보였다. 본문 기본을 **17px**로 올리고 에디터의 `h4`~`h1`을 그 비율에 맞춰 토큰으로 고정했다. **17 · 20 · 24 · 28 · 32.**
  - **업무 화면의 `h1`~`h4`는 사다리가 아니라 역할 이름이다** — `h4`=카드 제목·대형 버튼, `h3`=섹션 제목, `h2`=모달 제목, `h1`=페이지 제목. 서로 다른 자리의 크기라 단계 비율이 고르지 않다(`20→28`이 한 번에 1.4배). 에디터의 `h1`~`h4`는 **한 문서 안의 목차**라 고르게 벌어져야 한다. 그래서 기존 사다리를 17/14배로 늘리는 방식(→ 17·21·24·34·39)은 쓰지 않았다 — 뜻 없는 1.4배 점프가 그대로 따라오고 24와 34 사이가 끊어져 보인다. 세 안(rebase 17·21·24·34·39 / 등비 1.17 / 등비 1.125)을 나란히 렌더해 골랐다: 1.125는 `h4` 19px가 본문 17px과 구분되지 않았고, rebase는 `h2`·`h1`이 `h3`에서 떨어져 나왔다.
  - **새로 만든 범위가 아니라 이미 있는 구간을 편 것이다.** 본문 17px은 업무 화면의 `h4`이고 꼭대기 32px은 `h1`이다. 이 **17~32를 4단계 등비(≈1.17)로 나눴다** — 각 단계 ×1.14~1.20으로 고르다. 새 원시값은 `--font-size-24` 하나뿐이고 20·28·32는 기존 값을 그대로 쓴다.
  - **헤딩의 line-height는 `ui`(1.0)가 아니라 새 `--line-height-heading`(1.25)이다.** 본문 헤딩은 길이를 시스템이 정하지 못해 두 줄로 넘어가는데, 1.0이면 두 줄이 서로 붙는다. 기존에 값만 있고 의미가 비어 있던 `--line-height-tight`에 이름을 준 것이다.
  - ⚠️ **본문 안의 헤딩은 글 제목보다 클 수 없다** — 글 제목이 그 문서의 `h1`이므로 본문은 `h2`부터 쓴다(HTML 아웃라인 규칙이기도 하다). 에디터 툴바의 `h1` 자리에는 `board-h2`를 매핑한다.
  - ⚠️ **"작아 보인다"의 실체는 계층 역전이다** — 현재 상세 화면은 게시판 이름에 `.text-page-title`(32px), 글 제목에 `.text-modal-title`(20px)을 써서 글보다 게시판 이름이 크다. 글 제목을 `board-h1`(32px)로 올리고 게시판 이름을 Breadcrumb 계열로 내려야 한다. 화면 조립 규칙이라 토큰 문서가 아니라 `_requests.md`의 DetailPage에서 확정한다 — **후속 작업으로 남긴다.**
  - ⚠️ 목록(ContentList)·폼·테이블은 **업무 화면 크기를 그대로 쓴다.** 게시판 화면이라는 이유로 목록까지 17px로 올리지 않는다 — 목록은 읽는 글이 아니라 훑는 UI다. tokens/typography.md v1.0.0 → v1.1.0
- **FileUpload 좌우 배치 variant 추가** (`file-upload--split`) — 안내·추가하기 버튼을 왼쪽(**왼쪽 정렬**), 파일 목록을 오른쪽에 둔다. 기본(위아래) 배치는 **파일이 늘수록 드롭 영역이 아래로 길어져 폼 안에서 다음 필드를 밀어낸다**(첨부 아래 보안코드가 있는 글쓰기 화면). 좌우로 가르면 드롭 영역의 높이가 목록 높이에 묶여 폼의 세로 길이가 안정된다 — 카드 2장 기준 드롭 영역 높이 **290px → 196px**.
  - **왼쪽 칸은 폭을 정해 둔다** — `--file-upload-prompt-width`(기본 `12rem`). 비율(`1fr`)로 두면 컨테이너가 넓어질수록 안내문 칸도 같이 넓어지는데, 안내문은 길이가 정해진 두 문장이라 넓어져 봐야 빈 공간만 는다. 남는 폭은 목록이 가져간다(1000px에서 `192px | 654px`).
  - **안내문과 버튼은 왼쪽 정렬**이고 시작점이 한 축으로 선다(실측 둘 다 left 65px). 기본 배치에서 가운데 정렬인 것은 안내문이 드롭 영역 전체 폭을 쓰기 때문이고, 좁은 칸에서는 그 이유가 없다. hint의 `max-width`(28em)도 푼다 — 칸 폭이 이미 상한이라 상한이 둘일 이유가 없다.
  - **마크업은 그대로다.** 루트에 클래스 하나만 더 붙이면 된다 — dropzone을 2열 grid로 바꾸고 자식 셋(안내·버튼·목록)의 배치만 다시 한다. 새 래퍼를 두면 기본 배치의 마크업까지 바뀌어 기존 화면이 전부 손을 봐야 한다. 추가하기 버튼은 `> .btn`으로 짚는다 — dropzone 안의 버튼은 트리거 하나뿐이고(Anatomy), 전용 클래스를 새로 요구하면 기존 마크업에 이 variant를 못 붙인다.
  - **버그 수정: 첨부 전후로 상자 높이가 달랐다** — 0장 117px → 1장 187px로 첫 파일이 붙는 순간 **70px 뛰었다.** 폼의 세로 길이를 안정시키려고 만든 variant인데 정작 첫 첨부에서 폼이 밀렸다. 원인 둘: ①카드 행 높이(153px)가 안내·버튼 묶음(≈106px)보다 크고 ②카드가 `minmax(150px, 1fr)`이라 컨테이너 폭에 따라 행 높이까지 달라졌다(1000px에서 153px, 1400px에서 162px). 카드 폭을 **고정**하고(`--file-upload-card-width`, 기본 150px) 드롭존에 **카드 한 행 자리를 미리 비워 두는** min-height를 줬다(`--file-upload-row-height`, 기본 9.5rem). 800·1000·1400px 어디서도 **0장과 한 행이 모두 184px**로 같고, 한 행을 넘길 때만 커진다.
  - **파일이 없으면 한 칸으로 돌아가고 정렬도 기본(가운데)으로 돌아간다**(`:not(:has(.file-upload-item))`) — 오른쪽에 빈 칸이 남으면 드롭 영역의 절반이 이유 없이 비어 보이고, 한 칸에서는 안내문이 전체 폭을 쓰므로 왼쪽 정렬의 이유(좁은 칸에서 시작점 맞추기)가 사라진다. 첨부되면 두 칸 + 왼쪽 정렬로 바뀐다.
  - **빈 상태의 판정 근거를 `grid` 요소가 아니라 카드 유무로 잡았다** (0.6.0의 결함 수정). 처음 규칙은 `:not(:has(.file-upload__grid))`였는데, `initFileUpload`은 `fu.querySelector('.file-upload__grid')`를 **init 시점에 한 번** 잡아 그 자리에 카드를 넣는다 — 문서대로 빈 상태에서 grid를 빼면 추가하기 버튼이 아무 일도 하지 않고, 동작시키려고 넣어 두면 파일이 없어도 두 칸으로 남는다. 판정을 `:not(:has(.file-upload-item))`로 바꾸니 **마크업을 하나로 두고** 두 동작이 다 선다. grid는 파일이 없어도 마크업에 두는 것이 규칙이며(Anatomy에 명시) 카드가 없는 동안은 `display:none`으로 레이아웃에서 뺀다 — 안 빼면 기본 배치에서 dropzone의 gap이 한 번 더 붙고(안내·버튼 아래 16px) 좌우 배치에서는 `grid-column:2`가 빈 열을 만든다.
  - 실제 JS 경로로 검증했다(Chromium 1000px, `components.js` 로드 후 `initFileUpload`): 추가하기 → `filechooser` 열림, 0장 `1fr`·가운데·grid `none`·**184px** → 1장 `192px 702px`·왼쪽·grid `grid`·**184px** → 3장 동일 → 전부 삭제하면 다시 0장 상태로 되돌아온다. 콘솔 오류 없음. 기본(위아래) 배치도 함께 확인했다(0장 98px, 카드가 붙으면 아래로 늘어나는 기존 동작 그대로).
  - **`sm`에서는 위아래로 돌아가고 정렬도 함께 되돌린다** — 왼쪽 정렬은 좁은 칸에서 문장과 버튼의 시작점을 맞추려던 것이라, 한 칸으로 돌아오면 그 이유가 사라진다. 남겨두면 같은 화면에서 기본 배치와 이 variant가 이유 없이 달라 보인다. 두 칸으로 가르지 않는 이유는 카드가 한 장도 온전히 들어가지 않기 때문이다(카드 최소 폭 150px + 좌우 inset). 1000·390px 렌더로 확인했다. file-upload.md v0.4.0 → v0.6.1
- **토큰 `--color-text-brand-faint` 추가** (blue-500 50%) + **Toggle 아이콘 thumb의 off 색을 반투명 브랜드로** — off 아이콘이 중립 회색이라 트랙만 브랜드고 아이콘만 회색으로 한 부품 안에서 두 색이 따로 놀았다. off 트랙(브랜드 18%)·off 라인(브랜드 30%)과 같은 계열로 맞춰 꺼진 상태 전체가 한 톤으로 읽히게 했다.
  - **농도는 50%.** off 라인과 같은 30%로 두면 흰 thumb 위에서 **1.52:1**이라 형태가 읽히지 않고, 80%(`text-brand-alpha`)면 켜짐과 무게가 같아져 반투명한 느낌이 사라진다 — 30·50·80·100%를 나란히 렌더해 골랐다. 기존 브랜드 텍스트 계열의 `-faint`(가장 약한) 자리가 비어 있어 그 이름을 썼다.
  - ⚠️ 50%도 흰 thumb 위 **2.08:1**로 텍스트 기준 아래다. **아이콘이 상태의 단독 전달자가 아니라서** 두는 값이고(켜짐/꺼짐은 `role="switch"`의 aria-checked · 트랙 색 · thumb 위치 · 라벨이 함께 전한다), 그 전제를 접근성 절에 명시했다 — 아이콘만으로 상태를 읽어야 하는 화면이라면 이 variant를 쓰지 않는다.
  - disabled는 브랜드 계열을 버리고 회색을 유지한다 — 비활성 부품에 브랜드 색이 남으면 누를 수 있는 것으로 읽힌다. tokens/color.md v1.9.0 → v1.10.0 · toggle.md v1.1.1 → v1.2.0
- **Toggle 아이콘 thumb variant 추가** (`toggle--icon`). 기본 토글은 상태를 **색**(회색↔파랑)과 **위치**(좌↔우) 둘로 말하는데, 둘 다 "켜졌다"까지만 말하고 **무엇이 켜졌는지**는 옆의 라벨이 맡는다. 비밀글·공개 여부처럼 on/off가 곧 뜻인 설정에서는 그 자리에 아이콘을 넣어 상태를 **형태**로도 전달한다.
  - **이 variant만 치수를 다시 잡는다** — 기본 thumb는 12px이고 담을 수 있는 가장 작은 아이콘(`--icon-badge` 12px)과 같은 크기라 여백이 0이다. md `track 44×24 · thumb 20(--icon-md) · 아이콘 16(--icon-sm)`, sm `track 36×20 · thumb 16(--icon-sm) · 아이콘 12(--icon-badge)` — 둘 다 여백 2. 기본(빈 원) 토글의 치수는 건드리지 않았다(렌더로 확인).
  - **아이콘은 thumb에 꽉 채운다.** 12·14·16px을 나란히 렌더해 골랐다 — 작게 두면 흰 원 안의 점처럼 보여 형태로 전달한다는 목적을 잃는다. 여백 2px이 좁아 보이지만 아이콘 자신이 24 viewBox 안에서 이미 여백을 갖고 있어(자물쇠 실제 폭 ≈16/24) 눈에 보이는 간격은 그보다 넓다. sm의 12px이 바닥이다.
  - **크기 유틸리티(`.icon--{size}`)를 마크업에 붙이지 않는다.** Accordion은 어느 크기에서도 아이콘이 16px이라 클래스로 박아도 되지만, 여기는 토글 크기마다 아이콘 크기가 다르다 — 마크업에 한 크기를 박으면 두 크기 중 하나가 틀린다. 크기는 컴포넌트가 variant별로 정한다.
  - **아이콘 두 개를 모두 마크업에 두고 CSS가 하나만 보여준다** — Accordion의 expanded/collapsed와 같은 방식이고 래퍼도 같은 형태다(`span.icon.icon--badge` + 상태 클래스). JS로 `use href`를 바꾸지 않는다: 전환이 `:checked` 하나로 끝나고 두 아이콘이 같은 자리에 있어 서로 어긋날 수 없다.
  - 크기 유틸리티를 떼면서 `.icon--badge`의 `display:inline-flex`와 다투던 문제도 사라졌다 — 표시/숨김을 `.toggle__icon`이 온전히 갖는다. tokens.css와 components.css의 로드 순서를 뒤집어 렌더해도 항상 한 개만, 같은 크기로 보이는 것을 확인했다.
  - on 아이콘만 브랜드 색이고 thumb는 흰 원 그대로다 — thumb를 브랜드 면으로 칠하면 트랙과 붙어 위치가 읽히지 않는다. disabled에서는 아이콘도 함께 내린다.
  - 지침에 **아이콘 쌍은 뜻이 반대인 것**(`icon-lock`/`icon-unlock`, `icon-show`/`icon-hide`)을 고르라고 못박았다. 같은 아이콘의 색만 바꾸면 형태로 전달한다는 이점이 사라지고 색 하나에 두 뜻이 실린다. svg는 `aria-hidden` — 상태는 `role="switch"`가 이미 전하고, **아이콘이 라벨을 대신하지는 않으므로** `aria-label`은 그대로 필수다. toggle.md v1.0.0 → v1.1.1
- **아이콘 `icon-lock`·`icon-unlock` 추가** (닫힌 자물쇠 / 열린 자물쇠). 비밀글 표시처럼 잠금 상태를 나타낼 자리가 없었다 — 접수된 한계 목록의 첫 항목이다. 기존 아이콘과 같은 규격으로 그렸다: 24×24 viewBox · 단색 채움 `fill="currentColor"` · 열쇠구멍은 `fill-rule="evenodd"`로 뚫었다. 16·20·24·48·96px에서 렌더해 확인했고, 16px에서도 열림/닫힘이 갈린다(열린 쪽은 고리의 오른쪽 끝이 몸체에 닿지 않는다).
  - 이름은 시스템의 기존 쌍 규칙을 따랐다 — `show`/`hide`, `connect`/`disconnect`처럼 반대말 쌍이지 `-open` 접미사가 아니다.
  - `icons/`에 SVG, `sprite.svg`에 symbol(정렬 위치), `categories.json`의 **정보·상태** 그룹, `planner.md`의 아이콘 표까지 네 곳을 맞췄다. 스프라이트 참조(`icon--sm/md/lg`)가 실제로 해석되는 것을 렌더로 확인했다.
  - **Figma 라이브러리에 올라가 원본이 교체됐다** (2026-09-03 sync, `f157e890`). 임시로 그렸던 두 SVG가 Figma 버전으로 대체됐고 sprite도 함께 갱신됐다 — `LOCAL_ICONS`가 의도대로 동작해 sync 전까지 아이콘을 지키다가, Figma에 같은 이름이 생기자 그쪽에 자리를 내줬다. 등록해 뒀던 두 줄은 제거했다(`LOCAL_ICONS = {}`, 용법 설명은 남겼다).
  - 검증: 개별 SVG와 sprite symbol의 path가 일치하고, `categories.json`의 **정보·상태** 그룹에 그대로 있으며, 16·20·24·48px 렌더에서 열림/닫힘이 갈린다. planner.md v2.10.0 → v2.10.1
- **플로우 허브를 화면 색인(`index.html`)으로 바꿨다** — 접수된 요청은 "허브가 두 방식(자동 생성 카드형 `flow-hub.html` / 손으로 쓴 표형 `index.html`)인데 문서에는 한 방식만 있다, 둘 중 하나로 정해달라"였는데, **둘 중 고르는 대신 생성기의 출력을 표형으로 바꿨다.** 자동 생성의 편함과 표형의 정보량을 둘 다 가져간다.
  - 출력 파일명이 `flow-hub.html` → **`index.html`**이다. Pages에서 `.../<기능>/`만 쳐도 열린다.
  - 표 = `화면 · 파일 · 담기는 시나리오 · ← 들어오는 화면 · → 나가는 화면 · 열기`. 「담기는 시나리오」는 `@flow`에 **`scenarios:` 키를 추가**해 기계 소스로 만들었다 — 손으로 채우면 재생성 때 사라진다.
  - **사람이 쓰는 두 절은 재생성해도 보존된다** — `<!-- @keep:principles -->`(구성 원칙) · `<!-- @keep:todo -->`(확인 필요 사항) 마커 사이를 읽어 그대로 옮긴다. 표는 `@flow`가 소스이므로 마커 밖을 손으로 고치면 지워진다고 문서에 못박았다.
  - 들어오는 화면이 없는 항목이 있으면 표 아래에 경고가 붙는다 — 시작 화면이 아니라면 어딘가의 `exits`가 빠진 것이다(`validate-prototype.py`의 검사와 짝).
  - 가짜 프로토타입 4개(고아 화면 포함)로 생성 → 손으로 두 줄 추가 → 재생성까지 돌려 보존을 확인했고, 실제 렌더로 표·경고·두 절을 봤다. 생성 페이지가 쓰던 `--font-size-title`은 **존재하지 않는 토큰**이어서(제목이 본문 크기로 렌더됐다) `--font-size-h1`으로 고쳤다.
- **`planner.md`에 「시스템 한계 보고」 절 신설** — 작업 중 시스템에 없어서 우회한 것을 색인의 「확인 필요 사항」에 적는 것을 완료 절차에 넣었다. 채팅으로만 보고하면 대화가 끝나며 사라지고 다음 세션이 같은 한계를 다시 발견한다(접수 문서가 든 사례: 한 작업에서 나온 한계 7개, Card 부재를 세 번 확인). 이 목록이 `components/_requests.md`에 낼 요청서의 초안이 된다. planner.md v2.9.0 → v2.10.0
- **프로토타입 작업에서 접수된 문서 공백 반영** (게시판 프로토타입 작업 중 Planner가 접수). 확인한 것만 고쳤다.
  - **`planner.md` 파일명 규칙을 예시에서 규범으로** — `<대상>-<화면유형>.html` kebab-case, **번호 금지**. 문서 예시(`6-1-list.html`)를 따라 번호를 붙였다가 화면이 합쳐지며 번호가 어긋나 전부 개명한 사례가 있었다. 순서는 파일명이 아니라 색인이 표현한다.
  - **`planner.md` 화면 분할 기준에 "계열 변주" 행 추가** — 같은 화면 타입이 대상에 따라 구성만 달라지면(자료실형 상세 · 문의형 상세) **한 파일 + `proto-nav-group-label`**이다. 판별 질문은 *"URL 패턴이 같은가"* — `?docId=` 하나로 열리면 같은 화면이다. "독립 URL이면 별도 파일"만 보고 나눈 결과 비밀글·로딩·오류 패널이 계열 수만큼 복제됐다 — 이 문서가 경계하는 마크업 divergence가 **분할 기준 때문에** 생긴 경우다.
  - **`planner.md` 완료 절차에 중간 산출물 정리 추가** — 작업 중 만든 실험본은 삭제한 뒤 커밋한다. 남으면 본편만 고쳐지는 동안 낡은 파일이 같은 폴더에서 최신본 행세를 한다.
  - **`validate-prototype.py`에 `@flow exits` 대조 검사 추가** — 파일 안의 `href="*.html"` 목적지가 `exits`에 없으면 **오류**(허브에서 "아무도 들어오지 않는 화면"이 된다), `exits`에 적혔는데 폴더에 파일이 없으면 **경고**. `flow-hub.html`·`index.html`·앵커·외부 링크는 제외하고, `@flow`가 없는 파일은 검사하지 않는다. planner.md v2.8.0 → v2.9.0
- **문서 대 구현 불일치 정정: 버튼 타이포 클래스** — `form.md`와 `modal.md`가 *"`btn--[size]`가 폰트를 포함하므로 `text-button-*`를 함께 쓰지 않는다"*라고 적고 있었는데, 실제 `.btn--sm/md/lg`는 **높이와 padding만 정하고 font-size를 설정하지 않는다**(폰트를 갖는 것은 `btn--xs`·`btn--micro`뿐이고 이 둘에는 대응 유틸이 없다). 문서를 따르면 버튼 타이포가 빠진다. 세 곳(form.md 1 · modal.md 2)을 구현에 맞춰 고쳤다 — 나머지 문서(`_index.md`·`_spec.md`·`action-group.md`)는 이미 올바르게 `text-button-{size}`를 병기하고 있었다. form.md v0.4.1 → v0.4.2 · modal.md v0.5.4 → v0.5.5
- **ContentList: 분류가 없는 게시판(공지사항)을 정식으로 다룬다** — `sm` 분류 필터를 "필수"로만 적어둬서, 분류 축 자체가 없는 게시판에서는 거를 대상 없는 빈 칩 행만 남았다. **분류가 있는 목록에서만 필수**로 고치고, 분류가 없으면 `__cat`·`__filter`·분류 라벨을 전부 두지 않는다고 명시했다. 메타 2칸(`작성일 · 조회`)용 기본 트랙도 추가했다 — 3칸 기본값을 그대로 두면 쓰이지 않는 트랙 하나가 남아 목록 오른쪽에 빈 칸이 생긴다.
  - **버그 수정: 열 이름의 우측 정렬이 empty·loading에서 풀렸다.** 조회수 라벨을 `:has(.content-list__views)`로 짚었는데 그 조건이 **행에 달려 있어** 행이 없으면 라벨이 좌측으로 돌아가 본목록과 54px 어긋났다(실측). 정렬 근거를 열 이름 쪽으로 옮겼다 — 숫자 열 라벨에 **`content-list__col-num`**을 붙인다. 열 트랙 개수를 `__meta`가 아니라 `__columns`로 세는 것과 같은 이유다. content-list.md v0.46.0 → v0.47.0
- **ContentList가 `md` 이상에서 상자가 된다** — 테두리가 아니라 **면 + radius-md + shadow-sm**이다. 게시판은 페이지에서 **하나의 덩어리**인데(소제목 + 열 이름 + 행들이 한 단위로 읽힌다) 넓은 화면에서는 목록 좌우에 페이지 여백이 남아, 경계가 없으면 목록이 어디서 시작하고 끝나는지 페이지 배경과 갈리지 않았다.
  - **상자를 만드는 것은 선이 아니라 면이다.** 목록 안에 이미 선이 둘 있고(머리 아래 · 행 사이) 그 바깥을 선으로 한 번 더 감싸면 한 화면에 선이 세 겹이 된다. 데이터 테이블(`table-container`)이 테두리를 갖는 것은 가로 스크롤로 **잘리는 경계**를 명시할 이유가 있어서인데, 게시판은 접히는 목록이라 그 이유가 없다.
  - 그림자는 **shadow-sm**(`.elevation-base`와 같은 값)이다. 이 시스템의 elevation은 장식이 아니라 **레이어**를 뜻하고 게시판은 흐름 안의 base 레이어다 — shadow-md는 dropdown, lg 이상은 modal의 값이라 쓰면 목록이 떠 있는 것으로 읽힌다. 흰 페이지에서는 옅은 가장자리로, 회색 페이지(`surface-subtle`) 위에서는 면 자체로 또렷하게 읽힌다(두 배경에서 렌더해 확인).
  - `sm`은 그대로 화면 폭을 쓴다 — 목록이 곧 화면이라 경계 문제가 없고, 좌우 1px이 화면 가장자리에 붙으면 잘린 것처럼 보인다.
  - `overflow: hidden`이 필요하다(행 hover 배경과 header가 둥근 모서리를 각지게 덮는다). `__link::after` 행 오버레이는 li가 컨테이닝 블록이라 클리핑에 걸리지 않고, 포커스 아웃라인도 항목 padding 안쪽이라 잘리지 않는다 — 헤더 없는 목록에서 첫 행 hover와 Tab 포커스로 확인했다.
- **버그 수정: 표시가 하나도 없는 목록에서 번호가 다음 줄로 밀렸다** — 신규·고정 표시가 없으면 거터 칸을 없애는 규칙(`:not(:has(__new, __pin))`)이 `__body`만 1번 열로 당기고 `__no`는 2번 열에 고정된 채 두어, 둘이 같은 칸을 다퉜다. 표시를 번호 **앞**으로 옮긴 뒤 생긴 문제인데 문서 프리뷰에는 항상 핀이나 신규가 있어 드러나지 않았다(radius 작업 중 헤더 없는 목록을 렌더하다 발견). `__no`도 1번 열로 당긴다. content-list.md v0.44.0 → v0.45.0 · components/_requests.md v0.7.0 유지(문구만 갱신)
- **ContentList 메타 열 정렬을 자리 기준에서 값 기준으로** — "마지막 열은 오른쪽 정렬"이라는 규칙을 걷어냈다. 그러면 정렬이 **값의 성격이 아니라 자리**로 정해져, 같은 작성일이 자료 목록에서는 왼쪽(뒤에 조회수가 있으니까) 문의 목록에서는 오른쪽(마지막 칸이니까)으로 갈렸다. 이제 **우측 정렬은 조회수 하나뿐**이고 날짜·분류·작성자는 왼쪽이다 — 데이터 테이블이 이미 쓰던 규칙 그대로다(`organisms/table/data.md`: *"금액·수량 컬럼에만 우측 정렬, 순번·날짜·기간 등은 좌측 정렬 유지"*). 두 계열이 같은 정렬 규칙을 쓰게 됐다.
  - 라벨도 함께 옮겨야 하는데, 조회수는 메타 순서상 **항상 마지막**이라(답변 → 분류 → 작성자 → 작성일 → 조회수) `:has(.content-list__views)`인 목록의 마지막 라벨만 짚으면 된다 — 마크업에 정렬 표시용 클래스를 새로 두지 않았다.
  - 검증: 두 목록 모두 작성일 값의 시작점이 라벨과 일치한다. 조회수가 없는 문의 목록은 마지막 열이 왼쪽 정렬이라 오른쪽 끝에 6.9px가 남지만 트랙 최소값 안쪽이라 눈에 띄지 않는다. content-list.md v0.43.1 → v0.44.0
- **ContentList 문의 게시판의 분류를 문의 종류로** — 예시가 브랜드·제품명(김반장·이콘·4대보험)이었는데 `장애신고 · 개선요청 · 문의 · 기타`로 바꿨다. 자료실의 분류는 자료가 **무엇에 관한 것인지**를 가르지만, 문의 게시판의 분류는 그 글을 **어떻게 처리할지**를 가른다 — 장애는 기술지원으로, 개선요청은 기획으로 간다. 제품명은 그 안의 세부라 제목과 본문이 말해주고, 분류 열에 올리면 담당자가 훑고 싶은 "무엇을 해야 하는 글이냐"가 사라진다.
  - **게시판을 몇 개 둘지와 한 몸인 결정이다.** 종류마다 게시판을 따로 두면 사용자가 글을 쓰기 **전에** 게시판을 고르게 되고 잘못 고른 글이 매번 옮겨진다. 하나에 종류를 분류로 두면 그 선택이 글쓰기 폼 안의 한 필드가 되고, 목록에서는 `sm`의 필터 칩과 `md` 이상의 분류 열이 같은 축으로 남는다.
  - 닫힌 집합이고 5개 안팎으로 짧게 유지한다(글쓰기에서 고르는 값이라 자료실 분류보다 짧아야 한다). `기타`는 필터 칩에서도 열에서도 맨 뒤다.
  - 값이 길어졌지만(2자 → 4자) 기본 트랙 8rem 안에 들어와 열 좌표는 그대로다 — 라벨·값·empty 모두 840/920/1048/1144. 스펙·클래스 변경 없음(PATCH). content-list.md v0.43.0 → v0.43.1
- **토큰 `--color-text-caution-muted`(orange-700 #a8531f) 추가** — 주의 계열에서 **본문 대비 기준을 맞춰야 할 때** 쓰는 억제 톤. `--color-text-caution`(orange-600)은 흰 배경 3.92:1로 AA 본문 기준(4.5:1)에 미달인데, 그동안 그 사실이 ContentList 고정 제목의 ⚠️ 주석에만 적혀 있고 **대안 값은 토큰이 아니라 primitive(`--color-orange-700`)로만 언급돼 있었다** — 컴포넌트 CSS에서 primitive를 직접 쓸 수 없으니 실행 불가능한 안내였다. 브랜드·info가 이미 갖고 있는 `-muted` 패턴(blue-600→800, cyan-600→700)을 주의 계열에도 맞춰 채웠다. 5.35:1로 AA 통과. ContentList의 두 ⚠️ 주석(고정 제목·댓글 수)이 이제 이 토큰을 가리킨다. tokens/color.md v1.8.0 → v1.9.0
- **ContentList 댓글 수를 주의색으로** — 브랜드 파랑에서 바꿨다. 목록에서 색을 쓰는 자리는 고정(제목·핀)·신규(아이콘)·댓글 수 셋뿐이고, 셋 다 "이 행을 먼저 봐라"는 **주목** 신호다. 나머지(분류·작성자·날짜·조회수)는 회색조의 값이다. 네 후보(orange-600 · orange-700 · red-600 · brand)를 고정·신규가 함께 있는 목록에 나란히 렌더해 `--color-text-caution`(orange-600)을 골랐다 — 가장 밝고 선명한 주황이다.
  - **고정 제목과 같은 색이고, 갈리는 것은 형태다** — 고정은 제목 자체의 색(semibold 17px), 댓글 수는 제목 뒤의 대괄호 숫자(13px)라 한 행에 둘 다 있어도 자리로 구분된다. 신규 아이콘은 빨강(`--color-fill-error`)이라 또 갈린다.
  - ⚠️ **대비 3.92:1로 AA 본문 기준에 미달한다** — 고정 제목과 같은 사정이고, 문서에 같은 형식의 경고로 남겼다. 대괄호로 감싼 부가 수치이고 그 글의 목적지는 제목이라 정보가 색에만 실리지는 않는다. 기준을 맞춰야 하는 화면은 `--color-text-caution-muted`(5.35:1)로 바꾼다.
  - 참고: `icon-new`는 주황이 아니라 **빨강**(`--color-fill-error` #bb2e2e)이다. 화면에서 주황으로 보이던 것은 고정 핀(`--color-fill-caution` orange-500)이었다. content-list.md v0.41.0 → v0.43.0
- **ContentList: 댓글 수를 브랜드 색으로, 답변 칩의 "답변" 라벨은 화면에서 뺐다** — 댓글 수가 메타 톤(subtle)이라 제목 옆에서 묻혔다. **분류에 브랜드 색을 금한 이유가 여기서는 반대로 성립한다**: 목록에서 파란 글자는 "누르면 간다"를 뜻하는데, 분류는 누를 수 없는 값이지만 댓글 수는 제목(링크) 바로 뒤에 있고 행 전체가 그 글로 가는 링크라 파랑이 실제 동작과 어긋나지 않는다. 후보 4종(brand·info·error·body)을 나란히 렌더해 골랐다 — 주황은 고정(제목)이, 빨강은 신규 아이콘이 이미 쓰고 `--color-text-error`는 오류 메시지에 배정된 색이라 제외했고, body는 눈에 걸리지 않았다. 흰 배경 대비 6.36:1로 AA 통과.
  - 읽은 항목에서는 분류와 함께 톤이 내려간다(4.51:1). 회색 제목 옆에 파란 숫자만 남으면 그 행에서 가장 눈에 띄는 것이 제목이 아니게 된다 — "읽은 항목은 행 전체가 한 단계 물러난다"는 기존 규칙 그대로다.
  - 답변 칩에서 단위 라벨(`__unit`)을 뺐다. 조회수는 `1,011`만 남으면 무엇의 수인지 사라지지만 칩에 적힌 `대기`·`완료`는 그 자체로 뜻이 서고, `sm`에서 `답변 대기`로 두면 칩 앞에 라벨이 하나 더 붙어 좁은 메타 줄만 길어졌다. 맥락은 칩 안의 `.sr-only`("답변 ")가 두 폭 모두에서 스크린리더에 전달한다. content-list.md v0.40.0 → v0.41.0
- **ContentList: 답변을 Badge로, 댓글 수 슬롯(`__comments`) 추가** — 답변 상태를 텍스트의 톤 차이로만 표시했는데, 렌더해 보면 검정과 회색의 차이는 훑을 때 약했다. 답변은 이 목록이 존재하는 이유이고 Badge는 "상태·분류·수량을 나타내는 인라인 레이블"(`badge.md`)이라 상태의 제자리다. 대기 `badge--neutral`(아직 아무 일도 일어나지 않았다) · 완료 `badge--success`(처리됐다) — 사용자 대면 목록에서 눈에 걸려야 하는 쪽은 자기 글에 답이 왔는지 보러 온 사람의 **완료**다. `badge--caution`은 쓰지 않는다(고정이 쓰는 색이라 한 목록에서 같은 색이 두 뜻이 된다).
  - **"메타에 칩을 섞지 않는다"를 조건부로 고쳤다.** 가르는 기준은 **값이냐 상태냐**다 — 분류·작성자·날짜·조회수는 값이라 행마다 다른 문자열이 나열되고 칩으로 만들면 목록이 얼룩이 되지만, 답변은 값이 두 종류뿐이라 반복되는 칩이 얼룩이 아니라 패턴으로 읽힌다. **칩은 목록당 한 열까지.**
  - **슬롯과 칩은 다른 요소다.** 열 간격을 padding으로 주는데(margin은 트랙 크기 계산을 흔든다) 뱃지에 직접 걸면 그 여백이 칩의 **배경 안쪽**으로 들어가 글자가 오른쪽으로 밀렸다(실측: 칩 60px 중 24px이 빈 여백). 슬롯 span이 열과 간격을, 안의 `.badge`가 칩 모양을 맡는다.
  - **댓글 수는 열이 아니라 제목 뒤다**(`[3]`, 대괄호는 CSS). 답변과 다른 값이다 — 댓글은 오간 이야기의 양, 답변은 처리 상태다. 열로 두면 대부분의 행이 0이라 빈 열이 하나 늘고 "댓글이 없다"를 매 행에 반복하게 되므로 **0이면 마크업에서 뺀다.** 링크 밖이라 스크린리더에는 숫자만 남으므로 `.sr-only`로 "댓글 "을 붙인다.
  - **`sm`의 2줄 말줄임을 제목(`__link`)에서 제목 줄(`__headline`)로 옮겼다.** 제목만 클램프하면 그 상자가 블록이라 댓글 수가 2줄 상자의 **첫 줄 옆**에 서고 제목 둘째 줄이 그 아래로 흘러, 제목이 잘린 것처럼 보였다(실측: "…저장 버튼이 눌 [5] / 리지 않습니다"). 클램프를 위로 올리고 제목·댓글 수를 인라인으로 흘리면 댓글 수가 제목의 마지막 글자 뒤에 붙는다. `__link::after` 행 오버레이는 li가 컨테이닝 블록이라 이 `overflow:hidden`에 잘리지 않는다 — 1280·390px 양쪽에서 행 끝 클릭이 링크로 잡히는 것을 확인했다.
  - 검증: 열 좌표 840/920/1048/1144로 라벨·값·empty 일치, 칩은 트랙 폭으로 늘어나지 않고 글자 폭(대기 기준 60px→슬롯 분리 후 칩만), 행 높이 50.5px, 390px 가로 스크롤 없음. content-list.md v0.39.0 → v0.40.0
- **ContentList 문의 게시판 변형** — 작성자(`__author`)와 답변(`__answers`) 슬롯 추가. 장애신고·묻고답하기처럼 **사용자가 쓰고 담당자가 답하는** 목록은 메타가 다르다: 자료실은 `분류·작성일·조회수`지만 문의 게시판은 `답변·분류·작성자·작성일`이고, 조회수는 뺀다(대부분 비밀글이라 판단 근거가 못 된다).
  - **답변은 메타의 첫 칸이다 — 제목 바로 옆.** 상태는 그 글에 붙는 성질이라 글에 가장 가까이 둔다. 오른쪽 끝에 두면 훑는 축을 보려고 넓은 화면에서 행을 가로질러야 하고, `sm`에서는 메타 줄 맨 뒤로 밀려 제목 아래 첫 값이 분류가 된다. 첫 칸이면 두 폭 모두 **제목 → 상태**가 한 덩어리로 읽힌다.
  - **답변은 개수가 아니라 상태다.** 1:1 문의는 0 아니면 1이라 개수와 상태가 같은 말인데, 숫자로 적으면 "0이 미답변"이라는 규칙을 사용자가 따로 읽어내야 한다. 값은 `완료`/`대기`이고 단위 라벨(`__unit`)이 `sm`에서 `답변 대기`로 되살아난다 — `__views`와 같은 방식. 원본 화면이 같은 사실을 세 번(제목 뒤 `[답변]` · `[0]` · 답변 열) 말하던 것을 하나로 줄였다.
  - **미답변은 색이 아니라 톤으로 표시한다.** 주의색은 고정(제목)이 이미 쓰고 있어 같은 색이 한 목록에서 두 가지를 뜻하게 되고, 고정은 맨 위에 모여 있지만 미답변은 목록 전체에 흩어져 있어 얼룩으로 보인다. 미답변만 본문 검정으로 올린다.
  - **답변 슬롯이 있으면 분류의 톤 올림이 자동으로 풀린다**(`:has(.content-list__answers) .content-list__cat`). 한 행에 진한 값이 둘이면 훑는 축이 둘이 되어 어느 쪽도 걸리지 않는다 — 자료실의 축은 분류, 문의 게시판의 축은 답변 여부다. 목록마다 하나씩이다.
  - **메타 열 배치를 클래스에서 자리 순서로 바꿨다.** `__cat{4} __date{5} __views{6}`처럼 클래스마다 열 번호를 박아두면 슬롯이 늘 때마다 규칙을 다시 써야 했다. `__meta > :nth-child(n)`으로 얹어 **최대 5칸**까지 마크업 순서가 곧 열 순서가 된다(자동 배치는 쓸 수 없다 — 거터가 빈 행에서 분류가 1번 열로 들어간다). padding·정렬도 열 번호가 아니라 `:first-child`/`:last-child`로 바꿔 열이 몇 개든 라벨과 값에 같은 규칙이 걸린다. 오른쪽 정렬은 마지막 열 하나로 정리했다.
  - 4·5칸일 때의 기본 트랙 폭은 CSS가 **열 이름(`__columns`) span의 개수**를 세어 정한다(`:where(:has(.content-list__columns > :nth-child(4)))` — `:where()`로 명시도를 0으로 둬서 호스트의 `--content-list-meta-cols` 한 줄이 이긴다). 세는 대상이 메타가 아닌 이유는 empty·loading에는 행이 없어 `__meta`가 아예 없기 때문이다 — 메타를 세면 그 상태에서만 트랙이 3칸으로 돌아가 마지막 라벨이 다음 줄로 밀린다(실측으로 잡았다: 작성일 1144px → 952px). 순서가 고정이라 자리마다 다른 최소값을 준다(4칸 `5·8·6·7rem`).
  - `sm`에서 분류를 숨길 때 뒤 구분자를 지우는 규칙을 `.content-list__cat:first-child + *`로 좁혔다. 답변이 첫 칸이 되면서 분류가 가운데로 왔는데, 가운데 칸이 숨으면 그 칸의 구분자도 함께 숨으므로 뒤 칸의 구분자는 남아 있어야 한다 — 그대로 뒀으면 `답변 대기 김지현`처럼 붙어 읽혔다.
  - 1280px·390px 렌더로 검증: 3·4·5칸 모두 라벨과 값의 열 좌표가 정확히 일치하고(4칸 840/920/1048/1144), empty도 같은 좌표다. 390px 가로 스크롤 없음. 인라인 메타는 전체일 때 `답변 대기 · 이콘 · 김지현 · 2026.08.31`, 특정 분류로 좁히면 `답변 대기 · 김지현 · 2026.08.31`.
  - ⚠️ 비밀글 자물쇠 표시는 정의하지 않았다 — 거터 칸이 하나뿐이고 신규·고정이 이미 쓰고 있어 별도 결정이 필요하다. content-list.md v0.38.1 → v0.39.0
- 토큰 `--color-action-neutral-faint`(중립 4%) 추가. 중립 action 계열은 8%(subtle) · 10%(hover) · 15%(pressed)로 시작해 **그보다 연한 값이 없었다.** 버튼·아이콘의 중립 hover가 8~10%인 것은 면적이 작아서인데, 행처럼 폭 전체를 덮는 면은 같은 농도로 깔면 눌린 것(selected)처럼 보인다 — 면적이 커질수록 같은 신호에 필요한 농도는 낮아진다. 4·6·8% 렌더 비교로 정했고(3%는 hover 신호로 약하다), ContentList 행 hover가 바로 쓴다. color.md v1.7.0 → v1.8.0

### Fixed
- **`file://`로 열었을 때 폭 인계가 끊기고, 그 사실이 콘솔에 남지도 않았다.** 브라우저가 `file://` 문서를 **서로 다른 출처**로 취급해 바깥이 틀 안 주소를 못 읽는데(`contentWindow.location.href`), 그 자리는 예외를 삼키는 자리라 조용히 지나갔다.
  - **재현해서 어디까지 깨지는지 갈랐다.** `file://`에서도 ① 틀 안 시나리오 적용(문서가 제 주소를 읽는다)과 ② 틀 안에서 다른 화면으로 갈 때 최상위로 나오기(안쪽 규칙)는 **동작한다.** 끊긴 것은 **폭 인계 하나**였다 — 주소·`frameElement`·`referrer`가 전부 막히거나 비어 있다.
  - **sessionStorage를 통로로 더했다.** `file://` 문서끼리도 같은 탭에서 값을 주고받는 것을 실측으로 확인하고, 폭을 바꿀 때마다 거기에도 남긴다. 이제 `file://`에서도 sm에서 넘어가면 sm으로 열린다(실측: 최상위 `pageB.html?proto-view=sm`, 틀 390×844).
  - **조용한 실패를 말하게 했다** — 바깥이 틀 주소를 못 읽으면 콘솔에 한 번만 경고한다: `[proto-chrome] … 로컬 서버로 열면 전부 정상 동작한다: python3 -m http.server 8000`.
  - **planner.md에 「로컬 서버로 연다」를 넣었다** — 무엇이 되고 무엇이 안 되는지 표로 적었다.
  - ⚠️ 처음에 "`file://`에서는 아이콘이 안 나온다"고 적었다가 **고쳤다.** 내 하네스가 스프라이트를 **로컬 상대경로**로 복사해 두어서 크롬이 `file://` `fetch`를 거부한 것이고, 프로토타입 규약대로 **원격 절대 URL**(Pages, `Access-Control-Allow-Origin: *`)을 쓰면 `file://`에서도 통과한다 — planner.md가 원래 그렇게 적어 두고 있었다. 서버로 여는 이유는 아이콘이 아니라 **바깥이 틀 안을 읽지 못해 크롬이 스스로를 검사할 수 없다는 것**이다. build.py · planner.md
- **`sm`의 「필터」가 옆 칸의 드롭다운 트리거와 다른 물건처럼 보였다.** 같은 바 안에서 같은 일(무언가를 여는 것)을 하는데 값이 어긋나 있었다.
  - 실측 — 「필터」 padding 6 / gap 2, 드롭다운 트리거 padding 8 / gap 4. 게다가 **여는 컨트롤인데 셰브런만 없었다** — 옆 칸은 전부 셰브런으로 "열린다"를 말하고 있었다.
  - `.dropdown__trigger`의 값(`--space-inset-lg` · `--space-gap-xs` · `--font-size-base` · `--line-height-ui`)을 그대로 쓰고, hover도 같은 **브랜드 링**으로 맞췄다.
  - **수 뱃지와 셰브런은 드롭다운의 클래스를 그대로 재사용**한다(`.dropdown__count` · `.dropdown__chevron`) — 스코프가 없는 클래스라 그대로 적용된다. 직접 만들었던 뱃지는 `--color-surface-brand`/`--icon-sm`/`--font-weight-heading`으로 원본(`--color-fill-brand`/16px/semibold)과 미묘하게 달랐다. **베끼면 언젠가 갈린다.** BEM 클래스(`.filter-bar__toggle-count`)는 JS 훅으로만 남긴다.
  - 열려 있으면 셰브런이 뒤집힌다 — 드롭다운의 `.dropdown--open` 규칙과 같은 동작을 `aria-expanded`로 건다.
  - 실측(390) — padding 8/8 · gap 4 · 14px · 셰브런 16px `rgb(109,120,130)`, 열면 180° 회전, 뱃지 16×16 `rgb(17,90,198)` 11px. filter-bar.md v0.16.1 → v0.17.0
- **틀 안에서 다른 페이지로 넘어갔을 때 여전히 틀 안에 갇히는 경우가 있었다** — 넘어간 화면이 제 크롬(사이드바·폭 버튼)까지 달고 1280 틀 안에 들어가, 틀 안에 또 틀이 생겼다.
  - 바깥에서 iframe의 `load`를 보고 최상위를 옮기는 규칙은 이미 있었고 **지금 번들에서는 동작한다**(A → B 이동을 재현해 확인: 최상위가 B로 가고 틀은 하나뿐이다). 문제는 **바깥 페이지가 옛 `components.js`를 물고 있을 때**다 — 프로토타입은 자산을 `?v=20260904a`처럼 고정 쿼리로 부르므로 시스템이 바뀌어도 URL이 그대로고, 브라우저·CDN이 옛 파일을 계속 준다.
  - 그래서 **판정을 안쪽에도 넣었다**: "틀 안인데 `?proto-frame=1`이 없으면 스스로 최상위로 나간다". 안쪽 문서는 방금 새로 불러온 것이라 최신 규칙을 갖는다 — 바깥이 옛 번들이어도 갇히지 않는다.
  - 실측 — 바깥에 규칙이 없는 페이지(옛 번들 흉내)가 프로토타입을 `proto-frame=1` 없이 iframe에 띄우면, 그 문서가 스스로 최상위를 자기 주소로 옮긴다(iframe 사라짐, 콘솔 오류 없음). build.py · planner.md
- **`sm`에서 행만 16px 더 들어가 머리와 시작선이 어긋났다.** 상자 좌변에서 머리("공지사항")는 32, 행의 내용은 48이었다(실측).
  - 원인은 **번호가 숨은 자리의 간격이 남은 것**이다. `.content-list__body`의 `margin-inline-start`는 "번호 거터와의 거리"인데, `sm`에서 번호(`__no`)만 `display:none`으로 숨기고 그 간격은 그대로 뒀다. 간격의 근거가 번호였으므로 번호가 없으면 간격도 없다 — `sm`에서 0으로 되돌린다.
  - 함께 **죽은 열 하나도 걷어냈다.** 표시(신규·고정)가 제목 앞으로 옮겨가면서 subgrid의 「표시」 열은 아무도 쓰지 않게 됐고, 그 열이 있어서 필요했던 `:has()` 규칙 둘(표시가 하나도 없는 목록에서 빈 열을 접는 규칙)도 근거가 사라졌다. 열은 이제 **번호·본문 둘**이다.
  - 실측 — 390: 머리·행·메타가 모두 32에서 시작(표시 있는 행은 아이콘이 32, 제목이 52). 1000: 머리 40 · 번호 40 · 제목 85로 이전과 같다. content-list.md v0.52.0 → v0.52.1
- **`sm`에서 진짜 표적이 보이는 칸보다 작았다.** 「높이가 36이라 44에 8px 모자란다」는 지적을 받고 재 보니, 실제로 누르는 것은 칸(38×34)이 아니라 그 안의 `icon-on--md` **28×28**이었다 — 칸 안에서 남는 여백은 눌러도 아무 일이 없다. **문제는 높이가 아니라 표적이 칸보다 작았던 것이다.**
  - 초기화·검색의 아이콘 버튼이 칸을 다 쓰게 했다: `sm`에서 **40×34**(면적 2.2배). 바 높이(36)와 `md`(28×28)는 그대로다.
  - 선택자에 `.filter-bar__bar >`를 붙였다 — sm 블록이 파일에서 base 규칙보다 **앞**이라 같은 명시도로는 진다(첫 시도에서 38 그대로였다).
- **`sm`에서 높이를 올리지 않는 근거를 문서에 남겼다.** `adaptation.md`에 표적 크기 기준을 명시했다 — **WCAG 2.5.8(최소 표적, AA) 24×24가 기준이고, 44×44는 2.5.5(향상된 표적, AAA)라 이 시스템의 기준이 아니다.** 채택한다면 `adaptation.md`에서 한 번에 올려 버튼·페이지네이션·툴바가 함께 움직여야 한다 — 컴포넌트 하나만 올리면 바로 위아래 목록·폼과 높이 축이 어긋난다.
  - 전 문서의 `max-width: 767px` 블록을 훑어 확인했다: **어느 컴포넌트도 `sm`에서 컨트롤 높이를 올리지 않는다**(툴바 36 · 카드 액션 32 · 페이지네이션 28). FilterBar가 규칙을 안 받은 게 아니라, 규칙이 원래 그렇다.
  - 좁은 화면에서 필요한 것은 **표적을 키우는 것이 아니라 표적을 줄이는 것**이라는 문장도 함께 적었다 — FilterBar의 `sm`이 필터 다섯을 「필터」 하나로 모은 것이 그 규칙의 예다.
  - 실측(390) — 바 358×36, 「필터」 58×34 · 검색칸 257×34 · 초기화 버튼 40×34 · 검색 제출 40×34, 넘침 0. 1200에서는 아이콘 버튼 28×28 그대로. filter-bar.md v0.16.0 → v0.16.1 · adaptation.md v1.1.0 → v1.2.0
- **이미지가 아닌 파일(csv·html 등)을 눌러도 이미지 라이트박스가 열렸다.** 카드 자체는 이미 맞게 그려지고 있었다 — 확장자 글자, 썸네일 없음, 돋보기 오버레이 없음, 커서도 기본. 그런데 **누르면 열렸다.**
  - **원인은 속성 하나가 두 가지를 뜻한 것이다.** 보통 요소에서 `data-image-preview`는 "이걸 누르면 열린다"(트리거)이지만, `.file-upload`에서는 "이 라이트박스와 연동한다"(설정값)다. `initImagePreview`가 `[data-image-preview]`를 전부 트리거로 잡는 바람에 **업로드 영역 전체가 트리거**가 됐고, 카드 클릭이 위로 올라가 그 안의 첫 이미지(또는 빈 src)로 라이트박스가 열렸다.
  - 트리거 수집에서 `.file-upload`를 제외했다(`[data-image-preview]:not(.file-upload)`). 여는 판단은 카드마다 `initFileUpload`가 한다 — **이미지일 때만** 리스너를 단다.
  - 실측 — csv·html·png를 한 번에 올려 각각 눌렀다: csv ❌ · html ❌ · png ✅(라이트박스 열림). image-preview.md v0.3.0 → v0.4.0 · file-upload.md v0.7.0 → v0.7.1
- **`sm`의 FilterBar가 폭에 따라 다른 물건으로 보였다 — 바 프레임을 되살렸다.** 처음 `sm` 규칙은 프레임(테두리·radius·면)을 버리고 컨트롤마다 테두리를 주는 방식이었는데, 넘침은 사라져도 **같은 컴포넌트가 `md`와 `sm`에서 서로 다른 물건**이었다. 폭이 바꾸는 것은 **담긴 것**이지 바 자신이 아니다.
  - 이제 `sm`에서도 바는 `md`와 같은 한 덩어리다 — 테두리 1px·radius 8·흰 면·세로 구분선 그대로. 달라지는 것은 안에 무엇이 서느냐뿐이고, 필터가 시트로 빠지면 **「필터」·검색·초기화 세 칸**이 남는다.
  - 도중에 나온 「초기화가 바 밖으로 떠 보인다」도 이걸로 함께 풀린다 — 프레임이 있으니 세 칸이 모두 그 안의 칸이다. 시트가 있을 때 초기화를 감추는 안도 만들어 봤다가 되돌렸다(필터를 열지 않고 바에서 바로 되돌리는 길을 남긴다).
  - 실측(390px) — 바 358×36 한 줄, 칸은 「필터」 58 · 검색 261 · 초기화 37이고 두 번째·세 번째 칸에만 세로 구분선이 선다. 넘침 0, 문서폭 390. 1200px은 그대로. filter-bar.md v0.13.1 → v0.16.0
- **프로토타입 미리보기 틀 안에서 다른 페이지로 가면 그 페이지가 틀 안에 갇혔다.** 「목록으로」를 누르면 목록 페이지가 **제 크롬(사이드바)까지 달고** 390px 틀에 들어가, 본문이 60px쯤으로 눌려 제목이 한 글자씩 세로로 쌓였다.
  - 미리보기는 이 화면이 그 폭에서 어떻게 보이는지를 재는 장치이지 브라우저가 아니다. 우리가 띄운 주소(`?proto-frame=1`)가 아닌 문서가 틀에 실리면 **최상위 창을 그 주소로 옮긴다.**
  - **링크의 `target`만 바꾸지 않았다** — 프로토타입이 JS로 옮기는 경로(`location.href = …`)를 놓친다. 바깥에서 iframe의 `load`를 보고 판정하므로 링크든 JS든 같은 결과가 된다.
  - 실측 — 틀 안에서 「목록으로」를 누르면 최상위가 그 페이지로 이동한다(틀은 사라지고 본문이 목록 페이지다). 시나리오 전환·`lg`·`비교` 같은 **우리가 띄우는 재로드는 최상위를 건드리지 않는다**(틀 유지, 비교는 틀 3개). build.py · planner.md
- **문서 미리보기에서 남의 컴포넌트가 스타일 없이 뜨고 있었다** — ContentList의 목록 끝 Pagination이 문서에서만 맨 텍스트(`‹ 1 2 … 17 ›`)로 보였다.
  - **원인은 CSS가 아니라 `depends-on`이다.** 문서 뷰어는 `depends-on`을 따라가며 그 문서의 CSS만 주입한다(`collectDepAssets`). content-list.md는 Pagination을 미리보기에 쓰면서 의존에 적지 않았고, 그래서 **문서에서만** 스타일이 없었다 — 컴포넌트 CSS도, 프로토타입도 멀쩡해서 눈으로 보기 전엔 알 수 없다.
  - 같은 검사를 전 문서에 돌렸더니 **14개 문서**가 같은 상태였다(input·checkbox·textarea·tooltip·segment·spinner가 `.btn`을, calendar·DRP가 `.segment`를, badge·toggle이 `.icon`을, empty-state가 표 클래스를, 그리고 filter-bar가 방금 넣은 `sm` 시트의 `.modal__*`을). 전부 `depends-on`에 채웠다.
  - **빌드에 검사를 넣었다** — 미리보기가 쓰는 클래스를 정의한 문서가 의존 폐포에 없으면 경고한다(아이콘 참조 검사와 같은 자리). 문서만 거짓말하는 종류의 오류라 사람 눈에 기대면 또 놓친다. 검사가 실제로 잡는지 의존을 일부러 빼고 확인했다. build.py
  - _spec.md v1.1.1 · empty-state v0.2.2 · content-list v0.51.3 · filter-bar v0.13.1 · date-range-picker v1.4.5 · textarea v1.1.1 · badge v1.1.1 · input v1.4.1 · calendar v1.1.2 · checkbox v1.1.2 · tooltip v1.0.2 · segment v1.2.3 · spinner v1.0.1 · toggle v1.2.1 (전부 PATCH — 스펙 변경 없는 메타데이터 수정)
- **Pagination의 정적 예시가 컴포넌트 자신의 축약 규칙을 어기고 있었다.** 문서가 적어 둔 규칙은 "첫·마지막은 항상, **현재 페이지 주변 1개씩만**"인데, 예시는 첫 페이지에서 `1 2 3 … 12`를 보여주고 있었다(규칙대로면 `1 2 … 12`). 문서 안의 살아 있는 데모(JS)는 규칙대로 그리고 있어서, 같은 문서 안에서 두 그림이 어긋났다.
  - **예시가 틀리면 규칙이 아니라 예시가 퍼진다.** 게시판 목록 프로토타입도 `1 2 3 … 17`로 짜여 있었고, 그걸 그대로 옮겨 ContentList의 목록 끝 예시에도 같은 오류를 심었다. 셋 다 고쳤다.
  - `md` 예시는 **가운데 페이지(5/12)** 로 바꿔 `1 … 4 5 6 … 12`가 되게 했다 — 양쪽 축약이 한 그림에 보인다. `sm` 예시는 첫 페이지(`1 2 … 12`)를 남겨 두 경우를 나눠 보여준다.
  - 규칙 문장 아래에 "주변 1개씩이다 — 첫 페이지에서는 `1 2 … 12`이지 `1 2 3 … 12`가 아니다"를 못박았다.
  - 검산 — 문서의 `pages()` 알고리즘을 그대로 옮겨 정적 예시 셋을 대조했다(5/12 · 1/12 · 1/17 전부 일치). pagination.md v1.2.0 → v1.2.1 · content-list.md v0.51.1 → v0.51.2
- **`file-upload--split`의 `sm` 정렬 버그** — 390px에서 안내문이 오른쪽, 추가하기 버튼이 왼쪽으로 벌어졌다(실측: hint 중심 **+28px**, btn 중심 **−127px**). `sm`에서 dropzone을 flex column으로 바꾸면서 `align-self` 리셋이 빠진 것이다. grid에서 세로를 잡던 `align-self`(hint `end` / btn `start`)가 flex column에서는 **교차축이 가로**라 그대로 좌우 정렬로 작동했다.
  - **두 줄을 더 적는 대신 구조를 바꿨다.** `text-align`·`max-width`는 되돌렸는데 `align-self`만 빠진 이유는 **되돌릴 목록을 손으로 관리했기 때문**이다. 좌우 배치 규칙 전체를 `@media (min-width: 768px)`로 옮기니 `sm`에서는 아예 걸리지 않아 **되돌릴 것이 없어졌다** — `sm` 미디어쿼리 블록을 통째로 지웠다. base가 이미 hint의 가운데 정렬·`max-width: 28em`도, 카드 격자의 `minmax(150px, 1fr)`도 갖고 있다. **선언하지 않으면 되돌릴 것도 없다.**
  - 제안받은 `align-self: center`도 이 화면은 고치지만 **hint 없는 split에서 틀어진다** — base가 그때는 `align-items: flex-start`라 버튼이 왼쪽이어야 하는데 가운데로 박힌다. 세 경우(hint 있음/없음 × 카드 유무)를 390·1000px에서 렌더해 확인했다.
  - 확인 — 390px: `display:flex`, hint·btn 중심 오프셋 **0**, 상자 높이 0장 117 / 2장 168(기본 배치 그대로). 1000px: `display:grid`, 0장 가운데·2장 왼쪽, 상자 높이 **184로 동일**(좌우 배치 동작 그대로). file-upload.md v0.6.1 → v0.6.2

### Changed
- **행 번호를 왼쪽 정렬로 바꿨다.** 번호 칸은 네 자리(9999)를 위해 잡아 두는데 오른쪽 정렬이라, 두 자리 글에서는 앞에 빈 자리가 생겨 **번호만 상자 안으로 들여쓰인 것처럼** 보였다 — 머리("종합 게시판")는 여백 끝에서 시작하는데 번호는 한 칸 안이라 시작선이 둘이었다.
  - 오른쪽 정렬은 **자릿수를 맞춰 크기를 비교할 때** 쓰는 것이다. 이 번호는 값이 아니라 "165번 글"로 지목하는 **식별자**라(문서가 그렇게 정의한다) 비교 대상이 아니다.
  - `tabular-nums`는 그대로 둔다 — 자리 폭이 같아야 열이 흔들리지 않는다. 칸 폭(4ch)도 그대로라 **제목의 시작선은 변하지 않는다.**
  - 실측(1000px) — 머리 40 · 번호 165/7/62/7 모두 40에서 시작 · 제목은 85(표시 없음)·109(표시 있음)로 이전과 동일. content-list.md v0.52.1 → v0.53.0
- **ContentList가 폭과 무관하게 하나의 상자가 됐다.** `sm`에서는 상자를 풀고 상하 구분선만 두던 규칙을 없앴다.
  - **전제가 성립하지 않았다.** 그 규칙의 근거는 "좁은 화면에서는 목록이 곧 화면이라 좌우 여백이 없다"였는데, 프로토타입의 `.page`는 `sm`에서도 좌우 24px을 유지한다(실측 — sm 오버라이드가 없다). 그래서 실제로는 회색 페이지 위에 **각진 흰 판**이 뜨고 아래에만 선 하나가 남아, 같은 컴포넌트가 폭에 따라 다른 물건으로 보였다. FilterBar에서 정한 것과 같은 판단이다 — **폭이 바꾸는 것은 담긴 것이지 상자 자신이 아니다.**
  - 이제 어느 폭에서든 흰 면 + `--radius-md` + `--shadow-sm`이고, `sm`의 상하 구분선과 `ul`의 상단 선은 사라졌다(상자의 경계가 면이므로).
  - 문서의 `sm` 미리보기 하네스도 실제 페이지처럼 **회색 배경 + 좌우 여백**으로 바꿨다 — 흰 배경에 여백 없이 렌더하고 있어서 문서에서는 이 문제가 보이지 않았다.
  - 실측 — 390·1000 모두 radius 8 · `shadow-sm` · 테두리 0 · `overflow:hidden`, 헤더 밑선 1px 유지. content-list.md v0.51.2 → v0.52.0
- **고정·신규 표시를 번호 거터에서 빼내 제목 앞으로 옮겼다** (`__pin` · `__new`가 `__headline`의 첫 자식).
  - 표시는 "이 글이 어떤 글인가"를 말하므로 **제목의 것이지 번호의 것이 아니다.** 거터에 두면 열이 하나 더 생겨 표시가 없는 행에서도 그만큼 자리를 비워야 했다.
  - 이웃이 숫자에서 제목으로 바뀌면서 **거터 시절에 잡았던 두 값의 근거가 함께 사라졌다** — 번호 쪽으로 바짝 붙이던 `margin-inline-end`와 숫자 글리프에 맞춘 광학 보정 `translateY(-1px)`을 걷어냈다. 간격은 headline의 gap이 준다.
  - 고정·신규가 **같은 자리**라 함께 넣지 않는다는 규칙은 그대로다(나란히 붙이면 제목이 아이콘 둘로 시작한다).
  - 실측 — md: 표시 있는 행의 제목이 24px 들여쓰이고 아이콘과 제목 첫 줄의 세로 오차 0. sm: 2줄 제목에서도 아이콘이 첫 줄에 선다. content-list.md v0.52.0 (같은 릴리스)
- **프로토타입 셸의 배경을 검정(`--color-gray-950`)으로.** 화면(틀)이 놓이는 **판**이지 화면의 일부가 아니다 — 밝은 회색 위에 흰 화면을 얹으면 어디까지가 화면인지 눈이 매번 다시 찾는다. 판 위의 글자(폭 표시 `390 × 844 px · sm`, 비교 레이블)는 `--color-text-inverse` 계열로 뒤집었다. 사이드바는 흰 패널 그대로이고, **틀 안은 그대로 제품 배경**이다(framed 문서는 배경을 투명으로 둔다). build.py · planner.md
- **폭 미리보기에서 「자유」를 없애고 `lg · md · sm · 비교` 순으로 세웠다.** 창 폭을 그대로 쓰던 「자유」는 `lg`(1280×800)가 대신한다 — 버튼 넷이면 접힌 레일에서도 한눈에 들어온다.
  - **열면 바로 `lg` 틀에서 시작한다.** 창이 1280보다 좁으면 사이드바가 먼저 접히고(그래서 1440 창에서는 열자마자 접힌 상태다), 그래도 모자라면 가로로 스크롤한다.
  - `free`는 버튼에서만 사라지고 내부 상태로 남는다 — 틀을 만들기 전의 상태이자 되돌릴 자리(원래 자식들)를 가리키는 이름이다.
  - 실측(1440×980) — 첫 화면에서 버튼 `lg* md sm 비교`, 틀 1280×800, 사이드바 접힘, 표시 `1280 × 800 px · lg`. md 768×1024 · sm 390×844 · 비교 틀 3개. 콘솔 오류 없음. build.py · planner.md
- **폭 미리보기의 세로도 기기 값을 따른다** — `lg` 1280×**800**(데스크톱) · `md` 768×**1024**(태블릿) · `sm` 390×**844**(폰). 전에는 폭만 맞추고 높이는 창을 그대로 써서, **폰인데 세로가 끝없이 긴 화면**이 나왔다 — "한 화면에 어디까지 들어오는가"가 안 보이므로 접힘·「더 보기」·하단 버튼을 판단할 수 없었다.
  - 창이 그 높이보다 낮으면 **바깥 페이지가 세로로 스크롤된다.** 틀을 줄여 창에 맞추면 버튼이 844를 말하면서 다른 높이를 보여주게 된다 — 폭에서 이미 겪은 실수다(lg가 1280을 말하며 1095를 보여줬다).
  - 비교 모드는 처음부터 이 값을 쓰고 있었다. 이제 단일 폭과 비교가 같은 기기 값을 공유한다.
  - 폭 표시도 `390 × 844 px · sm`으로 바뀌었다. 모서리를 끌면 폭만 변하고 세로는 그대로다.
  - 실측(1440×980 창) — sm 틀 안 390×844 · md 768×1024(문서 1088로 세로 스크롤) · lg 1280×800. 콘솔 오류 없음. build.py · planner.md
- **Pagination 좌우 화살표를 숫자 버튼과 같은 정사각으로 맞췄다** — md `16×24 → 32×32`, sm `16×16 → 28×28`. 숫자가 32×32인데 화살표가 16×24라 **누를 수 있는 넓이가 1/6**이었고, 높이도 달라 화살표만 줄에서 떠 보였다.
  - 원래 의도는 광학 보정이었다 — 투명 배경일 때 화살표 좌우 여백이 숫자 사이 간격에 더해져 보이지 않게 하려던 것. 그런데 **덜어낸 여백은 실제로 문제가 아니었다**: 셰브런 아이콘이 24 viewBox 안에 이미 여백을 갖고 있어 32px 상자로 키워도 눈에 보이는 간격이 벌어지지 않는다. 16·24·28·32를 나란히 렌더해 확인했다.
  - 반면 **잃은 것은 두 가지였다.** ① 같은 줄에서 화살표만 맞히기 어렵다. ② `hover`·`disabled`가 배경을 칠하는 순간 얇은 조각으로 드러나 숫자와 리듬이 어긋난다 — 특히 첫 페이지의 disabled 이전 버튼이 회색 조각으로 남았다. **광학 보정이 클릭 영역을 깎으면 그건 보정이 아니다.**
  - sm도 같은 규칙으로 정리했다. 화살표·숫자·ellipsis가 **한 높이를 받는 규칙 하나**를 공유하게 바꿔서, 크기를 바꿀 때 셋 중 하나만 빠지는 일이 없다(이전엔 sm에서 화살표만 따로 16×16이었다). pagination.md v1.0.2 → v1.1.0

- **ContentList 머리의 글씨를 20 → 17px, 밑선을 gray-500 → 행 구분선과 같은 gray-100으로 통일했다.** 상자 이름이 목록이 담은 글보다 크고, 밑선이 상자를 가로지르는 검은 자처럼 보였다.
  - **글씨** — `--font-size-h3`(20px, 섹션 제목)이었는데 상자 머리는 섹션 제목이 아니라 **상자의 이름**이다. 행 제목이 17px/600이라 3px 차이밖에 안 나서, 사람이 보러 온 **글 제목**보다 "자료 목록"이라는 라벨이 더 크게 읽혔다. 카드 제목과 같은 급(`--font-size-h4` 17px)으로 내리고 구분은 열 이름(13px)·색·자리가 맡는다. 15px도 재 봤는데 열 이름과 붙어 상자 이름으로 읽히지 않는다.
  - **밑선 — 두 가지가 어긋나 있었다.** ① 처음 `--color-border-strong`(gray-500)으로 잡을 때는 컨테이너에 **테두리가 있어서** 그 선들 사이에서 머리선이 한 단계 위로 읽혔다. 같은 사이클에 상자가 선을 버리고 면과 그림자로 바뀌면서 이 선만 남아 화면에서 가장 단단한 모서리가 됐다 — **기준이 바뀌면 그 기준으로 잡았던 값도 다시 재야 한다.** ② 더 근본적으로, **그 위계를 선이 말할 필요가 없었다.** 머리는 이미 타이포가 말한다 — 소제목은 17px/600 진한 글자, 열 이름은 13px 회색, 머리에는 번호도 링크도 없다. 선까지 위계를 말하면 같은 말을 두 번 하면서 상자만 딱딱해진다.
  - 그래서 **선은 리듬만 담당하고 위계는 타이포가 담당한다**로 정리하고, 상자 안의 가로선을 **한 색(gray-100)** 으로 통일했다. gray-500 → gray-300 → gray-100을 나란히 렌더해 확인했고, 통일해도 머리는 그대로 머리로 읽힌다. `sm`의 필터 행이 넘겨받는 선도 같은 색으로 맞췄다.
  - 중간에 **선을 여백에 맞춰 줄이는 안**(인셋)도 그려 봤는데, 머리선만 인셋하면 **가장 진한 선이 가장 짧아져** 위계가 뒤집힌다 — 그 아래 연한 행 구분선이 상자 폭을 꽉 채우고 있어 약한 선이 더 구조적으로 읽힌다. 인셋은 선 전체의 규칙일 때만 성립하는데, 색을 통일하고 나니 인셋 자체가 필요 없어졌다. content-list.md v0.47.0 → v0.48.1

- ContentList: 행 hover 농도를 8% → **4%**(`--color-action-neutral-faint`)로 낮췄다. 측정: 배경 `rgba(19,20,22,0.04)`, 고정·읽음 제목 색은 hover 중에도 유지. content-list.md v0.38.0 → v0.38.1 (PATCH)

### Changed
- ContentList: 행 hover를 브랜드 틴트에서 **중립면**(`--color-action-neutral-subtle`, 8%)으로 바꾸고, hover 시 **제목 색을 바꾸던 규칙을 제거**했다. 데이터 테이블 행 hover와 같은 토큰을 쓰던 것을 일부러 벗어난다 — 이 목록은 제목 색이 이미 두 가지 사실(고정·읽음)을 나르고 있어서 hover까지 색을 쓰면 서로를 덮는다. 브랜드 틴트를 쓰면 고정 행에 올린 순간 주황 제목이 파랑이 되어 **보고 있는 동안 고정 표시가 사라진다**(렌더로 확인). 이 컴포넌트의 규칙을 문서에 못박았다: **색은 콘텐츠의 상태를 뜻하고, 인터랙션은 중립면으로 표시한다.** 농도는 브랜드 틴트와 같은 8%라 눈에 걸리는 정도는 그대로고, 행 전체가 링크라 커서도 함께 바뀌므로 배경 변화만으로 affordance는 충분하다. 측정 — 고정 hover: 배경 `rgba(19,20,22,0.08)` / 제목 `rgb(231,73,0)` 유지, 읽음 hover: 제목 `rgb(109,120,130)` w400 유지. 4안(현재 / 중립 8% / 중립 10% / 브랜드 틴트+제목 유지) 렌더 비교. content-list.md v0.37.0 → v0.38.0 (MINOR)

### Changed
- ContentList: 고정 표시를 **핀 아이콘 + 주의색 제목**으로 바꾸고, 블록을 닫는 경계선을 없앴다. 제목 색이 행마다 붙는 표시라 블록 경계를 따로 그을 필요가 없다 — 색이 끝나는 곳이 곧 고정이 끝나는 곳이다. 면(배경)·선·색을 함께 쓰면 같은 사실을 세 번 말하게 된다.
  - **규칙 순서가 동작을 정한다.** 고정 제목 색은 읽음 규칙 **뒤에** 선언한다 — 둘 다 명시도 (0,2,0)이라 순서로 갈리고, 앞에 두면 읽은 고정 글의 제목이 회색으로 돌아간다. 고정은 운영자가 정한 성질이고 읽음은 사용자마다 다른 상태라, 읽었다고 고정이 아니게 되지는 않는다. DON'T에 명시.
  - **읽음 규칙은 죽지 않는다.** 읽음은 굵기가 주 신호이고 색은 보조라(문서의 읽음 절), 고정 행에서 색을 가져가도 굵기로 계속 갈린다. 앞선 검토에서 "읽음 규칙이 사라진다"고 본 것은 색만 보고 판단한 것으로 과했다 — 렌더 측정으로 정정: 고정+읽음 행은 색은 같고 weight 400/600으로 갈린다.
  - hover(0,3,0)는 이 규칙을 이겨 브랜드 색으로 바뀐다(측정: `rgb(17,90,198)`). 올려둔 동안에는 "지금 누를 수 있는 것"이 우선이고, 손을 떼면 고정 색으로 돌아온다.
  - ⚠️ **대비 기준 미달을 감수한 결정이다.** 주의색(orange-600)은 흰 배경에서 **3.92:1**로 WCAG AA 본문 기준(4.5:1)에 못 미친다(15/17px semibold는 large text 예외 아님). 고정이라는 사실은 핀 아이콘이 함께 전달하므로 색 단독 전달(1.4.1)은 아니지만, 글자 자체의 읽기 쉬움(1.4.3)은 기준 아래다. 문서의 사용 지침·CSS 주석·접근성 절에 모두 명시했고, 기준을 맞춰야 하면 `--color-orange-700`(5.35:1) 한 줄 교체다.
  - 측정: 고정 `rgb(231,73,0)` w600 · 읽음 `rgb(109,120,130)` w400 · 기본 `rgb(19,20,22)` w600, 모든 행 사이 구분선 `1px rgb(230,232,234)`(닫는 선 없음). content-list.md v0.36.0 → v0.37.0 (MINOR)

### Removed
- ContentList: 고정 행의 **배경색을 없앴다.** 핀 아이콘이 생긴 뒤로 배경은 같은 말을 두 번 하는 것이 됐다 — 색면은 목록에서 가장 비싼 표현이라(hover의 브랜드 틴트와 자리를 다투고, 읽음 상태와도 겹쳐 보인다) 아이콘과 닫는 선으로 이미 전달되는 사실에 쓸 이유가 없다. 이제 고정 표시는 **거터의 핀 + 블록을 닫는 선** 둘이다.
  - 대안으로 검토한 **제목 색 변경은 채택하지 않았다.** 그 자리는 이미 읽음 여부의 것이라(안 읽음 gray-950 / 읽음 gray-500) 한 속성에 두 의미를 얹으면 "읽은 고정 글"에서 하나가 반드시 진다 — 렌더로 확인: 읽은 고정 행이 안 읽은 고정 행과 같은 색이 되어 읽음 규칙이 사라졌다. 대비도 모자란다(주의색 orange-600 흰 배경 **3.92:1** — AA 미달, 15px는 large text 예외에 해당하지 않는다. 통과하는 orange-700은 5.35:1이지만 제목으로 쓰기엔 탁하다). 5안(현재 / 배경 없음 / 주의색 제목 / orange-700 제목 / bold 제목) 렌더 비교.
  - `--pinned`는 이제 자체 시각 속성이 없다 — 닫는 선 선택자의 기준점이자 의미 훅으로 남는다. content-list.md v0.35.0 → v0.36.0 (0.x draft)

### Changed
- ContentList: 고정 행의 배경을 한 단계 연하게(`--color-surface-caution-tint` orange-100 → `--color-surface-caution-subtle` orange-50) 낮추고, 닫는 선에서 **색을 뺐다**(주색 50% → `--color-border-strong` gray-500). 목록 안에서 가로로 길게 그어진 유채색은 장식으로 읽힌다 — 행 하나를 칠하는 면과 달리 선은 색을 쓸 자리가 아니었다. 중립선을 쓰면 목록 안에 새 의미를 만들지 않고 header 아래 섹션 경계선이 이미 가진 "여기서 구획이 끝난다"는 뜻을 빌리게 된다(같은 토큰이다). 일반 행 구분선(gray-100)과는 명도가 크게 갈려 구분에는 충분하다. 배경을 낮춘 근거는 핀 아이콘이다 — 고정이라는 사실은 아이콘이 말하므로 면은 거들기만 하면 된다. 고정 항목끼리의 전용 구분선도 없앴다(배경이 이어져 한 덩어리로 읽히므로 그 안에 선을 둘 이유가 없다). 5안 렌더 비교. 측정: 다음 행 border-top `1px solid rgb(109,120,130)`. content-list.md v0.34.0 → v0.35.0 (MINOR)

### Removed
- 토큰 `--color-surface-caution-tint`(orange-100)와 `--color-border-caution`(주색 50%) 제거. 둘 다 이번 사이클에 고정 행을 위해 추가했다가 위 변경으로 쓰이지 않게 됐다. 계열 대칭(brand·info의 `-tint`, error·success의 base border)을 근거로 남겨둘 수도 있었지만, **토큰은 쓰이고 있는 결정을 기록하는 자리**다 — 나중에 필요해지면 그때 실제 용례와 함께 추가한다. color.md v1.6.0 → v1.7.0

### Changed
- ContentList: 표시(신규·고정)를 번호 **왼쪽**으로 옮겼다. 표시가 행의 맨 앞에 서야 훑을 때 먼저 걸린다 — 번호는 지목용 식별자라 표시를 찾는 눈길을 가로막지 않는 편이 낫다. subgrid 열 순서를 뒤집고(표시 1열 · 번호 2열) 마크업도 같은 순서로 바꿨다(subgrid 미지원 브라우저의 flex 배치가 DOM 순서를 따르므로 둘이 어긋나면 안 된다). 번호와의 2px 간격은 **번호의 왼쪽이 아니라 표시의 오른쪽**에 둔다 — 표시가 없는 행에서는 이 여백도 함께 사라져야 번호 열의 시작선이 흔들리지 않는다. `sm`에서는 번호가 숨으므로 이 여백도 0으로 되돌린다. 측정(1300px): 표시 48px · 번호 66px · 제목 110.9px, 표시 유무와 무관하게 네 행 모두 동일. content-list.md v0.33.0 → v0.34.0 (MINOR)

### Added
- **ContentBody** 추가 (`components/organisms/content-body.md` v0.1.0) — CMS 에디터가 만든 **클래스 없는 HTML**에 시스템 타이포·간격을 입히는 스코프 컨테이너. REQ-001 우선순위 1의 두 번째 항목이다. 시스템은 클래스 기반이라 에디터 출력(`h2`~`h4`·`p`·`ul/ol`·`table`·`img`·`blockquote`·`a`)을 받을 곳이 없었고, 그래서 본문만 브라우저 기본 스타일로 떠 있었다. **시스템에서 순수 태그 선택자를 쓰는 유일한 구간**으로 정의하고 모든 규칙을 `.content-body` 아래로 가뒀다.
  - 세로 리듬은 `> * + *`로 건다 — 첫 요소의 위 여백과 마지막 요소의 아래 여백이 자동으로 0이 되어, 본문 상자를 다른 것과 붙일 때 여백을 상쇄할 필요가 없다. 제목 위 여백은 아래보다 넓다(제목은 뒤 문단의 것이지 앞 문단의 꼬리가 아니다). 명시도 (0,1,1) > (0,1,0)이라 순서와 무관하게 적용된다.
  - 에디터의 최상위 제목은 `h2`다 — `h1`은 글 제목(ContentHeader)의 것이라, 본문 `h2`를 페이지 제목이 아니라 섹션 제목 크기(`--font-size-h3`)에 맞췄다. 본문 제목이 글 제목보다 커 보이면 위계가 뒤집힌다.
  - **시스템 컴포넌트를 만나면 비켜 간다** — `table:not(.table)` · `a:not(.link):not(.btn)` · `img:not([class])`. 태그 선택자는 깊이를 가리지 않아, 본문 안에 컴포넌트가 섞이면 그 내부까지 본문 스타일이 얹힌다. 안전망이지 허가가 아니라는 점도 문서에 명시했다.
  - 넓은 표는 `content-body__scroll` 래퍼 안에서만 가로 스크롤한다(`min-width: max-content`). 래퍼가 없으면 페이지 전체가 가로로 밀린다 — `sm`에서 특히 그렇다. 열을 접거나 숨기지 않는다(표는 비교 대상이라 접으면 의미가 사라진다 → `adaptation.md`). 렌더 측정: 390px에서 페이지 가로 스크롤 없음, 표만 스크롤됨.
  - 표는 `table--info`의 시각 톤을 태그 선택자로 옮겼다 — 값을 새로 정하지 않았다. 본문 안이라고 표가 달라 보이면 안 된다.
  - **max-width를 두지 않고**(→ `adaptation.md`) **밀도도 풀지 않는다**(→ `product.md`). 본문은 `--font-size-base` / `--line-height-reading`이다. `--line-height-prose`로 늘리는 것은 DON'T로 명시했다. 렌더 측정: 14px / 21px.
  - 상태는 loading(Skeleton + `aria-busy`)과 error(Banner)만 둔다. **empty는 두지 않는다** — 본문 없는 글은 영역 자체를 렌더하지 않으면 되고, "내용 없음"을 띄우면 없는 것을 있는 것처럼 보이게 만든다.
  - `sm`에서 바뀌는 것이 없다. 본문은 한 덩어리 글이라 접힐 열도 숨길 부속도 없다 — 이것이 게시판을 테이블로 만들지 않은 이유이기도 하다.
  - 접근성: 제목은 크기가 아니라 문서 순서로 고른다, 본문 링크의 밑줄을 유지한다(WCAG 1.4.1), 가로 스크롤 표에 `tabindex="0"`+접근 가능한 이름, `alt`는 에디터에서 필수 입력으로 막는다. 대비 — 본문 16.1:1 · 인용 8.68:1 · 캡션 4.51:1.
- `components/_index.md` v1.5.0 · `components/_requests.md` v0.5.0 — Organism 구현 목록과 REQ-001 진행 상태에 ContentBody 반영.

### Changed
- `icon-pin`을 Figma 라이브러리 버전으로 교체하고 `LOCAL_ICONS`를 비웠다. 동기화 결과 59개 유지, 사라진 아이콘 없음. 임시 글리프(수직 압정)와 정식 아이콘(비스듬한 압정)은 형태가 다르지만 거터 배치·정렬·색은 그대로 성립한다 — 아이콘 칸이 16px 고정이라 모양이 바뀌어도 열이 흔들리지 않는다.

### Changed
- 아이콘 원본을 **두 곳**으로 바꿨다(`FIGMA_SOURCES`) — 라이브러리 파일의 아이콘 페이지(`JI2Jfgq…/132-13`)와 기존 아이콘 파일(`NIechy…`). 라이브러리 한 곳만 읽자 삭제 가드가 걸렸고, 그 로그에서 이유가 드러났다: 그 페이지에는 **단색 아이콘 49개만** 있고 다색 아이콘 10개(`icon-new`·`icon-pdf`·`icon-excel`·`icon-file-drop` + 메뉴 진입 6종)는 아이콘 파일에만 있다. 그대로 갔으면 `icon-new`가 사라져 ContentList의 신규 표시가 깨졌을 것이다. 이름이 겹치면 **앞 소스가 이긴다** — 새 아이콘이 만들어지는 라이브러리를 앞에 둔다. 워크플로의 `file_key`·`icon_page` 입력은 이제 "단일 소스로 시험할 때만" 쓰는 덮어쓰기다. 가짜 Figma로 main()을 끝까지 돌려 검증: 59개 수집, 다색 10개·`icon-pin`·`icon-new` 전부 유지, 커스텀 fill·메뉴 색 규칙도 그대로 적용된다.

### Fixed
- `scripts/sync_icons.py`: 개별 `icons/*.svg` 쓰기를 **삭제 가드 뒤로** 옮겼다. 가드는 sprite만 지키고 있어서, 대상을 잘못 짚은 실행이 중단되더라도 개별 SVG는 이미 덮어써진 뒤였다(테스트에서 실제로 걸렸다 — "파일 변경 없음: False"). 이제 가드를 통과한 뒤에만 어떤 파일도 쓴다.
- `scripts/sync_icons.py`: `categories.json`을 **모든 아이콘에 프레임이 있을 때만** 다시 쓴다(`any` → `all`). 소스가 여럿이면 한쪽은 프레임으로 묶여 있고 다른 쪽은 평면이라, `any` 판정이면 평면 쪽 수십 개가 통째로 "기타"로 쓸려 들어간다. 일부만 분류된 상태에서는 기존 파일을 그대로 둔다.

### Changed
- 아이콘 원본을 **디자인 시스템 라이브러리 파일의 아이콘 페이지**(`JI2JfgqCQ8vCDRCSNtNQt4` / `132-13`)로 바꿨다. 워크플로 기본값이라 입력 없이 **Run workflow만 눌러도** 되고, 예약 실행도 같은 대상을 본다. 아이콘이 만들어지는 곳이 곧 원본이어야 한다 — 지금까지의 대상(`NIechy…`)은 아이콘이 새로 추가되지 않는 파일이라, 새 아이콘이 리포로 들어올 길이 없었다.

### Added
- `scripts/sync_icons.py`: **기존 아이콘이 사라지는 동기화를 막는다.** 대상 파일·페이지를 잘못 짚으면 sprite가 통째로 다른 파일 내용으로 바뀌는데, 그 사고는 항상 "있던 아이콘이 없어진다"는 형태로 나타난다. 삭제만 막으면 대상을 잘못 짚은 경우를 전부 걸러낼 수 있고, 추가·변경은 정상 동작이라 그대로 통과한다. 사라지는 아이콘이 있으면 이름을 전부 찍고 **파일을 쓰기 전에** 멈춘다. 의도한 폐기라면 `FIGMA_ALLOW_REMOVALS=1`(워크플로 `allow_removals` 입력)로 진행한다. `RENAME_MAP`의 구 이름과 `LOCAL_ICONS`는 사라지는 게 정상이라 검사에서 제외한다. 실제 `icons/`로 검증: 같은 세트+신규 1개는 통과, 3개만 받은 경우 56개 삭제로 중단, 1개 폐기도 중단. 이 가드가 있어 대상을 바꿀 때마다 dry run을 먼저 돌릴 필요가 없다.

### Added
- `scripts/sync_icons.py`: **`Icon/collapse` 형식의 이름도 아이콘으로 인식**한다(→ `icon-collapse`). 진단 출력으로 확인한 결과, 디자인 시스템 라이브러리 파일(`JI2Jfgq…`)의 아이콘은 전부 `Icon/xxx` 표기였다 — Figma에서 슬래시는 컴포넌트 목록을 폴더처럼 접어 보여주는 표기다. 컴포넌트도 위치도 맞는데 **이름 표기만 달라** 0개로 잡히고 있었다. 같은 아이콘이 파일에 따라 다른 이름으로 들어오면 안 되므로 읽는 쪽에서 하나로 맞춘다(슬래시·공백 → 하이픈, 소문자). `icon-`으로 시작하는 이름은 손대지 않는다 — 기존 파일의 이름을 그대로 지켜야 `RENAME_MAP`·`CUSTOM_FILLS` 같은 이름 기반 규칙이 어긋나지 않는다.

### Added
- `scripts/sync_icons.py`: 조건에 맞는 컴포넌트를 하나도 못 찾았을 때 **그 자리에 무엇이 있는지 두 겹까지 타입·이름으로 출력**한다. "0개 발견"만으로는 원인을 구분할 수 없다 — 지정한 노드가 페이지·프레임이 아닌 것인지, 타입이 `COMPONENT`가 아닌 것인지(라이브러리에서는 변형이 묶인 `COMPONENT_SET`인 경우가 많다), 이름이 `icon-`으로 시작하지 않는 것인지, 한 겹보다 깊은 것인지. 실제로 라이브러리 파일(`JI2Jfgq…`)의 `132:13`을 가리켰을 때 0개가 나왔는데, 접근은 됐으므로 이 넷 중 하나다.

### Added
- `scripts/sync_icons.py`: 대상 **페이지를 지정**할 수 있게 했다(`FIGMA_ICON_PAGE`). 이름(`ICON`)도 되고 노드 id도 된다 — Figma 주소창의 `node-id=132-13`을 그대로 붙여넣으면 하이픈을 콜론으로 바꿔 쓴다. id 쪽이 안전하다: 페이지 이름은 바뀔 수 있지만 id는 그 페이지가 살아 있는 한 그대로다. 지금까지는 노드 `0:1` — **파일의 첫 페이지**를 하드코딩하고 있었다. 아이콘 파일은 첫 페이지가 곧 ICON 페이지라 드러나지 않던 제약인데, 페이지가 여럿인 디자인 시스템 라이브러리 파일을 가리키는 순간 문제가 된다. 비우면 첫 페이지를 쓰므로 기존 동작은 그대로다. 못 찾으면 그 파일의 페이지 이름 목록을 찍고 종료한다.
- `scripts/sync_icons.py` · `sync-icons.yml`: **dry run**(`FIGMA_DRY_RUN`) 추가. 무엇을 가져올지 프레임별로 출력만 하고 파일을 쓰지 않는다. 대상 파일·페이지를 바꿔 시험할 때 스프라이트를 통째로 갈아엎지 않고 먼저 확인하기 위한 것 — 다른 파일에 `icon-*` 컴포넌트가 있으면 그 파일 내용으로 sprite 전체가 교체되므로, 확인 없이 대상을 바꾸는 것은 위험하다. 워크플로에 `file_key`·`icon_page`·`dry_run` 입력을 두고, dry run일 때는 빌드·커밋·Pages 배포 단계를 건너뛴다. 예약 실행은 입력이 비어 기존과 동일하게 동작한다.
- `sync_icons.py`: 컴포넌트를 못 찾았을 때의 안내를 구체화 — 이름이 `icon-`으로 시작하는 COMPONENT를 페이지 바로 아래나 프레임 **한 겹** 안에 둬야 찾는다(두 겹 이상은 못 찾는다는 것도 docstring에 명시).

### Fixed
- `scripts/sync_icons.py`: `LOCAL_ICONS` 가드가 **sprite만 지키고 `categories.json`은 못 지키던 것 수정.** 2026-09-02 동기화에서 실제로 드러났다 — sprite의 `icon-pin`은 살아남았지만 categories.json에서는 지워졌다(그룹이 매 sync마다 Figma 프레임에서 새로 만들어지므로 Figma에 없는 아이콘은 그대로 사라진다). sprite에는 있는데 categories.json에는 없으면 planner의 아이콘 표에서만 빠지는 어긋난 상태가 된다. `LOCAL_ICONS`를 set에서 **dict(이름 → 그룹)**로 바꿔 분류를 코드에 선언하고, 그룹 재생성 후 되돌린다. 분류를 이전 파일에서 읽지 않는 이유: 이 복구 없이 sync가 한 번이라도 돌면 이전 파일에서 분류가 사라져 그다음부터는 되돌릴 근거가 없다(실제로 이번에 그렇게 돼서, 파일 조회 방식으로 짰을 때 "기타"로 흘러가는 것을 확인했다).

### Changed
- Figma 아이콘 동기화 복구. `FIGMA_TOKEN` 만료로 `403 Forbidden`이 나 **8/3부터 5주간 예약 동기화가 전부 실패**하고 있었다(8/3·8/10·8/17·8/24·8/31). 토큰 갱신 후 수동 실행으로 복구 확인. 이번 동기화로 Figma의 아이콘 목록에 `icon-pin`이 **없다는 것도 확인됐다** — 아이콘 소스 파일(`NIechyVGJuzroGt5UdFFOR`)과 디자인 시스템 라이브러리 파일(`JI2JfgqCQ8vCDRCSNtNQt4`)이 다른 파일이고, sync는 앞쪽만 읽는다.

### Added
- ContentList: 고정 항목에 **핀 아이콘**(`.content-list__pin`, `icon-pin`) 추가. 신규 표시와 **같은 거터 칸**을 쓰고, 둘 다 해당하는 항목에서는 핀이 이긴다 — 고정 항목은 이미 맨 위에 모여 있어 "새로 올라왔다"를 따로 신호할 필요가 적고, 운영자가 손으로 지정한 "먼저 봐라"가 날짜로 자동 붙는 표시보다 앞선다. 칸을 둘로 나누면 표시가 하나뿐인 대다수 행에서 빈 칸만큼 제목이 밀린다(측정: 핀·N·표시 없음 세 행 모두 제목 시작선 110.9px @1300 / 72px @390으로 동일). 색은 `--color-fill-caution`(orange-500)이고 `icon-pin`이 단색 `currentColor` 아이콘이라 `fill`이 아니라 `color`로 칠한다 — 신규(빨강)와 색으로도 갈린다. 열 제거 가드도 `:not(:has(.content-list__new, .content-list__pin))`으로 확장했다. **접근성**: 고정이 배경색 단독 신호였던 것이 해소된다 — `aria-label="고정"`으로 스크린리더에 나가고 흑백 출력에도 남는다. 이에 따라 `sr-only` 대체 지침을 걷어냈다. content-list.md v0.32.0 → v0.33.0 (MINOR)
- 아이콘: `icon-pin` 추가. **Figma 라이브러리에서 동기화된 정식 아이콘이다.** 처음에는 Figma에 없어 기존 규칙(24 그리드·단색 면·`currentColor`)에 맞춰 임시로 그려 넣고 `LOCAL_ICONS`로 지켰지만, 디자이너가 라이브러리에 `Icon/pin`을 만든 뒤 동기화로 교체됐다(비스듬한 압정 형태). `LOCAL_ICONS`는 다시 비었다 — Figma에 없는 아이콘을 코드에서 임시로 관리해야 할 때만 채운다.
- `scripts/sync_icons.py`: `LOCAL_ICONS` 보존 가드 추가. sync는 Figma에서 받은 것만으로 sprite를 다시 만들기 때문에, 가드가 없으면 `icons/icon-pin.svg` 파일은 남아도 **sprite에서 조용히 사라져** 문서의 `#icon-pin` 참조가 빈 칸이 된다. Figma가 같은 이름을 내려주면 Figma 쪽이 이기고, 그때 안내 문구와 함께 목록에서 지우면 된다.

### Changed
- ContentList: 분류(`__cat`)의 색을 `--color-text-brand`(blue-600)에서 `--color-text-body`(gray-950)로. 목록 안에서 파란 글자는 hover와 "누를 수 있는 것"을 뜻하는데 분류는 누를 수 없는 값이라(누르는 역할은 `sm`의 필터 칩이 맡는다) 제목 옆·아래의 파란 텍스트가 두 번째 링크로 읽혔다. 같은 계열의 검정으로 올리면 "메타 중 하나만 진하다"는 위계는 그대로 남고 링크 오해만 사라진다. 4안(brand / body / label gray-700 / 메타와 동일 gray-500)을 렌더 비교했다. **읽은 항목에서는 분류도 함께 `--color-text-subtle`로 내린다** — 제목만 내리면 그 행에서 가장 진한 글자가 제목이 아니라 분류가 되어 위계가 뒤집힌다(측정: 읽음 행 제목·분류·날짜 모두 `rgb(109,120,130)`, 안 읽음 행 분류 `rgb(19,20,22)`). 흰 배경 대비는 제목과 같은 18.43:1. 텍스트 색 위계표에 분류 행과 근거를 추가. content-list.md v0.31.2 → v0.32.0 (MINOR)

### Changed
- ContentList: 고정 행의 배경을 `--color-surface-caution-subtle`(orange-50)에서 **`--color-surface-caution-tint`(orange-100)**로 올리고, 좌측 강조 바를 걷어냈다. subtle은 흰 배경 위에서 면이 잘 읽히지 않아 바가 없으면 고정 여부가 스쳐 지나갔는데, tint는 배경만으로 면이 선다. 대신 **고정 블록을 선으로 닫는다**: 고정 항목끼리는 주의 톤의 연한 선(`--color-border-caution-subtle`)으로 나누고 — 중립 회색 선이 들어가면 같은 블록이 잘려 보인다 — 고정이 끝나고 일반 목록이 시작되는 자리에는 `--color-border-caution`(주색 50%) 선을 둔다. 일반 행 구분선(`--color-border-faint` gray-100)과 색이 달라 "여기까지가 고정"이 한눈에 닫힌다. 두께는 1px 그대로다 — header 아래 섹션 경계선과 같은 무게라 목록 안의 선 위계가 늘지 않고, 색만으로 갈린다. `--color-fill-caution`(orange-500)은 쓰지 않는다 — 면으로는 맞지만 선으로 쓰면 채도가 튀어 목록에서 가장 강한 요소가 제목이 아니라 이 선이 된다. 두께(2px caution-subtle · 2px orange-200 · 1px/2px orange-500 · 1px orange-300)와 채도(orange-500 · rgba(231,73,0,.5) · orange-400 · orange-300 · orange-700 · orange-200) 두 축으로 렌더 비교했다 — 연한 쪽(caution-subtle·orange-200)은 회색 구분선과 구분되지 않고, 진한 쪽(orange-500·400)은 채도가 튄다. `border-caution`(주색 50%)이 같은 계열이면서 채도가 눌려 있어 경계로는 읽히면서 목록의 위계를 깨지 않았다 — `border-error`·`border-success`와 같은 짜임이라 계열 안에서도 어긋나지 않는다. 경계선은 마지막 고정 행의 `border-bottom`이 아니라 **다음 행의 `border-top`**에 둔다: 고정 항목이 목록의 마지막일 때 컨테이너 아래 선과 겹쳐 두 줄이 되는 것을 막고, 기존 행 구분선을 덮어써 선이 이중으로 쌓이지 않는다(측정: 고정 행 배경 `rgb(255,246,227)`, 다음 행 border-top `1px solid rgba(231,73,0,0.5)`, hover 시 배경만 브랜드 틴트로 바뀌고 경계선은 유지). content-list.md v0.30.0 → v0.31.2 (MINOR)

### Added
- 토큰: `--color-border-caution`(주색 50%) 추가 — 주의 톤 면을 닫는 경계선. 오류·성공에는 `border-error`/`border-success`(각 주색 50%)와 `-subtle`(15%)이 짝으로 있는데 주의에는 `-subtle`뿐이라 계열에 구멍이 있었다. `--color-fill-caution`은 면에 쓰라고 만든 색이라 선으로 쓰면 채도가 튀고, `-subtle`은 중립 구분선과 구분되지 않는다.
- 토큰: `--color-surface-caution-tint`(orange-100) 추가. 브랜드·정보 계열에 이미 있는 `-subtle`(50, 넓은 면적) / `-tint`(100, 좁은 면적) 짝을 주의 계열에도 맞췄다 — ContentList의 고정 행처럼 흰 배경 위에서 면이 분명히 읽혀야 하는 좁은 영역에 쓴다. color.md v1.5.0 → v1.6.0 (MINOR)

### Added
- ContentList: 메타 3열에 **최소 폭**을 두고(`minmax(최소, auto)`), `--content-list-meta-cols`로 재정의할 수 있게 했다. 기본 `8rem · 6rem · 5rem`. 폭이 순수 auto면 그 목록에 실제로 들어온 값이 폭을 정하는데, 행이 없는 상태(empty·loading)에는 정할 값이 없어 라벨 자신의 글자 폭으로 잡혀 본목록과 열 위치가 어긋났다(측정: 분류 열 923.4px → 1021px). 최소값을 두면 값이 그 안에 들어가는 한 두 상태가 같아지고(측정: 라벨 896 / 1024 / 1120px 완전 일치), 최소값을 넘는 긴 분류명은 여전히 잘리지 않고 열이 늘어난다(측정: 228.45px, 잘림 없음) — 고정 폭의 잘림과 auto의 흔들림 중 하나를 고를 필요가 없다. 기본값은 실측 콘텐츠 폭과 empty의 라벨 폭 **양쪽 다** 덮도록 정했다: 조회를 4rem(64px)로 뒀을 때 값(72.5px)과 라벨(66px)이 모두 최소값을 넘어 두 상태가 다시 갈렸고, 5rem으로 올려 해소했다. content-list.md v0.21.2 → v0.22.0 (MINOR)

### Fixed
- ContentList: 열 이름 라벨에 `white-space: nowrap` — `--content-list-meta-cols`로 좁은 폭을 주면 "조회"가 "조 / 회"로 갈라져 머리 줄 높이가 늘어났다(4rem에서 실제 발생).
- ContentList: 열 이름이 있는 목록의 **empty 상태가 번호 거터 칸에 찌그러지던 문제 수정.** 열 이름이 있으면 컨테이너가 grid가 되는데, `.empty-state`는 subgrid 자식이 아니라 auto 배치로 1번 열에만 들어갔다 — 폭이 1200px → **197.7px**. 직계 자식 전부에 `grid-column: 1 / -1`을 주고, 그중 header와 ul만 subgrid로 열을 물려받게 했다. `empty-state` 외에 `skeleton`(loading)·`banner`(error)를 컨테이너 안에 넣는 경우도 같은 규칙으로 덮인다. empty 프리뷰의 header에도 열 이름을 넣었다 — 결과가 돌아왔을 때 목록의 머리가 흔들리지 않아야 하므로 열 이름은 empty에서도 그대로 둔다. 상태 표에 이 규칙을 명시. content-list.md v0.21.0 → v0.21.1 (PATCH)

### Removed
- ContentList: 요약문(`__excerpt`) variant 제거. 열 이름이 기본이 된 이상 둘은 같은 목록에서 성립하지 않는다 — 요약문이 있으면 행이 세로로 길어져 열 정렬이 무너지므로, 남겨두면 "기본을 켜면 꺼지는 variant"가 되어 기본값이 두 개인 셈이 된다. 딸린 규칙도 함께 걷어냈다: `__excerpt` 스타일, `__excerpt + __meta` 간격 규칙, 데스크톱의 `:has(.content-list__excerpt)` 세로 유지 분기, 근접성 지침("덩어리 나누기") 절, 텍스트 색 위계표의 요약문 행, Do/Don't 4항목, 프리뷰 1블록. 열 이름 가드가 `:has(.content-list__columns):not(:has(.content-list__excerpt))`에서 `:has(.content-list__columns)`로 단순해졌다(21곳). `_requests.md`의 REQ-001 구조도에서도 제거. 색 위계는 제목 → 번호 → 메타 3층으로 유지되고 WCAG AA 기준(제목 18.43:1 · 번호 8.68:1 · 메타 4.51:1)도 그대로다. content-list.md v0.20.0 → v0.21.0 (0.x draft — 1.0 이후였다면 MAJOR)

### Removed
- ContentList: 플래그(`__flag`) 뱃지 슬롯 제거. 운영자가 문서 하나에 내리는 판단은 사실상 "꼭 봐라" 하나뿐이라, 라벨 슬롯을 열어두면 라벨만 늘어난다(`서식`·`인기`·`NEW`…). 후보를 검토했을 때 남는 것이 없었다 — 분류·형식은 **속성**이라 분류 열의 몫이고, `접수중`·`마감`은 **상태**라 시간에 따라 바뀌어 수동 라벨로 감당되지 않으며, `정정`·`개정`은 **내용**이라 제목에 적으면 된다. 딸린 규칙도 함께 걷어냈다: `__flag` 스타일, `__headline`의 baseline 정렬 근거, `__link`의 `flex: 0 1 auto` 주석, Do/Don't 4항목, 접근성 항목, 라벨 매핑 표.

### Changed
- ContentList: 고정 행 표시를 중립(gray-50)에서 **주의 톤 + 좌측 강조 바**로. 중립은 hover와 충돌하지 않지만 눈에 걸리지 않아 고정의 목적을 못 했다. 브랜드는 못 쓴다 — hover가 브랜드 틴트라 "지금 올려둔 행"과 같은 색이 된다. 배경 `--color-surface-caution-subtle`(orange-50) + 좌측 `--color-fill-caution` 2px. 바는 `border-left`가 아니라 `box-shadow: inset`이다 — border면 행의 안쪽 폭이 2px 줄어 고정 행만 내용이 밀리고 subgrid 열 정렬도 어긋난다(측정: 제목 시작선 390/1300px에서 48 / 86.9px로 변화 없음). hover는 배경만 바꾸고 바는 남아, 올려둔 동안에도 고정이라는 사실이 유지된다. 5안(gray-50 / orange-50 / orange-50+바 / orange-100 / 바만)을 렌더 비교해 선택. content-list.md v0.29.0 → v0.30.0 (MINOR)

### Added
- ContentList: 고정 항목 variant `content-list__item--pinned` 추가 — 라벨 대신 **행 배경**(`--color-surface-subtle`)으로 표시한다. 배경이면 라벨이 필요 없고, 오래된 글이 맨 위에 있는 이유도 함께 설명된다. 색은 **중립**이다: hover가 브랜드 틴트(`--color-action-brand-subtle`)이므로 고정까지 브랜드로 두면 "지금 올려둔 행"과 "고정된 행"이 같은 색으로 읽힌다 — 중립 = 고정, 브랜드 = hover로 가른다. hover가 명시도로 이겨 고정 행에 올려도 브랜드로 바뀐다(측정 확인). 배경색만으로는 색각 이상에 전달되지 않으므로 필요하면 `sr-only`를 넣고, 고정 항목을 맨 위에 모으는 것 자체가 순서로 주는 신호임을 접근성 절에 명시. content-list.md v0.28.0 → v0.29.0 (MINOR)

### Fixed
- ContentList: 플래그를 걷어내면서 그 아래 붙어 있던 **기본 `@supports` subgrid 블록까지 함께 지웠던 것 복구.** 삭제 범위를 "Flag 주석부터 다음 `/* ── ` 주석까지"로 잡았는데 그 사이에 목록 grid 정의(`.content-list { display:grid }`, `.content-list > .content-list__item { grid-column: 1/-1 }`)가 들어 있었다. 항목이 열 span을 잃고 auto 배치되면서 **행들이 세로가 아니라 가로로 늘어섰다.** 렌더로 확인하고 HEAD에서 복원했다.

### Changed
- ContentList: 읽은 제목의 색을 `--color-text-label`(gray-700) → `--color-text-subtle`(gray-500)로 내렸다. 안 읽음(gray-950)과의 대비가 **2.12:1**이라 나란히 놓고 봐야 겨우 보였다 — subtle이면 **4.09:1**로 벌어져 훑는 중에도 걸린다. 읽은 제목이 메타와 같은 값이 되는 것은 의도다: 이미 읽은 항목은 더 이상 목적지가 아니므로 "제목 → 번호 → 메타" 위계에서 내려온다(크기와 줄 위치가 달라 메타와 뒤섞이지는 않는다). 흰 배경 4.51:1로 WCAG AA 본문 기준은 유지 — 한 단계 더(gray-400)는 3.08:1로 미달이다. 행 전체 `opacity`로 흐리게 하는 방법은 메타까지 3:1 아래로 떨어뜨려 채택하지 않았고, DON'T로 명시했다. 사용 지침에 후보 3종 대비표 추가. content-list.md v0.27.0 → v0.28.0 (MINOR)
- ContentList: `sm`에서 행의 분류를 **"전체"를 고른 동안에만** 보여준다. 특정 분류로 좁히면 모든 행이 같은 분류라 중복이지만, 전체일 때는 행마다 분류가 달라 그 값이 실제 정보다. 판정은 첫 칩("전체")의 `tag--selected` 여부로 하므로 앱은 선택 클래스만 옮기면 된다(`:not(:has(.content-list__filter > .tag:first-child.tag--selected))`). 구분자 제거도 **같은 조건**에 걸었다 — 분류가 보이는데 구분자만 없으면 "4대보험 2024.03.20"처럼 붙어 읽힌다. "전체" 칩의 순서 고정을 Do/Don't와 AI 주석에 명시. content-list.md v0.26.0 → v0.27.0 (MINOR)
- ContentList: 분류 필터 행을 header **뒤**(소제목 아래, 목록 바로 위)로 옮겼다. 소제목은 목록 전체를 이름 붙이는 것이고 필터는 그 목록에 걸리는 조건이라, 이름 위에 조건이 오면 위계가 뒤집힌다. 머리 구획선(`--color-border-strong`)도 header에서 필터로 넘겼다 — 소제목과 필터는 "이 목록의 머리"라는 한 덩어리이고, 그 사이에 선을 그으면 필터가 목록의 조건이 아니라 별개 블록으로 읽힌다. 선은 머리 전체와 본문 사이에 하나만 둔다(측정: header 하단선 0px, 필터 하단선 gray-500). content-list.md v0.25.0 → v0.25.1 (PATCH)
- ContentList: `sm`에서 **번호(`__no`)도 숨기고**, 분류 숨김의 조건을 없앴다. 분류 필터 행은 `sm`에서 **필수**가 됐다(Variant 기본값 "있음") — 행의 분류를 숨기므로 필터가 없으면 분류를 다룰 방법이 사라진다. 번호가 빠지면서 신규 아이콘이 거터에 혼자 남으므로, 번호에 바짝 붙이려던 2px 여백을 0으로 되돌렸다. ⚠️ 번호는 상담 중 "165번 글 보세요"로 지목하는 식별자다 — 안내를 받는 쪽이 모바일이면 목록에서 번호를 찾을 수 없다. 상세 화면에서 번호를 노출하거나 링크로 안내하는 경로를 함께 둘 것을 사용 지침·접근성 절에 명시했다(`sm`에서는 `display:none`이라 접근성 트리에서도 사라진다). 구현에서 걸린 것 둘: sm 오버라이드를 필터 블록(파일 앞쪽)에 뒀더니 `.content-list__no` 기본 규칙(뒤쪽)에 순서로 져서 숨겨지지 않았고 — sm 블록으로 옮겨 해결. 그리고 구분자 제거 셀렉터를 `.content-list__cat + *::before`(0,1,0)로 단순화했더니 기본 규칙 `.content-list__meta > :not(:first-child)::before`(0,2,0)에 명시도로 져서 메타 줄이 다시 "·"로 시작했다 — `.content-list__meta >`를 붙여 해결. content-list.md v0.24.0 → v0.25.0 (MINOR)

### Added
- ContentList: 문서에 **모바일(sm) 미리보기** 추가. 실제 sm 대응은 `@media (max-width: 767px)`라 뷰포트가 좁아야 걸리는데 문서 뷰포트는 넓어, 지금까지 폭을 줄여보지 않으면 모바일 레이아웃을 볼 수 없었다. `iframe`으로 **진짜 390px 뷰포트**를 만들어 그 안에 렌더한다 — 페이지에 이미 로드된 토큰·컴포넌트 CSS와 아이콘 스프라이트를 그대로 옮기므로 **규칙을 옮겨 적지 않는다.** 컴포넌트를 고치면 미리보기도 자동으로 따라온다. 높이는 iframe 내부 `body.scrollHeight`로 맞추고, 웹폰트 로드 후 한 번 더 맞춘다(줄 수가 달라져 높이가 바뀐다). content-list.md v0.25.1 → v0.26.0 (MINOR)
- ContentList: 분류 필터 슬롯 `.content-list__filter` 추가 — 분류로 목록을 거르는 가로 스크롤 칩 행. **`sm` 전용**이고 `md` 이상에서는 숨는다. `__columns`(열 이름, md 이상 전용)와 정확히 반대로 동작해 둘이 동시에 보이지 않는다. `md` 이상에서는 분류가 열로 서서 눈으로 훑을 수 있지만 `sm`에서는 그 열이 사라진다 — 훑는 대신 **눌러서 거르는** 방식으로 바꾼다. Badge가 아니라 **Tag** 컴포넌트를 쓴다(`tag--pill tag--md`, 첫 칩 "전체"가 `tag--selected`) — 누를 수 있어야 하고 Badge는 비인터랙티브 상태 표시 전용이다. 줄바꿈하지 않고 가로 스크롤한다(분류가 많으면 필터가 목록보다 커진다). flex item의 `min-width` 기본값이 `auto`라 `overflow-x`만으로는 부족했다 — `min-width: 0`을 주지 않으면 칩들이 컨테이너를 밀어 목록 전체가 가로로 넘쳤다(실측). content-list.md v0.22.0 → v0.23.0 (MINOR)

### Changed
- ContentList: 열 이름을 **기본값으로** 전환하고 modifier 클래스를 없앴다. `content-list-container--columns` 대신 `.content-list__columns` 슬롯의 **존재**로 켜진다 — 라벨과 열 정렬은 한 몸이라, 둘 중 하나만 켜진 상태(라벨은 있는데 값이 안 맞거나, 값은 열인데 이름이 없거나)를 만들 수 있으면 안 된다. 요약문이 있는 목록에서는 `:not(:has(.content-list__excerpt))`로 통째로 꺼진다 — 본문이 세로로 길어지면 열 정렬이 성립하지 않는데 라벨만 남으면 값과 어긋난 채로 서기 때문이다. 마크업에 슬롯을 둬도 자동으로 인라인 메타로 돌아간다(측정: 라벨 display none, 컨테이너 flex, `__unit`과 가운뎃점 복귀). content-list.md v0.19.0 → v0.20.0 (MINOR)

### Added
- ContentList: 열 이름 variant `content-list-container--columns` + `.content-list__columns` 슬롯 추가. header에 `분류·작성일·조회`를 두고 메타를 실제 열로 정렬한다. `md` 이상에서만 동작하고 `sm`에서는 기본 디자인(인라인 메타 + 값에 붙은 단위)으로 돌아간다. 기본값이 아니라 variant인 이유: 열 이름은 "누르면 정렬된다"는 신호를 주고 열 폭이 고정되면 좁은 화면에서 접히지 않는다 — ContentList를 만든 이유와 정면으로 부딪힌다. 요약문(`__excerpt`)과는 함께 쓸 수 없다. 열 폭은 px로 박지 않고 **subgrid로 잡는다** — header와 각 행은 서로 다른 요소 안에 있지만 container가 정의한 같은 열을 물려받으므로, 폭이 그 목록에 실제로 들어온 가장 긴 값에 맞춰지고 라벨과 값이 저절로 맞는다(측정: 1200px에서 923.4 / 1046.4 / 1127.5로 header 라벨과 전 행 일치, 뷰어 cascade에서도 재확인). 값의 단위 라벨은 `.content-list__unit`으로 분리해 마크업에 항상 두고, `--columns`에서만 숨긴다 — `sm`으로 돌아가면 다시 나와 정보가 빠지지 않는다. content-list.md v0.18.0 → v0.19.0 (MINOR)
- Color: `--color-border-faint` 추가 (`var(--color-gray-100)`) — 본문형 목록의 항목 구분선용. `--color-border-subtle`(gray-200)과의 차이는 밝기가 아니라 **선이 하는 일**이다. 데이터 표의 행 구분선은 실제로 일을 한다(행을 가로질러 값을 비교하려면 선이 행을 잡아줘야 한다). 본문형 목록은 비교 대상이 아니라 읽을 것을 고르는 나열이라 선은 항목이 바뀌는 지점만 알리면 되는데, 같은 값이라도 20줄 이어지면 무게가 누적돼 제목과 경쟁한다. color.md의 "중립 border 3종 선택 기준"을 4종으로 확장하고, 데이터 표에 faint를 쓰지 말라는 DON'T를 추가. color.md v1.4.0 → v1.5.0 (MINOR)
- ContentList: 읽음 상태 variant `content-list__item--read` 추가 — 이미 본 글의 제목을 **굵기 body + 색 label**로 낮춘다. 게시판은 같은 목록을 반복해서 여는 화면이라, 무엇을 이미 봤는지가 보이지 않으면 매번 처음부터 훑게 된다. 색만 한 단계 내리면 눈에 띄지 않고, 눈에 띌 만큼 내리면 제목이 메타 수준으로 주저앉는다 — **굵기를 주 신호, 색을 보조 신호**로 둔다(6안 렌더 비교 후 선택). 굵기와 색이 함께 바뀌므로 색각 이상에서도 구분된다. hover는 읽음보다 명시도가 높아 읽은 항목도 hover 시 브랜드 색으로 돌아온다. 사용 지침에 `--read` 클래스와 `:visited` 두 방식의 비교표(서버 기록 vs 브라우저 방문 기록 · 기기 간 동기화 · 기록 삭제 영향 · 바꿀 수 있는 속성 · 백엔드 필요 여부)와 둘을 섞지 말라는 경고 추가. content-list.md v0.14.1 → v0.15.0 (MINOR)
- ContentList: `.content-list__new`(icon-new 아이콘) 복원 — v0.12.0에서 `__flag`로 합쳤던 것을 되돌린다. **신규와 플래그는 성격이 다르다** — 신규는 등록일 기준 시스템 자동 표시, 플래그는 운영자 수동 표시이고 **둘은 동시에 나올 수 있다**(새로 올라온 필독 공지). 형태를 아이콘/뱃지로 갈라 둘이 나란히 붙어도 무엇이 자동이고 무엇이 판단인지 읽히게 한다 — 둘 다 Badge면 그 구분이 사라진다. 순서 고정: 제목 → `__new` → `__flag`. `align-self: center` — headline이 baseline 정렬인데 아이콘은 글자가 없어 기준선이 잡히지 않는다. 사용 지침에 두 표시의 비교표와 Do/Don't 추가. content-list.md v0.12.0 → v0.13.0 (MINOR)
- ContentList: 플래그 슬롯 `.content-list__flag` + `.content-list__headline` 래퍼 추가. `필독`·`공지`·`마감임박`처럼 신규 외의 상태 표시를 제목 뒤에 붙인다. Badge 컴포넌트를 그대로 쓰고(`badge--error`·`badge--caution`·`badge--brand`), 색 강도 = 우선순위로 맞춘다. 사용 지침에 라벨·style 매핑 예시와 남용 방지 규칙(항목당 하나, 목록의 20% 이내) 추가.
- Color: `--color-border-strong` 추가 (`var(--color-gray-500)`) — 섹션 경계용 구분선. 행 구분선(`--color-border-subtle`)보다 위계가 높은 선이 필요한데 중립 border 스케일이 subtle(gray-200) / default(gray-300)에서 끊겨 있었다. `--color-border-selected`·`--color-border-complete`가 같은 gray-500이지만 선택·검증 **상태**를 뜻하므로 구획선으로 전용하지 않는다. color.md에 중립 border 3종 선택 기준 표 추가(무엇을 나누는 선인가 — 같은 층위 나열 / 인터랙션 윤곽 / 층위가 다른 구획). color.md v1.3.0 → v1.4.0 (MINOR)
- ContentList: 게시물 번호 슬롯 `.content-list__no` 추가(기본 표시). 상담원이 "165번 글 보세요"처럼 항목을 지목하는 식별자다 — 목록에 없으면 전화로 글을 특정할 방법이 사라진다. 오른쪽 메타가 아니라 **왼쪽 거터**에 둔다: 읽을지 판단하는 정보가 아니라 지목하는 식별자라 역할이 다르고, 메타에 섞으면 분류·날짜와 같은 무게로 읽혀 지목 기능이 묻힌다. `min-width: 4ch` + `tabular-nums`로 자릿수와 무관하게 제목 시작선을 고정. 링크 밖에 두어 스크린리더 링크명은 제목만 유지하고, `aria-hidden`은 붙이지 않는다(실제 식별자). `sm`에서는 세로로 쌓지 않고 `:has()` grid로 왼쪽 거터를 유지 — 좁은 화면일수록 번호를 훑는 동작이 중요하다. content-list.md v0.5.1 → v0.6.0 (MINOR)
- ContentList: 신규 Organism 컴포넌트 — 게시판·자료실처럼 읽을거리를 나열하는 목록. REQ-001 우선순위 1의 첫 구현. 정보 테이블(`table--info`)의 시각 톤(좌우 라인·radius 없이 상하 구분선만, 줄바꿈 허용, `.table` 기본 행 높이)을 채택하고, 컬럼 헤더와 "hover 없음"은 채택하지 않는다 — 행 전체가 링크이고 컬럼이 남으면 `sm`에서 접히지 않기 때문. 링크는 제목만 감싸고 행 전체 클릭은 `::after` 오버레이가 담당한다(스크린리더 링크명에 메타가 섞이지 않도록). layout(row·stack) · header · excerpt · 신규 표시 variant. `sm`에서 세로 스택 reflow. build.py FILE_ORDER 등록. content-list.md v0.1.0
- 컴포넌트 추가 요청 문서 신설 — `components/_requests.md`. 시스템에 없어 프로토타입을 막는 컴포넌트를 접수하는 백로그. 첫 항목으로 REQ-001 콘텐츠(읽을거리) 계열 접수: Card·ContentList·ContentHeader·ContentBody·AttachmentList(우선순위 1), PageHeader·ContentNav(2), ListPage·DetailPage Pattern(3). 모바일 지원 범위·밀도 정책·표/게시판 구분 결정을 반영. _requests.md v0.3.0
- Banner: 신규 Molecule 컴포넌트 — 페이지·섹션 내 고정 노출되는 인라인 상태 메시지 바. Toast의 상태 스타일 체계(info·success·caution·error) 참고, 인라인(그림자·고정위치·자동소멸 없음)으로 적응. title·action(선택) 지원. 닫기 버튼은 두지 않음 — 지속 노출이 목적이므로 닫힘은 Toast의 역할로 분리(조건 해소 시 앱이 제거). 기존 product.md·alert.md가 참조만 하고 구현이 없던 컴포넌트를 정식 추가. build.py FILE_ORDER 등록. banner.md v0.1.0
- Color: `--color-text-inverse-alpha` 추가 (`color-mix(in srgb, var(--color-gray-0) 65%, transparent)`) — 어두운 배경(surface-dark) 위 흰 텍스트 계층 구분용. 툴팁 내 라벨/값처럼 같은 어두운 배경에서 계층이 필요할 때. `text-body-alpha`·`text-brand-alpha`의 inverse 대응. color.md v1.2.0 → v1.3.0 (MINOR)
- Table: 헤더고정(sticky header) 지원 — `<table>`에 `table--sticky-head` 추가 시 세로 스크롤에서 thead가 상단 고정. 스크롤 래퍼에 max-height 필요. 열고정과 조합 시 코너 z-index 자동 처리. data.md v0.6.2 → v0.7.0 (MINOR)
- Badge: `badge--disabled` 상태 variant 추가 — 비활성·비적용을 나타내는 시각 스타일(비인터랙티브 유지). tint·fill·line 전부 대응. 비활성 Tab 내 카운트 badge 등에서 하드코딩하던 disabled 색 처리를 표준화. badge.md v1.0.2 → v1.1.0 (MINOR)
- Tab: 신규 Molecule 컴포넌트 — tablist/tab/tabpanel 패턴. 상태(default·hover·selected·disabled)·badge 카운트·키보드 내비게이션. tab.md v0.1.0

### Fixed
- 뷰어/빌드: 문서용 예시 CSS 누수 전면 정리. 추출된 CSS는 `components.css`로 번들될 뿐 아니라 **뷰어에서 그 문서를 열 때 `<head>`에 주입된다**(build.py 1806행) — 그래서 번들 대상이 아닌 토큰 문서의 예시도 실제로 적용되고 있었다. 전 문서를 감사해 예시·템플릿 11곳을 ` ```css example `로 전환: `components/_index.md`(CSS 조합 방식 예시), `components/_spec.md`×3(템플릿), `tokens/_spec.md`×2, `tokens/elevation.md`×2(z-index 사용 예시), `tokens/motion.md`(`prefers-reduced-motion` 전역 스니펫 — 뷰어에서 이 문서를 열면 `*{transition-duration:0.01ms!important}`가 주입돼 뷰어 전체 애니메이션이 죽고 있었다), `tokens/stroke.md`, `workflow/designer.md`. 컴포넌트 문서의 구현 블록은 건드리지 않았다(`.btn`·`button.icon-on--md:hover`는 button.md·icon-button.md의 실제 구현으로 정상 유지). `build.py`에 플레이스홀더(`{ ... }`·`[클래스명]` 등) 검출 경고를 추가하고 — 되돌려 넣어 경고가 실제로 뜨는 것을 확인했다 — `components/_spec.md`에 펜스 규칙을 명시했다.
- ContentList/뷰어: **문서용 예시 CSS가 실제 구현으로 적용되던 버그 수정.** `build.py`는 문서의 모든 ` ```css ` 펜스를 구현으로 추출해 `components.css`로 번들하는데, 읽음 상태 지침에 대안으로 적어둔 `.content-list__link:visited { color: subtle }` 스니펫이 그대로 실제 규칙이 됐다(`components.css:5299`). 프리뷰 링크는 `href="#"` — 현재 페이지 URL로 해석되어 **항상 방문한 링크**라, 실제 브라우저에서 제목이 전부 회색으로 보였다. 헤드리스 브라우저는 방문 기록이 없어 측정에는 잡히지 않았다. 예시 펜스를 ` ```css example `로 바꿔 추출에서 제외한다 — 추출 정규식이 `css` 뒤 개행을 요구하므로 자동으로 걸러지고, marked는 첫 단어를 언어로 잡아 하이라이팅은 유지된다. `build.py`에 이 규칙과 사고 경위를 주석으로 남겼다. content-list.md v0.17.2 → v0.17.3 (PATCH)
- ContentList: 신규 아이콘이 숫자보다 아래로 처져 보이던 문제 수정 — 크기를 `--icon-sm`(16px) → `--icon-badge`(12px)로 바꿨다. **정렬이 아니라 크기 문제였다**: 상자 중심도, canvas로 잰 숫자 글리프의 실제 잉크 중심도 오차 0이었는데(390/1200px 모두), 16px 원이 숫자 글리프(11px)보다 커서 위아래로 2.5px씩 삐져나오고 있었다. 숫자는 내려긋는 획이 없어 베이스라인이 곧 바닥으로 읽히므로, 아래로 삐져나온 2.5px만 "가라앉은" 것으로 보인다. 1px 밀어 올리는 광학 보정 대신 크기를 역할에 맞췄다 — icon.md의 정의상 `sm`은 "sm 컴포넌트" 크기이고 `badge`는 "보조 인디케이터(badge 내부, **메타 정보**)"인데, 번호 옆 신규 표시는 후자다. 처음부터 토큰 선택이 틀렸던 것이고, 크기가 역할에 맞지 않으면 정렬로 드러난다. 측정: 삐져나오는 양 2.5px → 0.5px. content-list.md v0.17.1 → v0.17.2 (PATCH)
- ContentList: 신규 아이콘을 번호에 바짝 붙였다(간격 16px → `--space-gap-2xs` 2px). 번호와 신규는 "몇 번 글이고 새 글인가"라는 한 덩어리인데, 본문과 같은 간격으로 벌어져 있어 셋이 균등하게 나열됐다. 열 간격을 item의 일괄 `gap`에서 각 열의 `margin-inline-start`로 바꿨다 — gap 하나로는 거터 안(2px)과 거터→본문(16px)의 차이를 낼 수 없고, margin이면 flex 폴백과 grid에서 같은 값이 나온다. 붙이면서 **세로 중심도 맞췄다**: 번호(13px)와 아이콘(16px)이 각자 자기 줄 높이를 쓰고 있어 나란히 두면 3px 어긋났다. 번호도 제목 첫 줄 높이의 상자 안에서 가운데 정렬한다(신규·플래그와 같은 방식). 측정(390/1200px): 번호 오른쪽 끝 44.9/52.9px → 아이콘 왼쪽 46.9/54.9px로 **2px**, 제목 시작선 92.9 → 78.9px(데스크톱 100.9 → 86.9px)로 14px 당겨졌다. 신규가 없는 목록은 번호→제목 16px 그대로. content-list.md v0.17.0 → v0.17.1 (PATCH)
- ContentList: `sm`에서 제목이 2줄로 접힐 때 신규 아이콘이 두 줄 한가운데(11.3px 아래)에 뜨던 문제 수정. 아이콘은 글자가 없어 `__headline`의 baseline 정렬이 통하지 않고, `align-self: center`가 2줄 박스 기준으로 잡혔다. 상단에 붙이되 박스 높이를 제목 첫 줄 높이와 같게 주고 그 안에서 가운데 정렬한다 — 1줄·2줄, 데스크톱·모바일 모두 오차 0. 제목 크기를 `--content-list-title-size` 변수로 빼 아이콘 박스가 자동으로 따라오게 했다(Table의 `--table-row-height`와 같은 cascade 패턴). content-list.md v0.14.0 → v0.14.1 (PATCH)
- ContentList: 문서 프리뷰의 `content-list__headline` div가 중첩돼 있던 것 수정. v0.12.0에서 headline 래퍼를 정규식으로 일괄 삽입할 때 이미 감싼 항목을 한 번 더 감쌌다. 태그 수는 맞아 균형 검사를 통과했지만 구조가 잘못이었다. 프리뷰 마크업 전체를 생성 스크립트로 다시 만들었다.
- ContentList: 요약문이 있는 항목에서 제목과 요약문 사이가 16px로 벌어지던 문제 수정. 데스크톱 가로 배치용 `gap: --space-gap-lg`(16px)가 세로 배치 분기에서 그대로 상속돼, 제목과 메타를 벌리려던 값이 한 덩어리로 읽혀야 할 제목·요약문을 갈라놓고 있었다. `--space-gap-xs`(4px)로 되돌림 — 항목 높이 116px → 92px.
- ContentList: 요약문(`__excerpt`)이 있는 항목에서 제목에 `flex: 1`이 남아 있던 것을 `flex: none`으로 되돌림. `flex: 1`은 가로 배치에서 제목이 남는 **폭**을 채우게 하려는 것인데, 세로 배치에서는 남는 **높이**를 채우게 된다. 현재 마크업에서는 항목 높이가 콘텐츠로 결정돼 증상이 드러나지 않지만, 항목에 고정 높이가 생기면 제목 상자만 늘어나 메타와의 간격이 벌어진다. (제목 글자 크기 자체는 두 경우 모두 17px로 동일 — 렌더 폭 179.45px 일치 확인) content-list.md v0.8.0 → v0.8.1 (PATCH)
- 뷰어: preview 중화 블록의 명시도 수정 — `.component-preview-stage li`(0,1,1)로 두면 컴포넌트 클래스(0,1,0)까지 눌러 ContentList 행의 좌우 padding이 사라졌다. `:where(.component-preview-stage)`로 감싸 (0,0,1)로 낮추고 문서 규칙 전체보다 뒤에 배치했다. 문서 규칙과 같은 명시도에서 순서로 이기고, 컴포넌트 클래스는 둘 다 이긴다.
- 뷰어: 문서용 마크다운 스타일이 컴포넌트 preview로 새던 문제 수정. `.md`의 엘리먼트 규칙(`p`·`ul`·`ol`·`li`·`code`·`em`·`strong`·`blockquote`)을 h1~h3·a와 동일하게 `:where()`로 감싸 명시도를 낮추고, `.component-preview-stage` 안에서 문서 장식을 명시적으로 되돌리는 중화 블록을 추가. 명시도를 낮추는 것만으로는 부족하다 — `:where(.md) li`(0,0,1)가 components.css의 전역 리셋 `*`(0,0,0)을 여전히 이겨 ContentList 행 아래에 `margin-bottom: 4px`가 남았고, hover 배경과 구분선 사이에 틈이 생겼다. `:where(.md) a`의 `border-bottom`(문서 링크 장식)이 컴포넌트 링크에 남아 제목 아래 가로선이 생기던 문제도 같은 블록에서 차단. ContentList에서 처음 드러났으나 `<ul>`·`<a>`를 쓰는 모든 컴포넌트(Dropdown·Combobox·Table의 `.link` 등)에 해당하던 버그다. preview는 프로토타입(tokens.css + components.css)과 같은 결과가 나와야 하며, 문서 스타일이 남으면 그 차이를 컴포넌트 버그로 오해하게 된다.
- Table: `table__cell--fit` 문서와 구현 불일치 해소 — 문서가 "콘텐츠 폭 수축"으로 설명했으나, base `.table`가 `width:100%`이고 셀에 `overflow:hidden`이 걸려 있어 셀 CSS만으로는 auto 레이아웃에서 안정적 수축이 불가(explicit width는 콘텐츠 클리핑, 무지정은 균등 분배)함을 확인. 구현을 실제 역할(`white-space:nowrap` 줄바꿈 방지)에 맞게 문서를 정정하고, 콘텐츠 폭 고정이 필요하면 `table-layout:fixed` + 명시 width를 쓰도록 안내 추가.
- Table: 헤더 셀에 배경(`surface-neutral`) 명시 — sticky 헤더 지원용(기존 `thead` 배경과 동일 색이라 시각 변화 없음). table-cell.md v0.3.0 → v0.4.0 (MINOR)

### Changed
- ContentList: 행 구분선을 `--color-border-subtle`(gray-200) → `--color-border-faint`(gray-100)로. 컨테이너 하단선·첫 항목 상단선·항목 사이 선 3곳. header 하단선(`strong`)과 header 내부 세로 구분자(`subtle`)는 그대로 둔다 — 층위가 다른 선이라 같이 내리면 위계가 무너진다. content-list.md v0.17.3 → v0.18.0 (MINOR)
- ContentList: 신규 아이콘을 `--icon-badge`(12px)에서 `--icon-sm`(16px)으로 되돌리고 광학 보정 1px을 넣었다. 12px은 정렬 문제는 없앴지만 표시가 너무 약했다. 계산상 중심은 이미 맞으므로(상자 중심·글리프 잉크 중심 모두 오차 0) 남은 것은 광학 문제다 — 16px 원이 숫자 글리프(11px)보다 커서 위아래로 2.5px씩 삐져나오는데, 숫자는 내려긋는 획이 없어 베이스라인이 곧 바닥으로 읽혀 아래쪽 2.5px만 "가라앉은" 것으로 보인다. 0·1·1.5·2px를 5배로 렌더해 비교했고 1px이 가장 균형이 좋다(1.5px부터는 반대로 떠 보인다). `transform`이라 열 폭·행 높이에는 영향이 없다.
- ContentList: 거터 열에 들어가는 것을 플래그에서 **신규 아이콘**으로 바꿨다. v0.16.0에서 플래그를 거터로 옮긴 판단이 절반만 맞았다 — 제목 뒤에서 뱃지가 제목 첫 줄을 끊는 문제는 실제였지만, **거터에 둘 수 있는 것은 폭이 고정된 것뿐**이라는 조건을 놓쳤다. 뱃지는 글자 수만큼 폭이 변해서, 열로 두면 목록에서 가장 긴 라벨(`마감임박`)이 뱃지 없는 행까지 포함해 **전 행의 제목 폭**을 깎는다. 신규 아이콘은 언제나 16px이라 그 비용이 없다. 390px·8건 기준 측정: 뱃지가 거터면 제목 시작선 132.9px·목록 높이 652px, 신규 아이콘이 거터면 92.9px·607.8px. 자리가 갈리면서 자동(시스템)/수동(운영자) 구분이 형태(아이콘/뱃지)에 더해 위치로도 남는다. subgrid 열 구성은 그대로 두고 가운데 열의 주인만 교체했다(`:has(.content-list__new)`로 신규가 없는 목록은 열이 사라진다). 측정 검증(390/1200px): 제목 시작선·번호 오른쪽 끝·신규 아이콘 위치 전 행 동일, 신규·플래그 모두 제목 첫 줄 중앙과 오차 0px. content-list.md v0.16.0 → v0.17.0 (MINOR — 마크업 구조 변경, 0.x draft)
- ContentList: 플래그(`__flag`)를 제목 뒤에서 **번호 옆 거터 열**로 옮겼다. 제목 뒤에 두면 제목이 2줄로 접히는 `sm`에서 뱃지가 첫 줄 끝에 걸려 단어 중간에 낀다("… 비즈 [필독] / 씨/세무사랑 …"). 앞에 두는 안은 v0.12.0에서 한 번 물렸는데, 그때 문제는 "앞"이 아니라 **인라인**이었다 — 뱃지 길이가 행마다 달라 제목 시작선이 97/137/161px로 흔들렸다. 열로 두면 폭이 뱃지가 아니라 열에 묶여 시작선이 고정된다(측정: 전 항목 108.9px). 열 폭은 px로 박지 않고 `subgrid`로 목록 전체에서 잡는다 — 그 목록에 실제로 쓰인 가장 넓은 뱃지에 맞춰지므로 뱃지 없는 행이 폭을 뺏기지 않는다. 390px·8건 중 2건 플래그 기준 비교: 고정 56px는 제목 시작선을 132.9px까지 밀어 3개 행이 한 줄 더 접히고 목록 높이가 652px, subgrid는 607px로 **플래그가 없을 때와 동일**. 목록에 플래그가 하나도 없으면 `:has()`로 가운데 열을 없애 `column-gap`이 남지 않게 한다(제목 시작선 60.9px로 복귀). 항목을 `display:contents`로 풀지 않는다 — li 상자가 사라지면 hover 배경·구분선·`__link::after` 오버레이 기준점이 모두 무너진다. subgrid 미지원 브라우저는 `@supports` 밖의 flex 배치가 그대로 남아 시작선만 흔들리고 정보는 전부 보인다. 신규 아이콘(`__new`)은 제목 뒤에 그대로 둔다 — 형태(아이콘/뱃지)에 더해 자리까지 갈라 자동/수동 구분이 이중으로 유지된다. content-list.md v0.15.0 → v0.16.0 (MINOR — 마크업 구조 변경, 0.x draft)
- ContentList: 제목(`__link`)의 `flex: 1` → `flex: 0 1 auto`. 남는 폭을 채우면 플래그가 제목에서 떨어져 행 오른쪽 끝(메타 옆)에 붙는다 — 측정 시 제목 끝에서 **345px** 떨어져 있었다. 플래그는 제목에 딸린 표시이므로 제목 바로 뒤(32px)에 있어야 한다. 요약문이 있는 항목은 `flex: none` 때문에 이미 붙어 있어서 **같은 컴포넌트 안에서 동작이 갈리고 있었다** — base를 `0 1 auto`로 통일하고 요약문 분기의 재지정을 제거했다. 긴 제목은 여전히 줄어들며 말줄임되고 플래그는 남는다. content-list.md v0.13.0 → v0.14.0 (MINOR)
- ContentList: excerpt 프리뷰에도 신규·플래그 예시 추가(162 = 신규 + 필독). 요약문이 있는 항목에서 표시가 어떻게 붙는지 문서에서 바로 확인된다.
- ContentList: 플래그는 링크 **밖**, `__headline`의 형제로 둔다. 링크 안에 넣으면 제목 말줄임에 함께 잘려 정작 읽혀야 할 "필독"이 사라진다. `flex-shrink: 0`으로 제목만 잘리고 플래그는 남게 한다.
- ContentList: 플래그를 제목 **앞이 아니라 뒤**에 둔다. 앞에 두면 뱃지 길이에 따라 제목 시작선이 행마다 달라진다(측정: 97 / 137 / 161px). 뒤에 두면 제목 시작선이 고정된다(측정: 전 항목 89px).
- ContentList: `__headline` 정렬을 `align-items: baseline`으로. `center`면 제목이 2줄로 접히는 `sm`에서 플래그가 두 줄 한가운데(9.2px 아래)에 뜬다. baseline은 1줄·2줄 모두 오차 2px 안쪽이라 breakpoint 분기가 필요 없다. content-list.md v0.11.0 → v0.12.0 (MINOR — 마크업 구조 변경, 0.x draft)
- ContentList: 요약문 다음 메타 앞에 `--space-gap-sm`를 더해 흰 공간을 6px → 14px로. 색 한 단계(v0.10.0)만으로는 부족했다 — **블록 사이 간격이 그 블록의 줄 간격보다 좁으면 다음 블록이 앞 블록의 다음 줄로 읽힌다.** 요약문 줄 사이 흰 공간이 7px인데 요약문↔메타가 6px이라 메타가 요약문의 마지막 줄처럼 붙어 있었다. 줄 간격의 2배를 확보해 끊는다. 제목·요약문은 붙여 한 덩어리(내용), 메타는 떨어뜨려 별개(판단 보조). 사용 지침에 근접성 규칙과 측정치 표 추가. content-list.md v0.10.0 → v0.11.0 (MINOR)
- ContentList: 요약문(`__excerpt`) 색을 `--color-text-subtle`(gray-500) → `--color-text-label`(gray-700)로. 요약문과 메타가 같은 회색이라 두 줄이 한 덩어리로 뭉쳐 어디까지가 내용인지 구분되지 않았다. 칩·아이콘·구분선을 더하지 않고 **색 한 단계**로만 나눈다 — 요약문은 "읽는 내용", 메타는 "읽을지 판단하는 보조 정보"라 역할이 다르다. 사용 지침에 텍스트 3층 색 위계표(body → label → subtle)를 추가하고, 세 층 모두 WCAG AA 본문 기준(4.5:1)을 넘는 것을 접근성 절에 명시 (제목 18.43:1 · 요약문 8.68:1 · 메타 4.51:1). content-list.md v0.9.0 → v0.10.0 (MINOR)
- ContentList: header 하단선 색을 `--color-border-default`(gray-300) → `--color-border-strong`(gray-500)로. 두께는 1px 그대로. 같은 두께에서 색으로만 위계를 만든다 — 머리와 본문은 층위가 다른 구획이고 항목 사이는 같은 층위의 나열이다. 두께로 강조하면 굵은 밑줄 관용구가 된다. content-list.md v0.8.1 → v0.9.0 (MINOR)
- ContentList: 항목을 **번호 거터 + 본문** 두 열로 재구성하고, `md` 이상에서 본문을 가로로 눕힌다 — 제목(좌)과 부가 정보(우)를 한 줄에. `sm`에서는 규칙이 풀려 자연히 세로로 접힌다. `.content-list__body` 래퍼 추가. 한 줄 배치가 표처럼 읽히지 않게 하는 것은 레이아웃이 아니라 메타의 처리라, v0.7.0에서 걷어낸 칩·아이콘은 그대로 두고 배치만 되돌렸다.
- ContentList: 번호를 메타 줄에서 **왼쪽 거터로 이동**(`#165` → `165`). 메타에 두면 앞 항목(분류)의 길이에 따라 번호 위치가 행마다 흔들려 상담 중 번호를 훑는 동작이 불가능하다. 거터에서 `min-width: 4ch` + `tabular-nums`로 자릿수와 무관하게 한 열로 정렬한다. `sm`에서도 거터를 유지.
- ContentList: 데스크톱 가로 배치에서 제목은 한 줄 말줄임, `sm`에서는 2줄 말줄임. 요약문(`__excerpt`)이 있으면 데스크톱에서도 세로 스택을 유지하고 제목도 2줄로 되돌린다(`:has()` 분기). 이때 항목의 세로 가운데 정렬도 해제해 번호가 제목 첫 줄에 맞도록 한다. content-list.md v0.7.0 → v0.8.0 (MINOR)
- ContentList: 항목 구조를 **세로 스택**으로 재설계 — 제목 줄(17px) + 메타 줄. 한 줄에 제목과 메타를 나란히 놓으면 컬럼 없는 표처럼 읽혀 데이터 테이블의 잔상이 남았고, 좁은 화면에서 다시 접어야 했다. 쌓으면 제목이 시각 위계 최상위를 독점하고 `sm`에서 레이아웃을 바꿀 일도 없어진다(좌우 inset만 조정). `:has()` grid 분기와 `layout` variant 제거.
- ContentList: 메타를 **같은 크기·같은 무게의 텍스트 한 줄**로 통일 — 분류 Badge 칩과 조회수 눈 아이콘 제거. `#165 · 4대보험 · 2024.03.20 · 조회 1,011`. 칩·아이콘·텍스트 세 가지 시각 언어가 섞이면 메타가 제목과 경쟁해 위계가 무너진다. 구분이 필요한 분류만 브랜드 색으로 처리. 가운뎃점은 CSS `::before`가 삽입한다(생성 콘텐츠라 스크린리더가 읽지 않는다).
- ContentList: 게시물 번호를 왼쪽 거터에서 메타 줄 맨 앞 `#165`로 이동. 지목 기능은 유지하면서 좌측 번호 컬럼이라는 오래된 게시판 관용구를 걷어낸다. `.content-list__no`의 `min-width: 4ch` 정렬 규칙은 불필요해져 제거.
- ContentList: header 하단선을 2px 브랜드 → `--stroke-sm` `--color-border-default`로, 소제목을 `--font-size-h3` `--color-text-display`로. 굵은 컬러 밑줄은 목록의 머리를 선의 굵기로 강조하는 관용구다. 머리는 선이 아니라 **타이포**로 세운다.
- ContentList: `.content-list__main` 래퍼 제거, `.content-list__cat` 추가. 마크업이 제목·요약·메타 세 슬롯으로 단순해진다. content-list.md v0.6.0 → v0.7.0 (MINOR — 마크업 구조 변경, 0.x draft)
- ContentList: header의 총 건수(`__count`)를 기본 표시에서 제외. 번호와 총 건수는 함께 두지 않는다 — 내림차순 게시판에서는 첫 항목의 번호가 곧 총 건수라 중복이다. `__count`는 필터 결과 수가 판단 근거인 조회 화면용 대안으로 남기고, 어느 쪽을 쓸지 사용 지침에 선택 기준 표를 추가했다.
- ContentList: "게시물 번호는 내부 시퀀스라 읽는 사람에게 의미가 없다"는 기존 지침을 철회. 지식센터처럼 사외 사용자를 상담하는 게시판에서는 번호가 지목 창구로 쓰인다. 메타 지침에서 삭제하고 `__no` 슬롯 설명으로 대체.
- ContentList: 한 줄 행 높이를 `.table` 기본 행(36px)과 정확히 일치시킴 — 세로 padding `--space-8` → `--space-6`. 기존 값은 39px라 표와 나란히 놓았을 때 행 높이가 어긋났다. hover 배경을 `--color-action-neutral-hover` → `--color-action-brand-subtle`로 변경 — 데이터 테이블 행 hover와 같은 토큰. "이 행은 누를 수 있다"는 신호를 시스템 전체에서 통일하고, neutral 계열이 `badge--neutral`(surface-neutral)과 겹쳐 hover 시 분야 칩이 사라져 보이던 문제도 해소. content-list.md v0.1.0 → v0.2.0 (MINOR)
- ContentList: 문서 서술 정정 — "상하 구분선만"은 상단 선 제거 후 사실이 아니다. "가로 구분선만"으로 바꾸고 AI 주석에 header 유무별 상단 경계 규칙을 명시. content-list.md v0.5.0 → v0.5.1 (PATCH)
- ContentList: 컨테이너 상단 선 제거 — header의 2px 브랜드 선이 목록 시작점을 표시하므로 그 위 회색 선은 시작점을 흐린다. header 없이 쓸 때만 `.content-list-container > .content-list:first-child`가 상단 경계를 갖는다. header 높이 `--height-compact`(32px) → `--height-loose`(48px) — 데이터 밀도를 다루는 toolbar가 아니라 섹션 머리이고, 2px 브랜드 선과 짝을 이루려면 그만한 높이가 필요하다. 항목 행 높이(36px)와는 무관하다. content-list.md v0.4.0 → v0.5.0 (MINOR)
- ContentList: header를 색면에서 선으로 변경 — 배경 `--color-surface-neutral`(회색) → `--color-surface-base`(흰색), 하단 border를 `--stroke-md` `--color-border-brand`(2px 브랜드)로. 소제목은 `--color-text-brand-muted`. 정보 테이블에서 가져온 "덜어내는 톤"의 연장이다 — 머리를 색면이 아니라 선으로 표시한다. 색면을 쓰면 hover(브랜드 틴트)와 채색 영역이 둘이 되어 "어디가 누를 수 있는 곳인가"가 흐려지므로, 칠해진 면은 hover 하나로 유지한다. 소제목에 `--color-text-brand`(링크 hover 색)를 쓰지 않은 이유도 같다 — 같은 값이면 바로 아래 목록 제목과 구분이 안 되어 소제목이 클릭 가능한 것처럼 읽힌다. content-list.md v0.3.0 → v0.4.0 (MINOR)
- ContentList: 좌우 inset을 `--space-inset-xl`(12px) → `--space-inset-3xl`(24px)로 확대. `sm`에서는 `--space-inset-2xl`(16px). 데이터 테이블 셀과 같은 12px를 쓰고 있었으나, 테이블은 좌우 테두리 안에 셀이 담기는 반면 ContentList는 좌우 라인이 없어(정보 테이블 톤) 제목이 프레임 없이 가장자리에 바로 붙어 눈에 띄게 좁아 보였다. header도 같은 값으로 맞춰 건수·제목이 항목 제목과 세로 정렬된다. 세로 padding은 그대로라 한 줄 행 높이 36px는 유지. content-list.md v0.2.1 → v0.3.0 (MINOR)
- ContentList: `.content-list__item`에 `margin: 0` 명시 — 행 사이 간격은 border만 담당한다는 것을 컴포넌트 CSS로 못 박는다. 전역 리셋 `*`(0,0,0)에 기대면 호스트 페이지가 `li { margin }`(0,0,1) 하나만 둬도 무너진다. content-list.md v0.2.0 → v0.2.1 (PATCH)
- Components: 계층표에 **구현 상태 표기** 추가 — `✅ 구현됨`(문서+CSS 존재) / `⬜ 계획`(이름만 있음) 두 열로 분리. 표에 누락돼 있던 구현 컴포넌트 6개 추가(Disclosure · Steps · Banner · ImagePreview · Breadcrumb · TableCell). 표에 이름이 있으면 사용 가능한 것으로 읽혀 미구현 컴포넌트를 비슷한 것으로 대체하는 원인이 됐다(게시판 → 데이터 테이블). `_requests.md` 연결 추가. _index.md v1.2.1 → v1.3.0 (MINOR)
- Planner: 컴포넌트 매칭을 `_index.md`의 **`✅ 구현됨` 열로 한정**. 미구현 컴포넌트를 비슷한 것으로 대체하거나 임의 클래스로 채우지 않고 `components/_requests.md`에 요청을 남기도록 지시 추가. planner.md v2.7.1 → v2.8.0 (MINOR)
- Adaptation: 모바일(`sm` < 768px) **정식 지원 범위 포함** — 앱 웹뷰 전환에 따른 결정. Breakpoint 표에 지원 여부 열 추가, `sm` 규칙 신설(Sidebar 기본 숨김·Modal 전체 폭·단독 주요 액션 `--height-loose`). "여러 건을 나열하는 화면 — 표와 목록은 다르게 접힌다" 절 신설 — 데이터 테이블은 `sm`에서도 가로 스크롤(컬럼 reflow 금지), 콘텐츠 목록은 세로 스택 reflow. 판단 기준은 "행끼리 비교하나?". adaptation.md v1.0.0 → v1.1.0 (MINOR)
- Product: B2B 제약이 **화면 종류와 무관하게 동일 적용**됨을 명시 — 게시판·자료실 같은 읽기 화면도 예외 없음. 콘텐츠 계열 밀도 예외 절을 두지 않기로 결정한 데 따른 명확화(스펙 변경 없음). product.md v1.1.0 → v1.1.1 (PATCH)
- Color: `--color-fill-error` 값 `var(--color-red-500)` → `var(--color-red-600)`. color.md v1.1.0 → v1.2.0 (MINOR)
- Dropdown: 폼 필드 DON'T 정책 제거 → 검색 불필요·소수 선택지 컨텍스트에서 FormField Control 허용. `--color-fill-neutral` → `--color-text-subtle` (placeholder·chevron·trigger-icon). `--space-4` → `--space-gap-xs` (count badge·패널 offset). `--color-fill-brand` → `--color-text-brand-vivid` (체크박스 아이콘). `:focus` → `:focus-visible` (옵션). ghost+error hover override 추가. 에러 Anatomy 트리거에 `aria-invalid` 추가. dropdown.md v0.3.0 → v0.4.0 (MINOR)
- Combobox: `--space-4` → `--space-gap-xs` (패널 offset). `--color-fill-brand` → `--color-text-brand-vivid` (체크박스 아이콘). `:focus` → `:focus-visible` (옵션). depends-on 미존재 파일 제거. `aria-live="polite"` empty state 추가. combobox.md v0.2.0 → v0.2.1 (PATCH)
- FormField: depends-on에 tokens/color.md·space.md·typography.md 추가. Primitive 토큰 의도 주석 보강. `div.dropdown` 중복 aria-invalid 제거. AI 주석에 Dropdown/Combobox label 연결 방식 추가. 접근성 유형 선언 업데이트. form-field.md v0.10.0 → v0.10.1 (PATCH)

### Added
- Color: `--color-action-brand-idle` 추가 (`rgba(22,109,238,0.12)`) — toggle off 트랙 배경 전용. color.md v1.0.0 → v1.1.0 (MINOR)
- Toggle: off 트랙 배경 `--color-action-brand-subtle` → `--color-action-brand-idle` 적용

### Changed
- Color: `button` 토큰 그룹 제거, `fill` 그룹으로 통합. `--color-button-brand/neutral/error` → `--color-fill-brand/neutral/error`. color.md v2.12.0 → v3.0.0 (MAJOR)
- Button: fill 토큰 참조 업데이트. button.md v2.0.0 → v2.0.1
- Checkbox / Radio / Toggle / Spinner / Badge / _index: fill 토큰 참조 업데이트 (각 PATCH)
- Progress: fill background `--color-text-brand-vivid` → `--color-fill-brand-vivid`. indeterminate shimmer 주석에 token 사용 의도 명시. AI 주석에 text-helper 출처 추가. progress.md v0.3.8 → v0.3.9
- Segment: 미선택 아이템 텍스트 `color-text-brand-vivid` → `color-text-brand-alpha` (blue-600 50%). segment.md v1.8.0 → v1.9.0
- Badge: brand tint 텍스트 `color-text-brand-vivid` → `color-text-brand` (600). badge.md v4.1.10 → v4.2.0
- Tag: brand 텍스트 `color-text-brand-vivid` → `color-text-brand` (600). tag.md v3.2.2 → v3.3.0
- Tag: shape(rect·pill)·size(sm·md) 차원 추가, ActionGroup과 동일한 rect 스타일 적용, 제거 버튼을 icon-button(icon-on--badge/sm)으로 교체, 사용 지침·AI 주석·Do/Don't 보완. tag.md v1.0.0 → v2.0.0
- Badge: AI 주석에 style 클래스 필수 여부 및 icon--badge 출처(utilities/icon.css) 명시. depends-on에 utilities/icon.css 추가. CSS에 line-height 의도 주석 추가. Do/Don't에 fill+line 동시 사용 금지 항목 추가. badge.md v4.1.9 → v4.1.10

### Added
- Color: `--color-fill-brand-vivid` 추가 — blue-500 범용 브랜드 solid fill 토큰 (버튼·프로그래스바 등). color.md v2.11.0 → v2.12.0
- Color: `--color-text-brand-alpha` 추가 — blue-600 50% 반투명 브랜드 텍스트 토큰. 브랜드 배경 위 미선택·보조 텍스트 계층 구분용 (Segment 미선택 아이템). color.md v2.10.0 → v2.11.0
- Color: `--color-text-body-alpha` 추가 — gray-950 50% 반투명 뉴트럴 텍스트 토큰. color.md v2.9.0 → v2.10.0
- Segment: `lg` 사이즈 추가(`segment--lg`). anatomy 사이즈 가로 정렬. segment.md v1.6.1 → v1.7.0
- Icon: `icon-check`, `icon-warning` 추가 — 입력 상태 표시 아이콘. sprite.svg, categories.json 반영
- Input: `## 동작` 섹션 신설 — 상태 전환 명세표 + 인터랙티브 데모(script). input.md v1.4.0 → v1.5.0
- Input: Anatomy 3분할 — 기본(default·ghost·readonly·disabled) / 상태(error·complete·success + 아이콘 + clearable 조합) / Addon. 상태 아이콘 color :has() CSS 추가
- build.py: `:::preview` 블록 내 `<script>` 지원 추가 — stage 스코프 IIFE로 실행
- Color: `--color-border-complete` 추가 — 에러 없는 입력 완료 테두리 토큰 (gray-500). color.md v2.2.0 → v2.3.0
- Color: `--color-text-success`, `--color-border-success` 추가 — 에러 수정 완료 상태 텍스트·테두리 토큰. color.md v2.1.0 → v2.2.0
- Input: `input--success` state 추가 — 에러 수정 완료, 초록 테두리·텍스트. input.md v1.3.0 → v1.4.0

### Changed
- Input: `input--complete` 색상 수정 — 초록(success) → 회색(`--color-border-complete`), 텍스트 색 오버라이드 제거. complete = 에러 없는 완료, success = 에러 수정 완료로 의미 분리. input.md v1.4.0

### Changed
- Input: style variant `input--line`(언더라인 오판) → `input--ghost`(border-transparent, hover 시 노출)로 교체. v1.1.0 → v1.2.0
- Input: style(box·ghost)·addon(icon-left·icon-right·clearable) variant 추가, 섹션 순서 재정비(개요→Variant→사용 지침→Anatomy→CSS→접근성→Do/Don't), CSS를 별도 섹션으로 분리, preview에서 inline style 제거. v1.0.0 → v1.1.0

### Added
- Icon(tokens): 조합형 CSS 변수 네이밍 규칙(`--icon-[이름]-[부분]`) 및 전체 변수 테이블, 진입 메뉴 아이콘 6종 변수 목록·color 모드 override 표 추가. tokens/icon.md v1.1.0
- Icon(component): `## 단색형 vs 조합형` 섹션 추가 — 유형별 마크업 패턴, 변수 상세는 tokens/icon.md로 위임. components/atoms/icon.md v1.4.1

### Changed
- Icon: 문서 구조 교정 — 섹션 순서 (개요→Variant→사용 지침→Anatomy→CSS→접근성→Do/Don't), `<style>` 블록 제거 후 `## CSS` 섹션 분리, CSS 하드코딩 px → 토큰 var() 교체. icon.md v1.1.0

### Added
- Color: `--color-action-light-hover/pressed/selected/overlay` 추가 — 어두운 배경 위 흰색 인터랙션 상태 토큰. color.md v2.1.0
- Icon: `icon-file-drop` 조합형 — `.icon--brand` 컨텍스트에서 `--icon-file-drop-bg: --color-action-brand-selected`, `.icon--dark` 컨텍스트에서 `--color-action-light-selected` 적용. icon.md v1.2.0
- Icon: badge 사이즈 variant 추가 (Variant 표·CSS)
- Icon: color 차원 추가 (brand · dark · white · disabled) — Variant 표·anatomy·CSS
- Icon: 사용 지침 섹션 추가 (decoration vs standalone vs 버튼 역할 선택 기준)
- Icon: anatomy에 data-component, anatomy-grid/row/label 패턴, 컬러·접근성 패턴 섹션 추가

### Added
- ActionGroup: `.action-group-label` 선택적 그룹 라벨 variant 추가. 텍스트 스타일은 `.text-form-label` 유틸리티 위임, 레이아웃·radius·구분선만 컴포넌트에서 정의. 라벨 있을 때 `aria-labelledby`로 접근성 처리. action-group.md v0.2.0

### Changed
- `--scale-interactive-hover`, `--scale-interactive-press` 제거 → `--translate-interactive-hover: -2px` 추가. scale hover는 GPU 합성 전환 시 폰트 렌더링 불일치 유발. motion.md v2.0.0
- Button: hover 모션 `scale(1.04)` → `translateY(-2px)` 변경

### Changed
- `--line-height-ui`: `var(--line-height-tight)` (1.25) → `1` — 한 줄 UI 세로 중앙 정렬 개선. Pretendard 폰트 메트릭 비대칭으로 인한 시각적 오프셋 해소. typography.md v1.1.0

### Added
- `--space-offset-focus: 2px` — 포커스 링 외곽 간격 토큰 (space.css Semantic)

### Changed
- `--color-background-brand/neutral/error` → `--color-button-brand/neutral/error` 토큰 그룹명 변경 (MAJOR). fill 배경·solid 텍스트·테두리 공용으로 의미 확장. color.md v2.0.0
- `--stroke-lg`: 5px → 4px. 지도 강조 레이어 + 인터랙션 hover/focus 링 공용으로 확장
- Button: hover `box-shadow` 스프레드 값 `4px` 하드코딩 → `var(--stroke-lg)` 토큰 적용

### Added
- Button: `btn--icon-left` · `btn--icon-right` · `btn--icon-only` variant 추가
- Button: `.btn-icon` 내부 아이콘 래퍼 클래스 추가
- `--scale-interactive-hover: 1.04` — 인터랙티브 요소 hover 확대 scale 토큰
- `--scale-interactive-press: 0.97` — 인터랙티브 요소 press 축소 scale 토큰 (예약)
- Button: hover 시 `transform: scale(var(--scale-interactive-hover))` 적용
